import type { Track } from '../types';

export const systemDesignTrack: Track = {
  id: 'system-design',
  name: '系统设计',
  icon: '🏗️',
  tagline: '方法论 + 经典题，应对设计面与架构面',
  description: '系统设计的答题方法论与秒杀、短链、Feed 流等经典场景题，适合中高级岗位的架构面准备。',
  color: 'amber',
  topics: [
    {
      id: 'sd-methodology',
      name: '设计方法论',
      description: '把模糊问题结构化的能力：需求澄清、容量估算、架构分层与权衡表达，是所有设计题的地基。',
      references: [
        { label: 'System Design Primer（GitHub 开源）', url: 'https://github.com/donnemartin/system-design-primer' },
        { label: 'Google SRE Book', url: 'https://sre.google/books/' },
        { label: 'AWS Architecture Center', url: 'https://aws.amazon.com/architecture/' },
      ],
      questions: [
        {
          id: 'sd-methodology-framework',
          title: '系统设计面试有没有通用的答题框架？',
          difficulty: 'basic',
          tags: ['方法论', '答题框架'],
          points: [
            '六步框架（几乎适用所有题）：**① 需求澄清**（功能范围与核心指标）→ **② 容量估算**（QPS、存储、带宽）→ **③ 定义 API 与数据模型** → **④ 画高层架构** → **⑤ 深挖关键模块**（面试官引导的热点）→ **⑥ 瓶颈、扩展与权衡收尾**。',
            '需求澄清问什么：功能边界（哪些必须、哪些可砍）、规模（DAU / QPS / 数据量）、**非功能需求**（一致性 vs 可用性、延迟要求、读写比例）。上来就画图是最大的扣分项——设计题没有标准答案，先对齐问题再谈方案。',
            '把面试当**协作讨论**而不是独白汇报：每给出一个设计就附上代价（“这样做的代价是 X，如果 Y 更重要，可以调整为 Z”）；被质疑时先承认权衡再谈改进，而不是死守原方案。',
            '时间分配参考（45 分钟制）：澄清 5 分钟、估算 5 分钟、数据模型与 API 5 分钟、高层架构 10 分钟、**深挖 15 分钟**、收尾 5 分钟——深挖才是得分主战场，前面的环节都是为它铺路。',
          ],
          followUps: [
            {
              question: '如果面试时间只有 20 分钟，怎么裁剪这套流程？',
              points: [
                '**压缩比优先保深挖**：参考分配——澄清 2 分钟（只问核心功能与一致性/延迟要求）、估算 1-2 分钟（只算 QPS 一个数量级）、高层架构 5 分钟、**深挖 10 分钟**、收尾 1 分钟。估算和数据模型可以口头带过，深挖是分值最高的环节绝不能砍。',
                '**砍环节不砍方法论**：省略的是“步骤的展开”，不是“步骤的意识”——估算一句“按 DAU 推算约 60 万 QPS，读路径必须缓存化”就够；数据模型并进架构图里讲。主动说“时间关系我压缩估算环节”比无声跳过更专业，面试官能分辨“没时间”和“不会”的区别。',
                '**提前埋两个深挖钩子**：时间紧时面试官只会挑一个热点下钻，讲架构时主动抛出“这里最有意思的是缓存一致性/扇出策略，我可以展开”——把深挖方向引导到自己准备最充分的模块，20 分钟制下尤其重要。',
              ],
            },
            {
              question: '需求澄清阶段，最值得问的三个问题是什么？',
              points: [
                '**第一问：核心功能与规模**——“这个系统最核心的 1-2 个功能是什么？DAU / QPS 大概多少？”功能边界决定砍哪些需求（面试默认只做核心链路），规模决定架构档位：万级 QPS 和亿级 QPS 是两套方案。',
                '**第二问：读写比例与数据量**——“读多写少还是写多读多？数据量多大、增长多快？”读写比决定缓存与分库策略（短链 100:1 读、秒杀极端写）；数据量决定存储选型与分片必要性。',
                '**第三问：一致性 vs 可用性**——“数据能容忍短暂不一致吗？故障时保可用还是保正确？”这是所有权衡的总开关：点赞走**最终一致**，库存必须**强一致**。能问出这一问，直接展示 CAP 级别的设计意识。',
                '加分收尾：三问之后**复述需求确认**——“我理解是：日活 X、读多写少、允许秒级不一致，那按这个设计”。既避免答偏，也向面试官证明你在对齐问题而不是背诵模板。',
              ],
            },
          ],
        },
        {
          id: 'sd-methodology-capacity-estimation',
          title: '怎么做容量估算？以日活一亿的短视频应用为例。',
          difficulty: 'basic',
          tags: ['容量估算', 'QPS'],
          points: [
            '只估**能改变架构决策**的量：QPS（决定缓存与限流）、存储（决定分库分表/对象存储）、带宽（决定 CDN）、缓存大小（决定 Redis 集群规模）。算到数量级即可，不要抠小数点。',
            '常用换算基准：**1 天 ≈ 10⁵ 秒**（86400）；读 QPS ≈ 日请求量 / 10⁵；**峰值 ≈ 平均值的 2-10 倍**（晚高峰场景取大）；还要算**写放大**（一次发布触发多少下游任务）。',
            '```text\n日活 1 亿，人均每天看 20 个视频，每次播放平均产生约 30 个请求（元数据、分片、上报的放大系数）：\n- 读 QPS ≈ 1e8 × 20 × 30 / 1e5 = 6e5（平均），峰值按 2 倍 ≈ 120 万 → 必须多级缓存 + CDN\n- 存储：每天新增 1e6 条视频 × 平均 50MB ≈ 50TB/天 → 对象存储 + CDN 分发\n- 视频元数据（MySQL）：1e6 行/天，三年约 1e9 行 → 预留分库分表\n```',
            '表达技巧：**从 DAU 出发**、先写公式再代数字、结论报“约 60 万 QPS”这类数量级；最后必须**回扣架构**——“120 万峰值读 QPS 单库扛不住，所以读路径全部走缓存集群和 CDN”。估算是手段，支撑决策才是目的。',
          ],
          followUps: [
            {
              question: '估完 QPS 之后，怎么推算需要多少机器、多大缓存？',
              points: [
                '记住几个**单机能力基准**再做除法：Nginx 静态内容数万到十几万 QPS、应用服务单机数千 QPS（取决于 RT）、MySQL 单机数千 QPS、Redis 单机 8-10 万 QPS——机器数 ≈ **峰值 QPS / 单机能力 × 冗余系数（2-3 倍）**，冗余是给发布与故障留水位。',
                '**缓存容量**：不是全量数据进缓存，而是**热点数据量 × 单条大小 × 放大系数**（Redis 对象头与碎片通常放大约 1.5 倍）；分片数取两个约束更紧的——单分片 10 万 QPS 的吞吐约束、单实例内存上限（如 32GB）的容量约束，并留一半水位。带宽用 **峰值 QPS × 平均响应大小** 估算，反推要不要上 CDN。',
                '表达要点：每一步都报**数量级和依据**（“单 Redis 约 10 万 QPS，120 万峰值要 12 个分片，留水位上 16 个”），最后回扣一句“这个规模意味着必须集群化 + 预热，单机方案直接排除”——估算的终点永远是架构决策，不是算术表演。',
              ],
            },
          ],
        },
        {
          id: 'sd-methodology-architecture-diagram',
          title: '画架构图有什么套路？如何让图替你说话？',
          difficulty: 'basic',
          tags: ['架构图', '表达'],
          points: [
            '分层模板自上而下：**客户端 → 接入层（DNS/网关/负载均衡）→ 服务层（按业务域拆分的服务）→ 中间件层（缓存/MQ/搜索）→ 存储层（DB/对象存储）**；旁路画异步组件（MQ、定时任务、数据同步）。',
            '**两遍画法**：先画 5-8 个框的粗粒度高层图，讲清数据流向；面试官指定热点后再下钻该模块的细节图（表结构、消息流、状态机）。一次画满所有细节反而失焦，也浪费黑板上最贵的注意力。',
            '图中必须可读：**箭头标协议与同步/异步**（HTTP/gRPC/MQ）、关键组件标技术选型（Nginx、Kafka）、热点路径标注 QPS；“这条边为什么存在”的注解（削峰、解耦、读放大保护）就是你的设计说明。',
            '讲图顺序 = 数据流顺序：以“一个用户请求的生命周期”为主线走一遍，每个框说清“为什么需要它”，而不是照着框念名字。**图是提纲，讲述和权衡才是得分点。**',
          ],
          followUps: [
            {
              question: '接入层的负载均衡有哪几层？算法怎么选？',
              points: [
                '**四层 vs 七层**：**LVS/DPVS 在四层**——只改包的 IP/端口做转发，不解析应用协议，性能极高（百万级并发），放最外层扛量；**Nginx/网关在七层**——能看 HTTP 语义做路由、限流、灰度、鉴权，性能低一档，放内层做业务分流。经典组合：**DNS → LVS（四层）→ Nginx（七层）→ 应用**。',
                '**算法按会话与负载特性选**：轮询/加权轮询适合无状态服务（机器性能不均时加权）；**最少连接**适合请求处理时长波动大的场景；**一致性哈希**适合有会话或缓存亲和的场景（同一用户落同一实例，命中本地缓存），节点增减只迁移相邻数据；**源 IP 哈希**是最朴素的会话保持，但节点变化时全量重分布。',
                '**健康检查是可用性的关键边**：四层做 TCP 探活，七层做 HTTP 探针（检查依赖状态而不是只回 200）；说出**主动检查 + 被动熔断**（连续失败快速摘除）的组合，以及探针间隔的权衡——探太慢故障放大，探太快网络抖动误摘。把负载均衡讲到“算法选择 + 健康检查”，这一层的分就拿满了。',
              ],
            },
          ],
        },
        {
          id: 'sd-methodology-cache-strategy',
          title: '缓存的读写策略怎么选？为什么"先更新数据库，再删除缓存"是主流？',
          difficulty: 'intermediate',
          tags: ['缓存', 'Cache Aside', '一致性'],
          points: [
            '两种主流写策略：**Cache Aside（旁路缓存）**——读：先查缓存，未命中查库并回填；写：**先更新数据库，再删除缓存**，最常用。**Write Behind（写回）**——只写缓存、异步批量刷库，性能极高但有丢数据窗口，适合点赞数、浏览量这类可容忍丢失的计数，不适合库存、余额。',
            '"删除缓存而不是更新缓存"的原因：并发写时后写入的旧值可能覆盖新值，且很多写根本不会被读（冷数据白白计算）。删除方案仍有一致性窗口，工程上用**延迟双删**或**订阅 binlog（Canal）异步删**兜底。',
            '设计底线：**过期时间必须设**（否则内存只增不减）；主动承认**缓存与数据库的一致性是概率问题**——讲清楚不一致窗口有多大、用什么手段兜底，比宣称"强一致"更加分。缓存"穿透/击穿/雪崩"三大防护与 Redis 实现细节是独立的实战考点，见"后端开发 → Redis 缓存"。',
          ],
          followUps: [
            {
              question: '为什么是"先更新数据库，再删除缓存"？反过来会怎样？',
              points: [
                '先删缓存后更库：删除后、库更新前，并发读把**旧值**重新填进缓存——旧值存活到过期，是最差形态；先更库后删缓存：不一致窗口只在"更库完成到删缓存完成"之间，且并发读回填旧值的概率极低（读必须发生在写库前、写缓存后，还要读得比写慢）。',
                '严谨表达是加分项：不存在绝对无窗口的方案——能说清"为什么这个顺序的不一致窗口最小 + 窗口有多大 + 谁兜底（延迟双删/binlog 异步删）"，就是系统设计的成熟度。',
              ],
            },
            {
              question: '延迟双删的"第二次删除"为什么要延迟？延迟多久？',
              points: [
                '针对"先删缓存后更库"或读写并发残留：第一次删后，一个在途读请求可能拿着**旧库值**回填缓存——立刻再删可能删在回填之前；延迟到**读请求回填之后**再删一次，才能清掉旧值。',
                '延迟取值 = 主从同步延迟 + 一次读耗时，通常几百 ms 到秒级（一般用消息/定时任务异步执行第二次删除）；它本身也是概率手段——彻底解法是把"删缓存"挂在 binlog 订阅上，与数据变更**最终对齐**。',
              ],
            },
          ],
        },
        {
          id: 'sd-methodology-tech-choice',
          title: '做技术选型时你如何权衡？怎么向面试官表达？',
          difficulty: 'intermediate',
          tags: ['技术选型', '权衡'],
          points: [
            '决策维度固定化：**功能匹配度、团队熟悉度、社区与运维生态、性能与扩展性、成本（人力 + 资源）、演进风险**。面试时按维度展开，展示的是结构化思维，而不是“我最熟悉 X 所以选 X”。',
            '三个通用权衡轴：**自研 vs 开源 vs 云服务**（由流量规模与团队规模决定）；**成熟稳定 vs 新锐高性能**（由故障成本决定）；**强一致 vs 高可用**（由业务性质决定）。任何选择都有代价——**说出“放弃了什么”比说出“选了什么”更重要**。',
            '表达范式：给 2-3 个候选 → 按维度对比 → 指出本场景权重最高的维度 → 得出结论并附迁移成本。例：消息队列选 Kafka 还是 RocketMQ——日志管道场景 Kafka 胜在生态与吞吐，交易场景 RocketMQ 胜在事务消息与延迟消息。',
            '加分项是**反悔能力**：数据可迁移、接口先做标准化抽象、灰度切换路径清晰。“选型错了能低成本回退”体现的工程成熟度，往往超过“一次选中正确答案”。',
          ],
          followUps: [
            {
              question: '自研、开源、云服务三选一，你的决策依据是什么？',
              points: [
                '**核心判断变量是流量规模与团队人力**：云服务（托管 Kafka、云 Redis）——启动最快、免运维，成本是**费用随规模线性上涨**且被厂商锁定，适合验证期与小团队；开源自建——规模上去后**单位成本更低、可控性强**，代价是自己养运维、踩坑、容量规划的能力；自研——只有当核心需求开源满足不了（极致定制、成本敏感的超大规模、合规隔离）才值得。',
                '一个实用的**量级参考**：日请求千万级以内，云服务通常更划算（人力成本远高于资源差价）；亿级以上且该组件是核心链路时，开源自建的 ROI 才开始反超。同时评估**退出成本**：接口走标准协议（Kafka 协议、SQL），数据格式不绑私有 API，将来迁移才不痛。',
                '答题表达：不要三选一站队，要说**“什么时候换档位”**——“现在 X 量级用云服务，预计到 Y 量级迁移到自建，触发条件是 Z”——把选型讲成随规模演进的路线图，比给一个静态答案高一档。',
              ],
            },
          ],
        },
        {
          id: 'sd-methodology-consistency-tradeoff',
          title: '面试中怎么落地表达 CAP、BASE 与一致性的权衡？',
          difficulty: 'intermediate',
          tags: ['CAP', 'BASE', '一致性'],
          points: [
            '先展示对定理的正确理解：CAP 的 P 是**分区必然发生**的前提，问题从来不是“三选二”，而是“**分区发生时，保一致性还是保可用性**”；网络正常时 C 与 A 可以同时满足。',
            '工程语言翻译：**CP** = 分区时拒绝服务保数据正确（etcd/ZooKeeper：选主、分布式锁、元数据）；**AP** = 分区时继续服务保体验（点赞数、Feed、购物车），事后靠**最终一致**收敛——消息队列重试、对账任务、读修复。',
            '**BASE 的落地三件套**：基本可用（降级、限流、返回兜底数据）、软状态（允许中间态，如“支付处理中”）、最终一致（补偿与对账）。高分的做法是把系统**逐模块标注 CP 或 AP**：账户走 CP、计数走 AP，而不是笼统说“我们系统是 AP 的”。',
            '一致性分级要能展开：**强一致**（共识算法/分布式事务，延迟与可用性代价大）、**最终一致**（异步收敛，互联网主流）、**会话一致/读己之写**（用户自己的操作自己立即可见，用会话粘住同一副本实现）。不同数据配不同级别，是架构成熟度的直接标志。',
          ],
          followUps: [
            {
              question: '最终一致靠什么收敛？读修复和反熵协调分别是什么？',
              points: [
                '收敛的三个引擎：**异步补偿**——消息队列 + 重试 + 死信告警，把跨系统的写操作最终补齐；**对账**——定时全量/增量比对两侧数据、差异触发修复，是最后兜底；**存储层自愈**——复制协议自带的读修复与反熵协调，这是本题重点。',
                '**读修复（Read Repair）**：读请求同时打多个副本（R > 1）时发现版本不一致，顺手把旧副本更新为新版本——**借读流量修复**，零额外成本，但冷数据没人读就永远不修复。**反熵协调（Anti-Entropy）**：副本间后台比对 **Merkle 树**（逐层哈希，快速定位不一致的 key 区间）再同步——不依赖读流量、保证最终收敛，代价是后台资源占用。Dynamo/Cassandra 两者并用：读修复处理热数据，反熵兜底冷数据。',
                '加分收尾：最终一致必须回答**收敛时限与冲突裁决**两个问题——收敛时限决定监控水位（超过 N 分钟未对平要告警）；并发写冲突的裁决方案（时间戳 last-write-wins、版本向量、业务侧合并）决定语义是否正确。“收敛机制 + 冲突裁决 + 时限监控”三件事讲全，才是完整的最终一致。',
              ],
            },
          ],
        },
      ],
    },
    {
      id: 'sd-classic',
      name: '经典系统设计题',
      description: '秒杀、短链、Feed 流等经典场景的综合设计，考察从需求澄清到落地权衡的完整方案能力。',
      references: [
        { label: 'System Design Primer（GitHub 开源）', url: 'https://github.com/donnemartin/system-design-primer' },
        { label: 'Apache Kafka 官方文档', url: 'https://kafka.apache.org/documentation/' },
        { label: 'Redis 官方文档', url: 'https://redis.io/docs/latest/' },
      ],
      questions: [
        {
          id: 'sd-methodology-migration',
          title: '老系统如何平滑重构/迁移？双写、切流、回滚的完整方案怎么做？',
          difficulty: 'advanced',
          tags: ['平滑迁移', '双写', '灰度切流'],
          points: [
            '先立原则：**重构失败的根源几乎都是"大爆炸切换"**——一次性下线老系统，新系统出问题无路可退。平滑迁移的本质是**让新旧系统在一段时间内共存**，用数据与流量逐步验证新系统，随时保有回退能力。先想清楚三个问题再动手：迁移的是什么（代码重构 / 换存储 / 换语言框架 / 拆微服务）、**数据如何迁**、**流量如何切**。',
            '**数据迁移三段论**：**存量**——全量搬迁（工具同步 + 校验），大表用 binlog/快照方式离线导，避免长事务锁源库；**增量**——存量搬迁期间的写入不能丢，标准做法是**双写**（新旧库同时写，以老库为准）或 **CDC 增量追平**；**校验**——全量对账（行数 + 关键字段抽样 + checksum），差异修复后才能进入切流。双写的经典坑要主动讲：**双写不一致**（一边成功一边失败——以老库为准，新库靠对账修复）、**双写顺序**（先写老库成功再写新库，新库失败记补偿，不能让新库失败阻断业务）。',
            '**切流按灰度递进**：按**流量维度**渐进（内部白名单 → 1% → 10% → 50% → 全量），或按**业务维度**渐进（低风险功能 → 核心功能；新用户 → 老用户）；切流开关要**运营级实时生效**（配置中心下发，秒级回切），并配**新旧双读比对**（读流量同时打到新旧，比对结果差异并告警——影子读是数据迁移的神器）。核心口径：**写以老为准 → 读逐步切新 → 最终写也切新 → 下线老系统**，每一步都有明确的准入条件（对账通过、指标达标）。',
            '**回滚预案是一等公民**：切流前先演练回滚；数据回滚最难——**老库在观察期内保持双写**（新系统写入也同步回老库），保证回滚时数据不丢；观察期（如两周全量）过后才允许下线老链路。最后补治理收尾：迁移文档、口径变更通知、旧接口下线节奏（先标废弃给调用方缓冲）——"能回来"的信心给足，迁移才推得动。',
          ],
          followUps: [
            {
              question: '如果迁移期间新旧系统对同一份数据的写顺序无法保证（双写冲突），怎么办？',
              points: [
                '先在架构上消除冲突面：**同一行数据的写入口在同一时刻只有一个**——切流按"数据分片"而不是"功能"做（这部分用户/订单全量切到新库，其余留老库），分片内的数据永远单写，天然无双写冲突；这是分片级灰度比功能级灰度更适合存储迁移的原因。',
                '兜底方案：无法分片时用**版本号/时间戳仲裁**（后写胜，写操作带单调递增版本），或**收敛到单写 + 队列异步同步**（所有写走老库，新库只做准实时镜像 + 只读验证，切换时机推迟到"写切换"那一次原子动作）。答出"分片级切流"这个关键设计，说明踩过真实的存储迁移。',
              ],
            },
          ],
        },
        {
          id: 'sd-short-url',
          title: '设计一个短链接服务（如 t.cn/aB3xY）',
          difficulty: 'basic',
          tags: ['短链接', '哈希', '缓存'],
          points: [
            '**需求澄清**：长 URL → 短码 → **301/302 跳转**；规模假设写 1000 QPS、读 10 万 QPS（读写比 100:1，读路径必须缓存化）；要支持点击统计；短码**不可预测**（防遍历）。',
            '**核心数据模型**：一张映射表（MySQL：id、short_code 唯一索引、long_url、expire_at）。**短码生成两派**：**自增 ID + Base62 编码**（永不重复、实现简单，但可预测，需加混淆或从随机段取号）；**哈希取前 7 位 + 冲突重试**（无规律，7 位 Base62 有 62⁷ ≈ 3.5 万亿空间，冲突概率极低）。',
            '**关键流程**：写——长 URL 查重（唯一索引兜底）→ 发号 → 存映射 → 回填缓存；读——短码查 **Redis**（命中率 95%+），未命中查库回填。发号器用**号段模式**（一次从 DB 取一段 ID 放内存分发），避免每次写都压数据库。',
            '**性能与扩展**：读多写少 → 缓存 + CDN 扛读，MySQL 单库能撑很久；跳转选 **302**（浏览器不缓存，可统计点击）还是 **301**（浏览器缓存，省请求但失去统计），由业务是否需要统计决定。',
            '**权衡收尾**：自增方案省存储但暴露规模（可用混淆缓解）；纯随机方案不可预测但要处理冲突；过期短码是否回收复用取决于业务对短码稳定性的要求。',
          ],
          followUps: [
            {
              question: '如何防止短码被恶意遍历？',
              points: [
                '风险根源：**自增 ID + Base62** 生成的短码连续可预测（aB3xY、aB3xZ…），攻击者可按序遍历拉走全站长链；哈希方案天然无规律，但两者都应从**发号源头**加防护。',
                '手段一：**不可预测的号段**——发号器不连续发号，用随机起点 + 随机步长，或直接在 62⁷ 空间中**随机取号 + 冲突重试**，让短码在空间中稀疏分布，遍历成本从 O(n) 变成近乎抽样。',
                '手段二：**读路径风控**——网关按 IP / 设备指纹做**频控与限流**，单位时间内大量访问不存在的短码直接封禁或弹验证码；不存在短码返回 404 并计入黑名单评分。与主答案的布隆/缓存防线互为补充。',
                '手段三：**业务层隔离**——短码带签名（HMAC 校验防伪造）、设有效期与访问次数上限、内部场景要求登录态。单点防护都可被绕过，**纵深防御**才是完整答案。',
              ],
            },
            {
              question: '同一个长 URL 要不要每次都生成同一个短码？利弊是什么？',
              points: [
                '**要（幂等去重派）**：同一长链只发一个短码——省存储与发号量、全局点击统计唯一、重复粘贴得到同一短链体验一致。实现靠长 URL **唯一索引查重回填**，代价是写路径多一次判重（可用缓存 + 布隆过滤器加速）。',
                '**不要（独立发码派）**：不同用户/渠道对同一长链生成不同短码——可**按渠道归因统计**（同一篇文章在 A/B 渠道的点击分开算）、不暴露“该 URL 已被缩短过”（信息泄露侧信道）、写路径免去查重更简单。',
                '结论表达：**由业务目标决定**——短码只是跳转工具就选去重，短码是运营资产（渠道追踪、活动归因）就选独立发码；折中方案是“默认去重 + 带渠道参数时独立发码”。把选择讲成权衡而不是站队，是这道追问的得分点。',
              ],
            },
          ],
        },
        {
          id: 'sd-leaderboard',
          title: '设计一个游戏/活动排行榜，要求实时更新，能查 TopN 和个人排名。',
          difficulty: 'basic',
          tags: ['排行榜', 'Redis', 'zset'],
          points: [
            '**需求澄清**：按分数排序、同分按达成时间先者居前；实时性要求秒级内；规模决定方案——百万用户和亿级用户是两套架构，先问清楚再设计。',
            '**首选方案 Redis Sorted Set（zset）**：`ZINCRBY` 更新分数、`ZREVRANGE` 取 TopN、`ZREVRANK` 查个人名次、`ZSCORE` 查分数，全部 O(log N)，一个结构覆盖全部需求。同分先到者靠前：用**复合分**（如 score = 主分数 × 1e10 + (2⁶³ - 时间戳)，时间早者加分大）或单独存次级排序键。注意 JS 的 double 只有 2^53 精度，拼分前先验位数，否则会踩追问里的精度陷阱。',
            '**扩展细节**：亿级用户时单 zset 内存和名次计算压力大 → **按大区/分区拆成多个 zset**，各区独立排名，全服 TopN 由各区结果合并；改分极高频 → 客户端聚合后批量提交；赛季榜 → key 按周期命名（如 board:2026W39），到期设置过期自动清理。',
            '**兜底与一致性**：Redis 做主存储就要认真对待可用性（哨兵/集群 + 持久化策略）；极端量级（百万 QPS 更新）退化为**准实时方案**：明细落库，流式引擎（Flink）或定时任务聚合出榜，接受分钟级延迟。',
            '**权衡表达**：zset 方案简单正确，用内存换实时；聚合方案扩展性强但复杂度高一档。按真实 QPS 与用户规模选，不要一上来就堆架构——这也是面试官考察的点。',
          ],
          followUps: [
            {
              question: '要显示用户前后各 10 名怎么做？复合分方案有什么实现陷阱？',
              points: [
                '**邻居名次**：`ZREVRANK` 拿到自己的名次 rank，再 `ZREVRANGE rank-10, rank+10 WITHSCORES` 一次取回前后 10 名——两次 O(log N) 操作搞定；注意 rank 小于 10 时下界取 0、最后一名时上界取 -1，边界处理别丢。',
                '**复合分的精度陷阱**：zset 的 score 是 **double（53 位有效整数）**，`主分数 × 1e10 + (2⁶³ - 时间戳)` 这类拼法，如果两个组成部分的位数相乘超过 2^53，低位会被**静默截断**——同分用户排序错乱且极难排查。必须先算清两个组成部分的位数上限再定偏移量；不够用就换**双 zset 方案**（主分数 zset + 时间戳 zset，读时归并）或应用层排序。',
                '同分的另一种实现：zset 默认按 member 字典序破平局——把 member 编码成“分数时间戳用户 ID”之类自排序字符串也行，但 member 臃肿、按用户反查麻烦。能主动讲出“精度上限先验证、方案带退化路径”，就是这道追问想听的工程深度。',
              ],
            },
          ],
        },
        {
          id: 'sd-bloom-dedup',
          title: '如何在 100 亿 URL 中快速判断一个 URL 是否已被抓取过？',
          difficulty: 'basic',
          tags: ['布隆过滤器', '去重', '海量数据'],
          points: [
            '**规模推导**：100 亿条 URL 均长 100 字节 → 原始数据 1TB 级；哈希表放不进单机内存（仅指针就要几百 GB）；每次查库又是毫秒级，爬虫判重的高频调用完全扛不住——需要**内存级概率结构**。',
            '**布隆过滤器（Bloom Filter）**：m 位的位数组 + k 个哈希函数。插入时把 k 个位置置 1；查询时 k 个位置**全为 1 → 可能存在（可能误判），任一为 0 → 一定不存在**。特性：有假阳性、无假阴性；不能删除元素（要删用计数布隆或布谷鸟过滤器）。',
            '**参数设计**：位数组大小 m ≈ -n·ln p / (ln 2)²。n = 100 亿、误判率 p = 1% 时 m ≈ **12GB**，单机可容纳或用 Redis 分片承载；哈希个数 k ≈ 0.7·m/n ≈ 7 个。误判率每降一个数量级，内存大约翻一倍——内存与误判率的权衡是显式的。',
            '**工程组合拳**：布隆过滤器挡在最前面——判定不存在直接爬；判定“可能存在”再查 Redis/DB 确认，误判的代价只是多一次确认查询。分片按 URL 哈希拆多实例；误判率随插入量增长，要规划**定期全量重建**。',
            '**场景迁移**：缓存防穿透（挡住不存在的 key）、推荐系统曝光去重、垃圾邮件黑名单——共同特征是“**海量集合 + 只判存在性 + 容忍极小误判**”。能迁移场景，说明真的懂原理。',
          ],
          followUps: [
            {
              question: '布隆过滤器不能删除元素，怎么解决？有哪些替代结构？',
              points: [
                '**为什么不能删**：一个比特位被多个元素**共享**，把某元素的 k 个位置回 0 会误伤其他元素，产生**假阴性**（明明存在却判不存在）——“无假阴性”这条概率结构的底线就破了，所以标准布隆过滤器只进不出。',
                '**计数布隆过滤器（CBF）**：每位扩展成 4 bit 计数器，插入加一、删除减一——支持删除，代价是空间扩大数倍，且计数器**溢出会静默出错**（4 bit 计到 16 回绕），要用饱和策略兜底。**布谷鸟过滤器（Cuckoo Filter）**：每个元素存**指纹**到两个候选桶之一，桶满就踢走老元素挪到它的另一候选位；支持删除（删指纹即可）、空间更省、误判率相近，代价是插入可能出现**踢链循环失败**，需要扩容。',
                '**工程兜底**：很多场景直接**定期全量重建**（爬虫代际切换、每日离线任务）就足够，简单可靠；Redis 侧用 **RedisBloom 模块**（含布谷鸟与计数变体）免自研。回答框架：先讲能不能删的原理，再给结构对比，最后给“重建就够了”的务实结论——三层都覆盖就是高分。',
              ],
            },
          ],
        },
        {
          id: 'sd-classic-rbac',
          title: '设计一套权限系统：功能权限、数据权限、字段权限分别怎么建模？',
          difficulty: 'intermediate',
          tags: ['权限系统', 'RBAC', '数据权限', '多租户'],
          points: [
            '**RBAC 三代模型先背骨架**：**RBAC0** = 用户—角色—权限（权限 = 资源 + 操作）三元，**角色是权限的集合、用户挂角色不直接挂权限**（这是可维护性的关键：换人换岗只动角色绑定）；**RBAC1** 加角色继承（总监角色继承经理角色的全部权限——注意继承要克制，深继承链是审计灾难）；**RBAC2** 加约束（互斥角色——申请人与审批人不能同角色；基数约束）。实战通常 RBAC0/1 + 部分约束就够。',
            '**功能权限 / 数据权限 / 字段权限三层分清（多数人答混的分水岭）**：**功能权限**（能不能调这个接口/看这个菜单——路由与按钮级，注解/中间件统一拦截）；**数据权限**（能看哪些行——"只能看本部门的数据"：实现上不是过滤每条 SQL 手写 where，而是**规则引擎拼查询条件**（部门树范围、本人、指定用户组），配 ORM 拦截器或逻辑表统一注入）；**字段权限**（能看哪些列——手机号脱敏、成本字段仅管理层可见：序列化层按权限裁剪输出 schema）。',
            '**鉴权链路与性能**：请求进来 → 网关/中间件做**功能权限**（菜单路由级，粗粒度快）→ 业务层做**数据权限**（查询条件注入）→ 出口做**字段权限**（序列化裁剪）；权限数据**缓存设计**是工程核心：角色—权限映射变更低频高读，**本地缓存 + 版本号失效**（变更时 bump 版本广播，秒级生效），别每次查库；**权限判断失败必须显式 403 并记录审计日志**（谁在什么时候试图访问什么——安全合规刚需）。',
            '**多租户与组织架构的现实复杂度**：SaaS 多租户**隔离优先在数据层做**（租户 id 全表冗余 + ORM 自动注入，重隔离上独立 schema/库）；组织架构是**树**，数据权限沿树继承（本部门含子部门）——**部门变更时的权限重算**（异步刷新 + 缓存淘汰）是经典坑；超级管理员与**临时授权/代理**（休假转交审批权）要有生命周期（到期自动回收）。',
            '收束口径：权限系统的质量看三件事——**变更生效速度**（改权限到全站生效的延迟）、**审计完整性**（每一次拒绝可追溯）、**接入成本**（新接口声明权限的成本要趋近于一个注解）——把"权限是横切基础设施"讲出来，就是平台视角。',
          ],
          followUps: [
            {
              question: 'ABAC（基于属性的权限）是什么？什么时候 RBAC 不够用？',
              points: [
                '**ABAC 按属性算权限**：主体属性（部门/职级/信用分）+ 资源属性（密级/归属部门）+ 环境属性（时间/IP/设备）——规则形如"工作时间 + 内网 + 本部门可编辑，其余只读"；表达力远超 RBAC（RBAC 的"角色"是 ABAC 里的一个属性特例），代价是**规则可解释性与调试难度**（一次拒绝要能回答"命中了哪条规则"——ABAC 系统必须建规则命中日志）。',
                '现实的混合架构：**RBAC 管粗粒度入口（菜单/接口），ABAC/规则引擎管细粒度数据**（行级过滤条件本质就是简化版 ABAC）；什么时候 RBAC 不够：属性维度多且变化（风控、医疗、政务密级）、角色爆炸（角色数量逼近用户数就是信号）——能给出"角色爆炸"这个判断信号，说明真设计过。',
              ],
            },
          ],
        },
        {
          id: 'sd-rate-limiter',
          title: '设计一个分布式限流器，保护服务不被突发流量打垮。',
          difficulty: 'intermediate',
          tags: ['限流', '令牌桶', 'Redis'],
          points: [
            '先定算法：**固定窗口**（每分钟 N 个，实现最简单，但窗口边界处可能出现 2N 的瞬时尖刺）；**滑动窗口**（细分格子统计，平滑）；**漏桶**（恒定速率放行，整形流量）；**令牌桶**（恒速发令牌、桶可攒突发额度）——突发友好，Guava RateLimiter、Sentinel 都是它的变体，是默认选择。',
            '**单机 → 分布式**：多实例各自限 N 实际放行 N × 实例数，配额必须全局共享。基础方案：**Redis + Lua** 原子执行“取令牌/计数”（避免读改写竞态）；滑动窗口可用 Redis ZSET 存请求时间戳，`ZREMRANGEBYSCORE` 清理过期后 `ZCARD` 计数。',
            '**规模化关键设计**：Redis 中心化判定会成为热点和单点。进阶方案是**配额二次分发**——中心只负责把总配额按实例分配（心跳回报用量、动态再平衡），各实例用本地令牌桶执行判定，**限流路径零网络开销**；Sentinel 集群流控、云厂商 API 网关的限流都是这个思路。',
            '**落地必备件**：多维度分级限流（接口 / 用户 / IP）；冷启动预热（缓存刚建好时阈值爬坡）；被限流后的**快速失败**（返回明确的错误码与 Retry-After，而不是让请求挂着）；核心接口与非核心接口阈值隔离。',
            '**权衡表达**：中心化配额强一致、简单，但引入 Redis 依赖与延迟；本地配额高性能，但总放行量有瞬时偏差。多数业务允许 ±5% 偏差——**明确误差容忍度，再选架构**，这句话本身就是加分项。',
          ],
          followUps: [
            {
              question: 'Redis + Lua 做分布式限流，脚本要保证什么？边界场景怎么处理？',
              points: [
                '**Lua 解决的是读改写竞态**：检查计数、判断阈值、写回计数必须**原子执行**，否则两个实例同时读到 99、都放行、都写 100，配额被超发。固定窗口的脚本就是 `INCR` + 首次 `EXPIRE` + 超限判断；滑动窗口用 ZSET：`ZREMRANGEBYSCORE` 清窗口外时间戳 → `ZCARD` 计数 → 未超限则 `ZADD` 当前时间戳，整段一个脚本执行。',
                '**边界细节**：先 `INCR` 后 `EXPIRE` 的两步写法要防“首次 INCR 后进程崩溃、key 永不过期”的脏 key（Lua 内原子做就没事）；Redis 故障时限流器的**失败策略**要先定——fail-open（放行保可用）还是 fail-close（拒绝保依赖），多数业务选 fail-open + 本地兜底限流。',
                '**方案光谱**：要求不高用**本地令牌桶**（每实例按 N/实例数配额，零依赖有偏差）；标准方案 Redis + Lua（准全局精确）；大规模用**配额二次分发**（呼应主答案）；或直接用云网关限流。能按“精确度需求 × 可用性依赖”把光谱排出来，就是完整答案。',
              ],
            },
          ],
        },
        {
          id: 'sd-red-packet',
          title: '设计一个群抢红包功能，如何做到又快又不超发？',
          difficulty: 'intermediate',
          tags: ['抢红包', '秒杀', '库存'],
          points: [
            '**需求澄清**：发红包时**金额预先拆分锁定**（不是抢时现算）；抢的瞬间瞬时流量极高（群人数 × 转发速度）；每人限抢一次；24 小时未抢完原路退回。金额拆分用**二倍均值法**：每次在 [0.01, 剩余金额/剩余人数 × 2] 随机取值，保证不出现 0 元且分布自然。',
            '**核心数据模型**：红包主表（总额、个数、状态、过期时间）+ **红包明细表**（预拆分好的每份金额、领取状态、领取人）。预拆分把“抢”简化为**单行状态更新**，金额确定，天然不会超发。',
            '**关键流程（抢）**：请求按 red_packet_id 分片路由 → Redis **预扣名额**（`DECR` 原子递减，小于 0 直接返回“已抢完”，这一层拦掉 99% 流量）→ 数据库行级条件更新占位一份明细（`UPDATE ... SET user_id = ? WHERE id = ? AND user_id IS NULL`）→ 返回金额。行锁只锁当前红包的明细行，不同红包互不影响。',
            '**性能与扩展**：万人群的红包单行更新成为热点 → **拆分子红包**（一个逻辑红包拆 N 条明细记录，把行锁分散）或 MQ 串行化消费；“Redis 预扣 + MQ 异步落库”是秒杀同款的削峰结构——先让用户抢到，落库慢慢来。',
            '**一致性兜底**：抢到红包等于资金变动，**不能用最终一致丢数据**：落库失败要回滚 Redis 名额并告警；定时对账（Redis 已抢数 vs DB 明细数）兜底；未抢完的退款用**延时任务**（延迟队列或过期扫描）保证必达。',
          ],
          followUps: [
            {
              question: '红包金额怎么拆分既随机又不出现 0 元？',
              points: [
                '**二倍均值法**：每次在 `[0.01, 剩余金额 / 剩余人数 × 2)` 随机取值。上限是剩余均值的两倍，期望恰为均分值，且天然保证后面每人至少 0.01 元（最坏情况前面每人拿 0.01，最后一人拿剩余全部）——**不超发、不少发、无 0 元**同时满足。',
                '代码直觉：\n```text\n// 发红包时拆第 i 份：剩余 remain 元、剩 remainPeople 人\nmax = remain / remainPeople * 2\namount = randomInt(1, floor(max * 100)) / 100   // 全程用整数“分”，避免浮点误差\nremain -= amount; remainPeople -= 1\n```',
                '工程细节：金额全程**整数分**计算；拆分在发红包时**一次性完成并落明细表**，抢的瞬间只做状态更新——随机性在“拆”，不在“抢”，这也是主流程能简化为单行更新的前提。',
                '变体可答：要“手气最佳”分布更陡 → 上限放宽（三倍均值）或截断正态分布；受单笔限额约束 → 随机前先 clamp。能顺着追问给出分布形态的调节手段，说明真推演过这个算法。',
              ],
            },
          ],
        },
        {
          id: 'sd-classic-comment',
          title: '设计一个评论系统：盖楼数据怎么建模？热评排序和计数怎么扛住大 V？',
          difficulty: 'intermediate',
          tags: ['评论系统', '盖楼', '计数服务', '内容审核'],
          points: [
            '**数据模型：两级 vs 无限级（第一个决策点）**：主流是**两级折叠**（根评论 + 子回复，子回复不再展开嵌套——知乎/B 站模式：楼中楼只展示"回复链摘要"）；**无限级盖楼**（贴吧模式）要用**闭包表/物化路径**（`path: 1/5/23/98`）才能高效查整棵子树——纯 parent_id 递归查询在深楼场景是性能灾难。核心表：评论主表（id、subject_id、user_id、content、parent_id、root_id、floor、状态）+ 点赞/回复计数冗余在主表——**为读而写**。',
            '**读写分离的读路径**：首页只取根评论分页 + 子评论数（点开展开再拉）；**热评排序**（点赞数 × 时间衰减的 score，定时任务或流式重算——与排行榜题衔接）；**大 V 评论的读热点**（单条微博评论百万级）：列表接口多级缓存（评论 id 列表缓存 + 内容缓存分离——列表变了内容还能命中）。',
            '**写路径与计数（大 V 的写挑战）**：点赞/评论是高频写——**计数与内容分离**：计数走**内存聚合 + 异步批量落库**（Redis 计数器 + 定时 flush，或专门计数服务），DB 只存内容；精确性对账（异步丢一次就少——补偿任务比对）；**楼号的分配**（并发插入的 floor 连续性：按 root 评论加锁取号或预取号段——与分布式 ID 题的号段模式同构）；写扩散的边界：**回复通知**走信箱（收件人拉自己的通知列表），不做在线推送风暴。',
            '**审核与状态机（内容系统的标配）**：评论状态机：待审/机审通过/人审拒绝/用户删除/**仅自己可见（影子删除）**——影子删除是关键设计（违规者自己还能看到，不知道被删了，防反复重发对抗）；先发后审 vs 先审后发按合规等级（直播弹幕先发后审 + 撤回，时政内容先审）；机审（敏感词/模型打分）+ 人审队列 + **举报回流**闭环。',
            '收束框架：评论系统 = **两级模型 + 读多级缓存 + 写异步计数 + 审核状态机** 四件套；它看似 CRUD 实则全是"高频读写 + 内容治理"的浓缩——面试官用这题筛"只会增删改查"的人，答出计数分离与影子删除就过了筛子。',
          ],
          followUps: [
            {
              question: '明星发了一条微博，百万用户同时来评论和点赞，怎么扛？',
              points: [
                '**入口层**：评论接口**排队削峰**（MQ 或写入限流——"评论成功"的反馈可以异步，用户体验容忍几百毫秒）；**热点 key 分片**（计数值拆成 N 个子 key 随机加，读时聚合——防单 key 自增成为瓶颈）；**写合并**（同一评论的点赞计数在内存窗口内合并成一次落库）。',
                '**读侧与降级**：列表按**分页深度限制**（翻到第 N 页封顶 + "仅展示热门评论"，倒序扫描深分页本来就是灾难——与 MySQL 深分页的优化呼应）；极端时**降级预案**：关闭实时排序（固定顺序缓存）、关闭子评论展开、只读模式（一键开关，与高可用题的预案衔接）——大 V 事件的评论系统设计，考的全是"承认扛不住时怎么办"的预案思维。',
              ],
            },
          ],
        },
        {
          id: 'sd-feed-stream',
          title: '设计一个朋友圈/Twitter 的 Feed 流：关注的人发帖，我能在首页看到。',
          difficulty: 'intermediate',
          tags: ['Feed 流', '推拉结合', 'Timeline'],
          points: [
            '**需求澄清**：Timeline 按时间倒序（先不做复杂个性化排序）；**读远多于写**（刷新比发帖多两个数量级）；关注数有上限（如 ≤ 5000）；支持无限下拉分页。',
            '**两极方案**：**推模式（写扩散）**——发布时把帖子 ID 写进每个粉丝的收件箱（Redis ZSET，score 用时间戳），读时只拉自己的收件箱，**读快写慢**；**拉模式（读扩散）**——读时实时聚合关注列表各自的发件箱再合并排序，**写快读慢**，且大 V 没有写放大问题。',
            '**工程主流：推拉结合**。普通用户发帖**推**给粉丝；**大 V（如粉丝 > 10 万）不推只拉**——读者刷新时把关注的大 V 实时查出合并。若不区分，一条明星帖子就是亿级写扩散事故；这个“推拉阈值”就是本设计的核心参数。',
            '**关键流程**：发布 → 内容服务落库（帖子用雪花 ID，全局趋势递增）→ 判定推/拉 → 扇出服务通过 **MQ 异步**写粉丝收件箱（削峰，大 V 之外的粉丝多时分批推）。读取 → `ZREVRANGEBYSCORE` 游标分页拉自己收件箱的帖 ID → 批量回源取正文（正文放缓存）。',
            '**扩展与权衡**：收件箱只存**帖 ID + 时间戳**，轻量；缓存淘汰后可按“关注列表 + 各自发件箱”重建；帖子删除/审核下架不删收件箱，读时过滤（墓碑机制）；推模式天然是最终一致（粉丝晚几秒看到可接受）——把每个不一致讲出容忍理由，方案就立住了。',
          ],
          followUps: [
            {
              question: '为什么大 V 不能走纯推模式？阈值怎么定？',
              points: [
                '**写扩散的代价随粉丝数爆炸**：大 V 一条帖子要写进每个粉丝的收件箱——1000 万粉丝 = 一次发布触发 1000 万次写入，瞬时扇出压垮 MQ 与存储，而多数粉丝可能根本不在线（写了永远不被读的数据）；读扩散则把压力转到每次刷新的实时聚合，百万级关注列表合并同样是灾难。',
                '**推拉阈值的本质是两边压力的边际成本比较**：普通用户粉丝少，推的扇出小、收益是所有粉丝读取都快；大 V 不推只拉，代价摊到**每个刷新的读者**头上——读者数量 × 多查一个大 V 发件箱，远小于粉丝数 × 发布频率的写放大。',
                '**阈值怎么定**：不是拍数字，而是按容量倒推——用扇出服务的最大写入吞吐 / 单帖扇出成本，得到“单帖扇出耗时 ≤ 可接受延迟”的最大粉丝数，再留安全余量（实践常见 1 万-10 万级）；阈值必须**可配置可动态调整**，配合灰度观察扇出延迟。',
                '边界补全：大 V 走拉模式后，其发件箱要**常驻缓存预热**（否则刷新时的聚合查询成为新热点）；推/拉的判定要在发布时异步完成，不能阻塞发帖主链路。',
              ],
            },
            {
              question: '某个粉丝的收件箱数据丢了，怎么重建？',
              points: [
                '先明确**数据可推导**：收件箱只存“帖 ID + 时间戳”索引，帖子本体在内容库、关注关系在关注服务——收件箱是**可重建的衍生数据**。这是设计时就埋好的前提（否则丢了只能认赔），答题要先说出这一点。',
                '重建流程：触发（缓存淘汰、Redis 故障、用户反馈漏帖）→ 以该用户**游标**（最后已知帖 ID/时间戳）为界 → 拉全量关注列表 → 回溯各自发件箱近窗帖子 ID（窗口按活跃习惯定，如近 1000 条）→ 合并排序回填 ZSET 并设过期。',
                '工程细节：重建是重查询操作，要**异步 + 限流**（重建任务进 MQ 低峰执行），期间用户读取走降级路径（直接实时聚合）；按游标断点续传，失败可重入，避免大 V 粉丝重建风暴。',
                '主动延伸：若**帖子本体**已删（审核下架/过期清理），收件箱里就是悬空 ID，回源查空——用**墓碑/过滤列表**读时剔除。重建与下架过滤是同一套读时过滤机制的两面，能点出这层关系说明设计成体系。',
              ],
            },
          ],
        },
        {
          id: 'sd-like-favorite',
          title: '设计一个点赞/收藏系统：计数要准，“我是否赞过”要精确。',
          difficulty: 'intermediate',
          tags: ['点赞', '计数', '最终一致'],
          points: [
            '**需求澄清**：写 QPS 高（热点内容瞬时百万级点赞）；读分两类——**计数展示**（容忍毫秒到秒级误差）与**个人状态**（“我赞过没有”必须准确，直接影响交互）。本题核心考点：**同一系统里对不同数据用不同一致性级别**。',
            '**核心模型**：点赞记录表 `like_record(user_id, biz_type, biz_id, status)`，**唯一索引 (user_id, biz_type, biz_id)** 保证幂等、天然防重复点赞；计数**绝不实时 COUNT(*)**，用汇总表 `like_count(biz_id, cnt)` 或 Redis 计数器承载。',
            '**关键流程**：点赞 → Redis 记个人状态（Set/Bitmap 标记已赞）+ `INCR` 计数 → **MQ 异步落库**（插入记录 + 汇总表 `SET cnt = cnt + 1`）；取消点赞走对称的反向流程。计数展示**以 DB 汇总表为准**，Redis 只是加速层，丢了可以从 DB 重算。',
            '**热点行问题**：千万级点赞的热门内容，汇总表单行 UPDATE 成为瓶颈 → **分片计数**（拆成 16 个 Redis key，展示时求和）或**本地聚合批量刷库**（单机内存合并同一内容的 +1，每秒批量写一次）；记录表按 user_id 或 biz_id 分库分表。',
            '**权衡收尾**：计数走**最终一致**（秒级误差对展示无感知，换来极高写吞吐）；个人状态走**准确路径**（Redis 优先，miss 回源查库）；落库失败靠 MQ 重试 + 对账兜底。能主动说出“哪里允许不一致、哪里绝不允许”，就是这道题的高分答案。',
          ],
          followUps: [
            {
              question: '点赞列表页要展示“谁赞过”，数据模型和分页怎么做？',
              points: [
                '**读模型与计数模型分开**：列表页按 (biz_type, biz_id) 查点赞用户——`like_record` 上建 **(biz_id, biz_type, created_at) 索引**按时间倒序；而“我是否赞过”走 (user_id, biz_type, biz_id) 唯一索引或 Redis Set。两条查询路径对应两个索引，别指望一张表一个索引通吃。',
                '**大 V 内容的列表问题**：百万点赞的列表不可能全量放 Redis ZSET（内存爆炸）——**冷热分层**：前 N 页（如前 1000 条）放缓存，更深的翻页直接走 DB 游标分页 `WHERE created_at < last ORDER BY created_at DESC LIMIT 20`，任何深度都是常数成本。',
                '**取用户信息防 N+1**：列表页要拼头像昵称，20 条点赞就是 20 次用户查询——**批量接口**一次取回 + 用户信息本地缓存；注销/封禁用户用**读时过滤**（不物理删除记录，保留计数一致性）。能从“点赞”延伸到列表读路径，说明设计的是完整功能而不是一个计数器。',
              ],
            },
            {
              question: 'Redis 计数和数据库汇总表对不上怎么办？对账怎么设计？',
              points: [
                '**先定义谁是事实**：DB 的 `like_count` 汇总表是**计数的最终事实**，Redis 只是加速层——“对不上”的处理原则是**以 DB 为准回填 Redis**，而不是反过来；展示层短暂偏差可接受，但要有办法发现偏差在扩大。',
                '**对账设计**：实时层——监控“Redis 与 DB 的差值”指标（差值持续增长说明丢消费）；兜底层——定时任务按内容维度**重算 `COUNT(*)` 写回汇总表并刷新 Redis**，全量对账安排在低峰，大表分批跑 + 状态表断点续跑。',
                '**防丢靠设计，对账只是兜底**：落库走 MQ 就要处理**消费失败重试**，幂等靠唯一索引（重复消费不会重复计数）；Redis `INCR` 成功但 MQ 发送失败要有本地兜底队列或事务消息。能说出“对账是兜底、幂等才是正确性保证”，与秒杀、红包题的结论同构，体现模式抽象能力。',
              ],
            },
          ],
        },
        {
          id: 'sd-classic-risk-control',
          title: '设计一套实时风控系统：规则、模型、名单怎么配合？误杀率怎么权衡？',
          difficulty: 'advanced',
          tags: ['风控系统', '规则引擎', '实时特征', '反欺诈'],
          points: [
            '**分层防御架构（答题主线）**：**事前拦截层**（实时决策：请求进来毫秒级判放行/拒绝/挑战）——内部三引擎配合：**名单**（黑白灰名单，毫秒级、零成本，覆盖已知坏人）、**规则引擎**（"同 IP 10 分钟注册 > 20 个账号"类专家规则，可解释、可快速热更新）、**模型**（GBDT/图模型/序列模型打分，覆盖规则表达不了的复杂模式）；**事中挑战**（人脸/短信验证——不确定流量的缓冲带，直接拒掉误伤真用户，直接放行漏掉攻击者，挑战是折中）；**事后离线**（批量案件分析、名单回流、模型训练——离线发现的新模式喂回实时层，闭环）。',
            '**实时特征是系统的真正难点**：决策要毫秒级，特征查询不能现场算——**特征平台**：离线特征（画像，天级更新）+ **实时特征**（滑动窗口计数：该设备 5 分钟下单次数——由 Flink/流引擎维护在 Redis）；要解决**特征一致性**（离线训练用一套特征口径、在线推理用另一套会翻车——训练 serving skew 是风控模型的经典事故源，与推荐系统题的"特征穿越"呼应）。',
            '**决策的艺术：误杀率与漏杀率的权衡（本题的灵魂）**：风控的代价是**不对称的**——误杀真用户（下单被拒直接损收入+客诉）vs 漏杀攻击（资损）——不同业务两者权重完全不同（拉新场景误杀代价高，支付场景漏杀代价高）；工程手段：**分级处置**（放行/挑战/限额/人工审核/拒绝——不要二元决策）、**灰度观察期**（新规则先"影子模式"只记日志不生效，验证误杀率再启用）、**申诉通道**（误杀可恢复，用户验证后白名单化）。',
            '**对抗性思维（与静态系统设计的差异点）**：风控的对手会**适应**——规则太严攻击者换设备农场、换 IP 池、养号；所以系统必须**可快速迭代**（规则热更新分钟级生效、模型按周重训）、**设备指纹与关系图谱**（单点特征易伪造，"这个设备和哪些账号关联过"的图关系难伪造——图模型/社区发现找团伙）、**蜜罐与延迟封禁**（不立即封，观察行为网络再一网打尽，避免打草惊蛇暴露规则）。',
            '收束口径：风控系统 = **实时决策引擎 + 特征平台 + 离线闭环** 三件套 + **对抗运营**的长期战；面试加分句："风控不是一个系统而是一场持续对抗，架构的 KPI 是**迭代速度**——新攻击模式出现到规则上线的时间越短，损失越小"。',
          ],
          followUps: [
            {
              question: '规则和模型打架了（规则说放、模型说拒），听谁的？怎么治理规则膨胀？',
              points: [
                '仲裁设计：给决策引擎定义明确的**优先级与组合策略**——常见是**否定优先**（任一强信号拒绝则拒绝，名单/强规则优先级高于模型分）+ 模型分做**连续调节**（阈值区间内叠加规则微调），而不是简单平均；关键是**每次决策落全量证据快照**（命中了哪条规则、模型分多少、特征值是什么）——事后面审才有据可查，规则打架可归因。',
                '规则膨胀治理（规则引擎的宿命病）：规则只会越加越多直到没人敢删——治理靠**规则生命周期管理**（每条规则有 owner、生效条件、效果监控：命中量/准确率/误杀率，长期零命中或高误杀的下线）+ **定期把规则簇合并成模型特征**（能用模型学出来的模式就不该靠人肉规则堆）——"规则是模型的前身，模型是规则的沉淀"这个演进观是风控团队的成熟标志。',
              ],
            },
          ],
        },
        {
          id: 'sd-classic-video',
          title: '设计一个视频平台（上传、转码、分发、播放）的核心链路？',
          difficulty: 'advanced',
          tags: ['流媒体', '转码', 'CDN', '自适应码率'],
          points: [
            '**上传链路三件套**：**分片上传 + 断点续传**（大文件按块上传、秒级重试不重传全量——与对象存储题的 multipart 机制衔接）；**秒传**（按内容哈希查库，已存在直接建索引——去重思想与短链/布隆题同源）；上传完成后**回调触发转码流水线**（上传与转码解耦，经消息队列分发）。',
            '**转码流水线（成本与体验的核心）**：源视频 → **多码率转码**（1080P/720P/480P 多档 + 多编码 H.264/H.265/AV1 权衡兼容性与带宽成本）→ **切片打包**（HLS 切 fMP4 + m3u8 播放列表，或 DASH——切片让播放器按需拉取、CDN 缓存粒度变细、seek 秒级）；**关键帧对齐**（多码率切换时对齐 IDR 帧才能无缝切档）；热点内容**预热**（新片发布提前推 CDN 边缘）；转码是 CPU/GPU 密集的长任务——**任务队列 + 弹性 worker 池**（与容量规划题的弹性思路衔接），转码完写元数据（多码率版本、时长、封面抽帧）。',
            '**分发与播放（CDN 命中率是带宽成本的命门）**：点播走 **CDN 边缘缓存**（命中率 95%+ 时带宽成本差数倍——切片小文件天然 CDN 友好）；播放器端 **ABR 自适应码率**（按实时带宽与缓冲水位动态切档：缓冲下降切低码率保不卡，带宽富余升档保清晰——buffer-based 与 throughput-based 混合算法）；**首帧优化**（首片预取 + 起播码率选择（首帧别选最高档）、边缘节点就近；首帧时间 TTFF 是播放体验的第一指标）；预加载（Feed 里下个视频的前几秒切片预取）。',
            '**运营与技术深水区**：**DRM 与防盗链**（加密 HLS + 签名 URL 时效鉴权）；**版权与内容审核**（上传侧异步审核，转码前拦截违规）；弹幕/评论等互动与播放主链路**隔离**（互动挂了不能影响播放）；直播变体（推流 → 转推/录制 → 切片成伪直播，LL-HLS 降延迟——能点出直播与点播架构的差异就是加分）。',
            '**成本账收束（高级感所在）**：视频平台成本 = **带宽（CDN 流量）+ 存储 + 转码算力** 三座大山，带宽通常最大——所以架构处处为省带宽设计：ABR 防止"高码率播在弱网"的浪费、AV1 等新编码省 30%+ 带宽但转码成本升（**编码投入 vs 带宽节省的 ROI 曲线**）、预热与命中率运营；答这题能把"每个设计决策折算成成本/体验指标"，就是平台架构师的视角。',
          ],
          followUps: [
            {
              question: '用户反馈「视频老是卡顿」，你怎么定位是哪一环的问题？',
              points: [
                '**播放端数据先行**（卡顿分析靠端上埋点，不靠猜）：卡顿率按维度切——**网络原因**（带宽不足触发降档仍卡：看端侧实测带宽 vs 当前码率档位）、**CDN 原因**（切片下载慢：命中率、回源率、边缘节点距离）、**源站/转码原因**（特定视频缺低码率档——冷门内容转码队列堆积没出多档）、**端原因**（低端机解码跟不上：丢帧型卡顿与网络无关，看解码耗时指标）。',
                '归因到环之后动作各不相同：CDN 环调调度策略/预热热门；源站扩转码池、补齐码率档；端侧调 ABR 算法参数（降档更激进）与起播档位；网络差地区单独投放低码率转码策略——**先分维度定位再动手**与所有分层排查题同构，但这题的数据全靠端上埋点体系，能点出「播放质量体系是埋点驱动的」就是做过流媒体的人。',
              ],
            },
          ],
        },
        {
          id: 'sd-classic-collab-editing',
          title: '多人实时协同编辑（腾讯文档/飞书）怎么设计？OT 和 CRDT 的取舍是什么？',
          difficulty: 'advanced',
          tags: ['协同编辑', 'OT', 'CRDT', '一致性'],
          points: [
            '**问题本质先说透（这题的门槛在问题定义）**：多人并发编辑同一文档，要同时满足——**收敛**（所有人最终看到相同结果）、**意图保持**（我删的字不会"复活"、别人在别处打字不被我挤掉）、**实时**（延迟 < 100ms 级）、**离线可编辑**（断网后再合并）。难点在于**并发操作之间没有天然的先后顺序**，而顺序不同结果就不同。',
            '**OT（Operational Transformation，Google Docs 的经典路线）**：广播的是**操作**（在第 i 位插入 x）；服务器收到并发操作后，**按变换函数改写后到的操作**，使得两个并发插入都能按"各自认为的意图"落位——`apply(OT(op, other))`；核心是**变换函数的正确性证明**（TP1/TP2 性质），工程上极难做对（文本相对好做，富文本/表格的操作变换复杂度爆炸）；**中心化服务器定序**是它的舒适区（服务器是单一事实源）。',
            '**CRDT（Conflict-free Replicated Data Type，新一代主流）**：数据结构本身**数学保证收敛**——每个字符带**唯一 id 与逻辑时钟**，删除是墓碑、并发插入靠 id 排序规则（如 Yjs 的 **YATA 算法**：左侧 origin 链比较）自动定序，**不需要服务器仲裁**，天然支持**离线编辑与 P2P**；代价：**元数据内存开销**（每字符 id/origin，比原文大数倍——要用墓碑 GC 与状态压缩缓解）与部分场景的**意图牺牲**（并发冲突按规则裁决，可能不完全符合任一方直觉）。',
            '**选型口径（答题的决策层）**：**强中心、有服务器、文本型 → OT**（Google Docs/老协同）；**离线优先、去中心、复杂结构（白板/笔记 Notion-Obsidian 生态）→ CRDT**（Yjs/Automerge 已成开源事实标准）；工程现实：**新项目大多直接上 CRDT 库**（自己写 OT 的正确性成本远超想象），服务器仍存在但只做**消息分发 + 持久化 + 权限**，不再做一致性仲裁。',
            '**完整系统拼图（别只答算法）**：**在线状态与光标同步**（presence 广播：谁在哪、选区是什么——低频元数据独立通道）；**历史与撤销**（CRDT 的撤销要做**逆操作**而非回滚版本，多人场景"我撤我的"要能区分）；**持久化**（文档快照 + 增量 update 分段存储，加载 = 快照 + 重放尾部）；**权限与鉴权**挂消息通道。收束：这题考的是"**一致性算法只是 1/4，剩下 3/4 是实时系统工程**"的完整视野。',
          ],
          followUps: [
            {
              question: '两个人同时在不同地方插入、又同时删除对方的插入，CRDT 怎么保证结果一致？',
              points: [
                '并发插入：两个字符 id 不同，**YATA 排序规则**（比较各自的左侧 origin 链）在任何副本上得到**相同的全序**——结果可能"你在我前"不符合某人直觉，但**所有副本绝对一致**（收敛性 > 意图完美性，这是 CRDT 的哲学）。并发删除与插入互不干扰（删的是墓碑、插的是新 id）；**并发删除同一字符**结果平凡一致（都是墓碑）。',
                '真正的边界是"语义冲突"（两人同时把标题改成不同文字——文本 CRDT 只保证都留下并排序，不保证语义合理）：这要靠**产品层策略**（显式冲突提示、锁定编辑权）兜底——能说出"CRDT 保证的是**最终一致的文本状态**，不保证**业务语义的合理性**"，说明对能力边界想得非常清楚。',
              ],
            },
          ],
        },
        {
          id: 'sd-classic-search',
          title: '设计站内搜索：倒排索引为什么快？ES 架构、分词与相关性怎么答出深度？',
          difficulty: 'advanced',
          tags: ['搜索', '倒排索引', 'Elasticsearch', 'BM25'],
          points: [
            '核心数据结构**倒排索引**：term → posting list（文档 ID + 词频 + 位置信息）；正排是"文档→包含哪些词"，倒排把"含某词的文档有哪些"变成一次定位 + **posting list 集合求交（AND）/求并（OR）**——这就是搜索快的全部秘密。再快一层靠三件配套：**term dictionary → term index 分层定位**（FST/前缀结构放内存，先粗定位再读磁盘字典）、**跳表/位图加速集合运算**、删除用位图标记不真删。',
            '**分词决定召回**：中文分词（IK 等可插拔分词器）+ 归一化（小写、单复数、繁简）；"搜不到"的第一嫌疑永远是**索引分词与查询分词不一致**——专有名词被切碎要上自定义词典，同义词/拼音扩展解决口语化查询（与 AI 方向 Tokenizer 题分工：那题讲 LLM 子词与计费，本题讲搜索召回）。',
            '**相关性打分 BM25**（说出三要素即赢过多数人）：**词频饱和**（词出现越多分越高但边际递减，不像 TF-IDF 线性）、**IDF**（越稀有的词越重要）、**文档长度归一**（长文档的词频天然高，要打折）；工程排序 = BM25 相关性 × 字段权重 × 时效性/业务分加权，深度方案是**词法 + 向量混合检索**（专有名词靠词法精确匹配、语义泛化靠 embedding 近邻，与 RAG 混合检索同一思想）再统一 rerank。',
            '**ES 架构要点**（按写读路径讲）：写入先进内存 buffer + **refresh（默认 1s）生成新 segment 才可搜**——这就是"近实时"而非实时；segment 不可变、后台 merge；**分片 = 一个 Lucene 实例**，副本提供吞吐与高可用；深分页 from+size 每个分片都要取 top (from+size) 全局排序（代价爆炸），生产用 **search_after + PIT**（游标续查 + 快照一致性）；聚合分析走 **doc_values 列存**（与大数据方向 OLAP 列存原理呼应）。',
            '系统分层收束：数据同步（binlog/消息管道 → 索引）→ **query 理解**（纠错、改写、意图识别、分词）→ 召回（倒排 + 向量）→ 排序（BM25 + 业务重排）→ 展示；高级感在 query 理解层——**索引建得再好，查询理解不行也是垃圾进垃圾出**；答这题先画分层再下钻，深度按面试官反应调节。',
          ],
          followUps: [
            {
              question: '为什么 ES 是近实时而不是实时？refresh、flush、translog 分别管什么？',
              points: [
                '三个机制各管一段：**refresh** 把内存 buffer 的文档生成新 segment 进入文件系统缓存（可搜）——1 秒间隔是"近实时"的来源，调大提写入吞吐、调小提搜索新鲜度；**translog** 是写前日志（写请求先记 translog 防宕机丢数据）；**flush** 把缓存里的 segment 真正落盘并清空 translog——**可搜索性（refresh）与持久性（translog/flush）是两条独立时间线**，分得清这个就是真读过架构。',
                '代价意识：refresh 越频繁 segment 越碎（查询要开更多文件）→ merge 压力；写入洪峰场景（日志）常把 refresh 拉到 30s 换吞吐——所有参数都是取舍，说得出"换什么"才算懂。',
              ],
            },
            {
              question: '搜索数据与 MySQL 的双写一致性怎么保证？',
              points: [
                '标准方案：**以 DB 为源头的 binlog 订阅同步**（Canal/Flink CDC → 写 ES），业务写库不感知搜索（与大数据 CDC 题同源）；一致性保障：同步链路**顺序性**（按主键 hash 到同一分区，防乱序覆盖）+ **定时对账**（数量与抽样比对，差异自动重推）+ 手动重建索引兜底。',
                '对比双写方案：业务代码同时写 DB 与 ES——侵入业务且**两写不一致**是老大难（一个成功一个失败），除非有强事务手段否则不选；能说出"同步管道 + 对账"是正解、"双写是反模式"的判断，这题就答到位了。',
              ],
            },
          ],
        },
        {
          id: 'sd-classic-payment',
          title: '设计支付系统：支付单状态机、掉单与对账体系怎么兜住「钱」的事故？',
          difficulty: 'advanced',
          tags: ['支付', '状态机', '对账', '一致性'],
          points: [
            '**分层先立住**：订单（业务域）→ **支付单**（支付域，一笔订单可多次支付尝试）→ 渠道请求（微信/支付宝/银联通道）；支付单是严格的**状态机**：待支付 → 支付中 → 成功/失败/关闭/退款中，**每次状态流转都要 CAS 更新 + 落事件流水**（`UPDATE ... WHERE status = 支付中`，影响行数=0 说明被并发处理过）——状态机 + 流水是审计与对账的底座，这道题的骨架。',
            '**同步受理 + 异步确认两段式**：下单调渠道是"受理"（同步返回不代表支付成功），真实结果靠**渠道回调通知（notify）**：验签 → **幂等处理**（渠道回调必会重复）→ 状态机流转 → 通知业务（MQ）。**掉单三重防线**：① 回调丢失 → **主动查询补偿**（定时扫描"支付中超过 N 分钟"的单去渠道查）；② 查询也漏 → **日终对账兜底**（渠道账单 vs 本地流水比对，差异生成处理单）；③ 全程事件流水可回放——每一层都假设上一层会丢。',
            '**对账体系（本题的深水区）**：**信息流对账**（订单/支付单/账务流水三方核对：金额一致、状态一致）+ **资金流对账**（渠道账单金额 vs 本方账务）；差异分型处理：**长款**（渠道有我无——用户付了但我没记录，最高优先补单）与**短款**（我有渠道无——疑似事故，核查后处理）；工程细节：日切时间窗、币种与手续费科目的账务处理；账务核心用**复式记账**（每笔交易借贷双向分录，借贷必平——天然防"钱凭空出现/消失"）。',
            '**幂等与一致性贯穿一切**：回调、查询补偿、对账补单**都会重复触发**——渠道单号唯一索引 + 状态机 CAS 是最后防线；支付成功后的账务入账与业务通知用**本地消息表/事务消息**保最终一致（与 MQ 可靠投递题衔接）；**绝对禁区**：回调直接改业务状态绕过支付单（会造成两边状态分叉，对账灾难）。',
            '安全与容灾收束：金额一律**整数分**运算（浮点误差在钱的场景是事故）；风控与限额前置；渠道**多通道冗余**（单渠道故障降级切换，路由按费率/成功率/限额动态选）；发布灰度比普通业务更保守。设计哲学一句话："**宁可慢、不可错——任何异常都有对账兜底，以对账为中心的最终一致**"。',
          ],
          followUps: [
            {
              question: '用户投诉「扣款了但订单没生效」，你的排查链路是什么？',
              points: [
                '**三段定位法**（按链路顺序）：① 用渠道单号/商户单号查**渠道扣款事实**（渠道侧确实成功？——先确认钱真扣了）；② 本地**支付单状态**：若停在"支付中"→ 回调丢失或处理失败，触发查询补偿/手动重新拉取回调，状态机推进；③ 支付单已成功但业务未生效 → **业务通知链路**（MQ 是否投递/消费失败、业务侧消费幂等是否吞掉）→ 修复并重发。',
                '处理原则：先恢复用户（补单或一键退款通道），再修根因（回调为什么丢：网络/验签失败/处理异常），最后回写对账规则（这类 case 纳入自动对账差异类型，下次系统自己修）——把个案修复沉淀成机制，是支付团队成熟的标志。',
              ],
            },
            {
              question: '为什么账务系统要用复式记账？单式流水表不行吗？',
              points: [
                '复式记账每笔交易**借贷双向记录**（用户支付 100 元：现金账户借 +100，待清算账户贷 −100），全局 **借贷必平**——每天试算平衡就是一道自检：不平立即报警，**钱的总量守恒由结构保证**，任何一笔"无中生有"的账都会破坏平衡而暴露。',
                '单式流水（只记"谁付了多少"）无法回答"这笔钱从哪来、到哪去"，账户余额靠增量累加——累加 bug、并发覆盖、漏记都难以自动发现；复式记账还能天然支持清算（多科目轧差）与审计追溯。这个细节是区分"做过支付"与"看过支付文章"的硬指标，主动讲出来基本锁定这题满分。',
              ],
            },
          ],
        },
        {
          id: 'sd-classic-recommend',
          title: '设计一个推荐系统：召回、粗排、精排、重排的漏斗分别解决什么？',
          difficulty: 'advanced',
          tags: ['推荐系统', '召回', '排序', '漏斗'],
          points: [
            '**漏斗骨架先背牢（答题的地图）**：百万/千万级物品 → **召回**（多路并发，捞回千级候选：协同过滤、双塔向量近邻、热门、关注关系）→ **粗排**（轻量模型快速打分，截断到几百）→ **精排**（重模型精打分，几十到百级）→ **重排**（去重、打散多样性、业务规则、广告混排）→ 展示。**每一层的本质是"数量级裁剪 × 精度递增"的算力分配问题**——在几百毫秒延迟预算内完成从千万到几十的筛选。',
            '**召回为什么必须多路**：单路覆盖不了多目标（精准兴趣、发现性、时效性），典型组合 = 向量召回（双塔：用户塔 × 物品塔内积，离线建 ANN 索引在线近邻检索——与 AI 方向 Embedding 题的"学出好向量"衔接）+ 行为协同（i2i：看过 A 的也看 B）+ 热门/地域兜底（冷启动）+ 关注/订阅关系；各路**配额与融合策略**（各取 top 合并去重），召回的目标是**别把好东西挡在门外**（召回率优先）。',
            '**特征与样本（真功夫所在，能讲细节的极少）**：特征体系 = 用户画像 + 物品特征 + **交叉统计特征**（该用户在该类目的历史点击率）+ 上下文（时间/设备）；样本从**曝光日志**构造（点击为正、曝光未点击为负）；两个必说的深水概念：**position bias**（展示位置影响点击，训练时要做位置加权或建模校正——说出这个词就是分水岭）、**样本与特征的穿越**（用了训练时刻不该有的未来信息，离线指标虚高线上拉胯的经典根因）。',
            '**反馈闭环与实验体系**：实时特征（最近几分钟的行为）经流式链路进特征库（与大数据方向 Flink 题呼应）；曝光去重、已购过滤；**AB 实验是推荐迭代的命脉**——每层改动都要实验，指标 = 北极星（时长/GMV）+ 护栏（负反馈率、多样性）；冷启动三条线：新物品（内容特征兜底）、新用户（热门+注册信息）、新模型（小流量影子播放）。',
            '收束格局：推荐系统工程本质是"**延迟预算内的级联裁剪**"，而从业者共识是**数据与特征工程占一半以上工作量**（样本管道、特征平台、实验平台）——把"数据工程 > 模型花活"讲出来，是从论文视角到工程视角的切换，也是这道题的隐藏考点。',
          ],
          followUps: [
            {
              question: '为什么不直接对全量物品跑精排模型？粗排存在的意义是什么？',
              points: [
                '算力与延迟的硬约束：精排模型重（深度特征交叉、多目标），单次打分毫秒级 × 百万物品 = 无法在预算内完成；**级联裁剪**用粗排"以 1% 的算力保留 95% 的好结果"——粗排通常是精排的蒸馏版（双塔简化结构），训练时用精排分数当教师（蒸馏对齐两者排序一致性）。',
                '通用模式拔高：级联裁剪无处不在——搜索引擎（召回→排序）、多级缓存（L1/L2/L3）、法院的审级制度都是"**便宜的粗筛 + 昂贵的精判**"；能说出这是资源约束下的通用架构模式而不是推荐特有的技巧，视野分就拿到了。',
              ],
            },
            {
              question: '推荐效果不好，你怎么定位是哪一层出了问题？',
              points: [
                '**分层评估**（先定位层再调模型）：召回层看**召回率/命中率**（用户点击的 item 在候选集里吗？没进候选 = 召回问题，怎么排都没用）；粗排精排看 **AUC/GAUC** 与线上线下一致性（离线 AUC 涨线上不涨 → 特征穿越或样本分布偏移）；重排看业务规则是否误伤（过度打散压垮精准度）。再加**重叠度分析**（新旧策略推荐重合度，判断是换了排序还是换了内容）。',
                '这套"先分层定位、再动刀"的排查纪律与性能排查（先分层看瓶颈在哪层）完全同构——**分层系统的 debug 方法论是通用的**，能点到这一层说明排查素养已经内化。',
              ],
            },
          ],
        },
        {
          id: 'sd-classic-ai-coding',
          title: '设计一个 GitHub Copilot 式的代码补全服务：延迟、上下文与模型怎么权衡？',
          difficulty: 'advanced',
          tags: ['AI 系统', '代码补全', 'FIM', 'LLM 应用'],
          points: [
            '**需求先算账（本题的特色：延迟是第一约束）**：补全是"幽灵文本"交互——用户每停顿几百毫秒就可能触发请求，**端到端延迟预算 200~500ms**（含网络+推理+首 token），超了体验就是"提示总是迟到"；同时是**高频高并发**（活跃 IDE 用户持续触发），与聊天类 LLM 应用的成本结构完全不同。这道题是 2026 年系统设计的新贵，本质考"LLM 推理服务 + 编辑器工程"的合体。',
            '**补全的两种触发模式**：**FIM（Fill-in-the-Middle，中间填充）**——只给光标前缀 + 后缀（`<prefix>…<suffix>` 特殊标记），小模型毫秒级出多行建议，这是**行内补全的主力**（继续写循环/样板代码）；**对话/Agent 式**（Edit/Chat 面板）——带仓库上下文的指令式生成，延迟容忍秒级。两层用不同模型：**小而快的做打字陪伴，大而强的做显式指令**——模型路由（与推理成本题的路由思想衔接）是本题的第一个架构决策。',
            '**上下文拼装（真正的难点）**：光标处该看什么？工程链路 = 当前文件前后文（截断策略：语义边界优先于字数硬切）+ **跨文件信号**：同一语言/同目录的相似代码片段（用 embedding 检索相邻 tab 与 import 的符号定义，参考 Tabnine/Copilot 的公开思路）、用户**最近的编辑序列**（刚重命名的方法名要立刻学会——prompt 里给"刚刚发生的变化"比检索更有效）；**缓存与增量**：前缀缓存让连续补全共享 KV（输入前缀逐字节稳定，与上下文工程题的缓存布局衔接）。',
            '**服务端架构要点**：**防抖与取消**（客户端打字节奏触发，服务端要处理大量"生成到一半被取消"——取消要有信号传播，算力不白烧）；**投机式的流式返回**（返回前 N 个候选，按用户接受率在线调 N）；**个性化与遗忘**：企业版的"不拿客户代码训练"是合规硬约束（数据隔离、遥测最小化）；评测闭环 = **接受率（accept rate）分场景埋点**（按语言/补全长/触发方式切维度），它是模型与 prompt 迭代的北极星。',
            '**收束表达**：这类题的答题公式 = **延迟预算倒推模型分层 → 上下文管道决定质量 → 缓存/取消/接受率是三个关键词**；主动对比"聊天式 LLM 应用"与"补全式"的差异（触发频率、延迟敏感度、上下文获取方式），说明理解的是"LLM 产品的形态差异决定架构差异"。',
          ],
          followUps: [
            {
              question: '补全建议总被用户拒绝，你会从哪些角度优化接受率？',
              points: [
                '**先分维度定位**：按语言、触发位置（行中/行尾/块尾）、建议长度、延迟切接受率——通常"长建议 + 高延迟"接受率崩（用户已经自己写了）；**展示策略**：有时不是模型错，是"该出手时不出手"（用户根本不需要建议的位置频繁弹）——触发策略（何时静默）与生成质量同样重要。',
                '再优化供给：**多候选与排序**（生成 N 个按小 ranker 排序，首条接受率优先）、**下文感知**（建议与已有代码风格不一致是高频拒绝原因——用仓库内代码做 few-shot 比换更大的模型更有效）、**即时反馈回路**（用户对建议的编辑距离——改了一半说明接近了，信号比二元的接受/拒绝更细）。',
              ],
            },
          ],
        },
        {
          id: 'sd-classic-vector-search',
          title: '设计一个向量检索服务：HNSW 为什么快？带过滤的向量查询怎么不退化？',
          difficulty: 'advanced',
          tags: ['向量检索', 'HNSW', 'ANN', '向量数据库'],
          points: [
            '**问题定义**：海量向量（千万~亿级）里找 TopK 最近邻（余弦/L2），精确暴力搜 O(N×d) 扛不住——**ANN（近似最近邻）**用少量召回率损失换数量级加速。与 Embedding 题分工：那题讲"向量怎么来、用在哪（RAG/召回）"，本题讲**检索服务本身的原理与工程**。',
            '**HNSW（分层可导航小世界图，主流默认索引）**：多层跳表式图结构——上层稀疏（长边快速跳跃）、底层稠密（短边精确逼近）；查询从顶层入口**贪心走图**（邻居更近就跳过去），逐层下沉，复杂度近似 O(log N)；建图参数 **M（每节点边数）与 efConstruction/efSearch（搜索时候选队列宽度）**调"索引内存 vs 召回 vs 延迟"；对**插入友好**（增删不需全量重建），这是它统治在线场景的原因。',
            '**另一条路线 IVF-PQ（量化分桶）**：IVF 先聚类分桶（查询只探最近的几个桶——粗筛），PQ 再把向量切段量化压缩（64 倍压缩，内存大户的解法）；对比口径：**HNSW = 快与准（吃内存）、IVF-PQ = 省内存（召回打折）**，亿级+成本敏感常用 IVF-HNSW 混合（分桶内建小图）。DiskANN 等盘上索引是超大规模的延伸——能报出这个谱系说明跟得住检索前沿。',
            '**带过滤的向量查询是工程深水区（必考）**：`topk 相似 AND category=x AND tenant=y`——**后过滤**（先 ANN 再过滤）会"召回 100 过滤剩 3"（topk 不足）；**预过滤**（先过滤再搜）过滤比例高时索引碎片化退化成暴力扫；现代向量库的解法：**过滤感知的图遍历**（导航时跳过不满足过滤的节点，动态扩 ef 补偿）或**分索引**（按高频过滤维度分片，各建 HNSW）——答得出"过滤比"这个变量（过滤后剩余 1% 还是 60% 决定策略）是真做过的信号。',
            '**服务化设计收束**：分片（按向量 hash 分 node，topk 各分片归并）与副本扩读；**内存账**（原始向量 1536 维 float = 6KB/条，1 亿条 ≈ 600GB——PQ/二值化的动机）；**增量与更新**（upsert = 删 + 插，HNSW 的删除是墓碑标记 + 后台压实）；标量字段（过滤所需的元数据）与向量的存储一致性。收束口径：**向量数据库 = ANN 索引 + 过滤 + 分布式 + 数据管理**，四件事各是一层工程。',
          ],
          followUps: [
            {
              question: '为什么 HNSW 的参数 efSearch 调大召回就上去？代价是什么？线上怎么调？',
              points: [
                'efSearch 是搜索时**候选优先队列的宽度**——宽度越大，图遍历探索的邻居越多，越不容易困在局部最优点漏掉真近邻（召回率 ↑），但每次查询的**距离计算次数与延迟同比例 ↑**；它与建图期的 efConstruction/M 不同：前者是运行时可调的"查询时旋钮"，后者影响索引质量不可逆。',
                '线上调法：**按 QPS 与延迟 SLO 反推**可用预算，再扫描 efSearch 画"召回-延迟"曲线选拐点；进阶是**分级 ef**（首查低 ef 快返回，用户翻页/精排时再高 ef 补查）——把"检索质量"做成延迟预算内的动态分配，这个思路与推荐系统的级联裁剪同构。',
              ],
            },
          ],
        },
        {
          id: 'sd-seckill',
          title: '设计一个秒杀系统：10 万件库存，预计 100 万人同时抢。',
          difficulty: 'advanced',
          tags: ['秒杀', '削峰', '库存超卖'],
          points: [
            '**需求澄清定调**：瞬时峰值极高（日常的百倍）、**库存绝对不能超卖**、允许“没抢到”快速失败、下单结果允许秒级异步可见。整套方案只围绕两个词：**削峰、防超卖**。',
            '**层层削峰（漏斗）**：活动页静态化 + CDN（静态流量不回源）→ **网关限流 + 答题/验证码**（拦截脚本与瞬时洪峰，请求量砍掉 90%）→ **Redis 预扣库存**（`DECR stock` 原子扣减，结果小于 0 直接返回售罄）——最终只有约 10 万级请求进入下单链路，与库存同量级。',
            '**关键流程（下单链路）**：Redis 预扣成功 → **MQ 异步下单**（流量与数据库解耦）→ 消费者扣库存：`UPDATE stock SET num = num - 1 WHERE id = ? AND num > 0`（**条件更新，影响行数为 0 说明没库存**，回滚 Redis 并补偿）→ 创建订单 → 用户轮询或推送获取结果。',
            '**防作弊与热点**：一人一单靠**唯一索引 (activity_id, user_id)** 幂等兜底；链接提前不暴露（动态 URL/签名）+ 风控频控防脚本；Redis 热点 key 用**库存分段**（10 万拆成 10 个 key 各 1 万，并行扣减）+ 本地缓存“售罄”标记减少无效请求。',
            '**兜底与权衡**：MQ 堆积或 Redis 故障时降级为排队页（先收单后慢慢处理）；数据库只承担过滤后的流量，所以可以用强一致扣减保正确性；对账任务兜底（Redis 已扣数 vs DB 订单数）。**结论句主动说：宁可少卖（对账后补回），绝不超卖。**',
          ],
          followUps: [
            {
              question: '为什么 Redis 扣完库存，数据库还要做条件更新？',
              points: [
                '**分层职责**：Redis `DECR` 负责**挡洪峰**——把 100 万请求削到与 10 万库存同量级；数据库 `UPDATE stock SET num = num - 1 WHERE id = ? AND num > 0` 负责**最终事实**——影响行数为 0 说明库存已尽，此时回滚 Redis 并补偿。库存正确性以数据库为准。',
                '只信 Redis 的风险：主从切换时异步复制可能**丢最近写入**，已扣的库存“凭空回来”就是超卖；对账必须以 DB 为锚。只信数据库的风险：10 万行更新叠加百万请求的行锁竞争直接打挂 DB——所以必须先削峰，DB 才扛得住。',
                '闭环细节：MQ 消费失败或条件更新未命中 → `INCR` **回滚 Redis** + 记补偿表；`DECR` 前先判断剩余量可以减少无效写；用户看到的“抢购成功”以 MQ 消费产出订单为准，前端轮询/推送获取最终结果。',
                '答题要点：这题和抢红包是同一套结构（预筛 + 条件更新 + 对账），面试官换皮再问时，直接点破“同一分层模型”，展示的是模式抽象能力而不是背场景。',
              ],
            },
            {
              question: '如果业务允许少量超卖，架构可以怎么简化？',
              points: [
                '**超卖容忍 = Redis 可以升格为唯一事实**：去掉“DB 条件更新强一致扣减”这层，库存只存 Redis，扣减成功直接生成订单异步落库（纯 append 写，无条件更新冲突）——MQ 消费从“扣库存 + 建单”简化为“建单”，锁冲突消失，吞吐上一个台阶。',
                '进一步简化：库存分段可合并（单 key 足够）；对账从“必须回滚”降级为“事后补差”；甚至凭“先到先得 + 一人一单唯一索引”近似控量即可。',
                '**适用判断要主动给**：火车票、酒店房源这类履约成本极高的场景绝不接受超卖；优惠券、虚拟商品（多发成本几毛钱）可接受千分之几超卖换数十倍吞吐。**先把业务损失函数问清楚，再定一致性档位**——这正是 CAP 分级思维在秒杀题里的落点。',
                '兜底话术：即使允许超卖也要有**止损开关**——监控实际发放数，超阈值即熔断活动；超发部分用补偿（作废/换品/赔付）收尾，而不是无上限超发。',
              ],
            },
          ],
        },
        {
          id: 'sd-im',
          title: '设计一个 IM 即时通讯系统，要求消息不丢、不重、有序。',
          difficulty: 'advanced',
          tags: ['IM', '长连接', '消息可靠性'],
          points: [
            '**需求澄清**：单聊/群聊；**可靠性**（不丢、不重）、**顺序性**（同一会话内严格有序）、在线状态、离线消息、已读回执。第一性原理：**TCP 长连接 + 心跳**保证实时推送，HTTP 作为降级兜底通道。',
            '**核心模型**：**消息 ID**（服务端在同一会话内单调递增的 seq，排序与去重的基石）；存储两张表——`message(msg_id, conv_id, sender, content, seq)` 存消息本体，`user_timeline(user_id, conv_id, last_read_seq)` 存每个用户的收件游标。',
            '**关键流程（可靠投递）**：发送方带**客户端幂等 ID** 上行 → 服务端落库并分配 seq → 查路由表（user → 所在网关）**在线推**，离线则只入库 → 接收方回 **ACK** 才算送达，超时未 ACK 就重推 → 客户端按 seq **去重 + 排序**。可靠性公式：**落库 + ACK + 重推 + 去重**，四者缺一不可。',
            '**连接层设计**：网关只做**连接维护与心跳**，业务逻辑无状态化，连接路由表放 Redis（user → gateway）；扩容就是加网关机器并更新路由表；单机 10 万连接 × 10 台 = 百万连接，注意 fd 上限与每连接内存开销。',
            '**群聊的扩散权衡**：**写扩散**（每个成员一份收件箱，写放大）vs **读扩散**（群消息只存一份，成员各自用 last_read_seq 游标拉取）——群聊主流是**读扩散**（一条消息存一份，读时按 seq 增量拉）；万人超大群再加下行扇出限速与分层推送。能讲清两种扩散的适用边界，是本题的分水岭。',
          ],
          followUps: [
            {
              question: '消息「已送达」和「已读」分别怎么实现？',
              points: [
                '「已送达」（delivered）= **到达设备**：服务端把消息推到对端网关并收到网关确认即标记；本质是上行 ACK 的镜像——投递超时未 ACK 就转离线，等对方上线拉取后补标记。它是系统可判定的客观事实，不需要对方应用确认。',
                '「已读」（read）= **对端用户确认**：客户端真正看到消息后上报 **read_seq**（读到哪条 seq）→ 服务端更新该用户 `last_read_seq` → 通知发送方更新状态。**用游标而非逐条回执**：一次上报覆盖一段消息，回执流量与消息量解耦。',
                '**语义与隐私差异**：delivered 是客观事实无隐私问题；read 是用户主观行为，必须由客户端主动上报（且多数产品允许关闭已读回执，关闭后双端都看不到）——这一层产品语义也是考点。',
                '工程细节：read_seq 上报要**去抖合并**（会话页滚动时客户端节流、服务端幂等覆盖最新值）；多端取各端 max(read_seq) 群发回执；群聊已读量是 O(群人数)，大群只做“N 人已读”不做逐人回执。',
              ],
            },
            {
              question: '多端登录（手机 + 电脑）时消息同步怎么做？',
              points: [
                '**每个设备独立维护同步游标**（sync seq / last_ack_seq）：登录时上报自己最后确认的 seq，服务端从该位置增量下发缺口——**服务端不关心哪台设备在线，只按设备各自游标补齐**。新设备登录、断线重连、推送丢失统一收敛成同一个“增量同步”模型。',
                '**推送只是加速器，同步才是正确性锚点**：在线推提升实时性，但客户端以 seq 连续性判断缺口，发现空洞就主动拉取——可靠性的锚在“游标 + 拉取”，不在推送，这与主答案“落库 + ACK + 重推 + 去重”一脉相承。',
                '**写路径一致性**：任一端发出的消息由服务端分配全局 seq 后，**所有端（含发送端的其他设备）都经同步通道看到它**；发送端本地先乐观回显，以同步到的 seq 版本为准对齐，避免多端视图分叉。',
                '扩展细节：服务端要**按 device_id 存游标**，设备解绑时清理；已读状态同步复用游标机制（上报 read_seq、其他端订阅变更）；漫游历史 = 按 seq 区间翻页，与实时同步共用一条链路。',
              ],
            },
          ],
        },
        {
          id: 'sd-task-scheduler',
          title: '设计一个分布式任务调度系统，支持定时、延时任务和海量任务的可靠执行。',
          difficulty: 'advanced',
          tags: ['任务调度', '时间轮', '分布式'],
          points: [
            '**需求澄清**：任务类型（**cron 周期 / 延时一次性 / 失败重试**）、量级（每天千万级）、可靠性要求（**不丢、不重复执行**、可重试）、任务是否幂等（发券、推送多数天然幂等——幂等假设直接决定方案的复杂度）。',
            '**核心模型**：**任务表**（task_id、cron 表达式或触发时间、payload、**状态机**：待触发 → 执行中 → 成功 / 重试中 / 死信、next_fire_time、shard_key）+ **执行器注册表**（worker 分组、心跳、负载）+ 调度记录（防重的依据）。',
            '**关键流程**：**触发**——调度节点按时间窗批量捞取到期任务（每秒 tick，配合内存时间轮/最小堆管理近期任务）；**分片**——按 shard_key 哈希把任务划分给不同调度节点，各管各的区间，天然并行且互不重复；**投递**——通过 MQ 推给执行器 worker，执行完回调更新状态。',
            '**可靠性三板斧**：**不丢**——触发前先落库改状态（WAL 思想），节点宕机后按 next_fire_time 重新捞取恢复；**不重**——数据库**乐观锁抢占**（`UPDATE ... SET status = 执行中 WHERE task_id = ? AND status = 待触发`，抢到的节点才投递），任务侧幂等做最后防线；**失败处理**——指数退避重试 N 次后进**死信表** + 告警人工介入。',
            '**扩展与权衡**：海量同秒到期 → 批量拉取 + **时间轮**（HashedWheelTimer 思想）替代逐条扫表；调度层与执行层分离，执行器按任务积压量独立扩缩容。边界感加分项：**简单延时场景直接用 MQ 延迟消息（RocketMQ 延迟级别）或 Redis ZSET 就够了**——需要 cron、分片、重试策略、控制台时才值得自建系统。',
          ],
          followUps: [
            {
              question: '怎么保证同一个任务不被两个调度节点同时执行？',
              points: [
                '首选**数据库乐观锁抢占**：`UPDATE task SET status = \'running\', owner = ? WHERE task_id = ? AND status = \'pending\'`，影响行数为 1 的节点获得执行权——不引入新组件；崩溃后任务停在 running，由超时重置任务（reaper 扫描超时 running）恢复，简单可靠。',
                '**分布式锁方案**：etcd/ZooKeeper 会话租约或 Redis（`SET NX PX` + 唯一持有值 + Lua 释放）。锁必须带**持有者标识与看门狗续期**，防业务没执行完锁先过期被抢；Redis 锁在主从切换可能失效，强正确性场景用 etcd/ZK 租约。',
                '更根本的规避是**分片**：按 shard_key 哈希把任务空间划给固定节点，每片同一时刻只有一个 owner（心跳租约做故障转移）——分片内天然串行，把“抢锁”变成“认领”，是 XXL-Job / Elastic-Job 这类调度器的主流做法。',
                '**最后一道防线是业务幂等**：网络抖动仍可能重复投递，发券/扣款类任务用唯一业务键（幂等表/唯一索引）兜底。**调度层防重是概率收敛，幂等才是正确性保证**——这句总结是本题的加分点。',
              ],
            },
            {
              question: '同一秒有百万任务到期怎么办？（分片 + 时间轮 + 批量）',
              points: [
                '**先定位瓶颈**：百万任务/秒若逐条扫表（`WHERE next_fire_time <= now LIMIT n`），数据库先崩。答案不是更快的 SQL，而是把“找到期任务”从 O(到期量) 降为 O(1) 的**结构改造**。',
                '**时间轮**：环形数组 + 指针，每 tick 移动一格，O(1) 添加/触发；同秒百万任务挂在同一格链表，触发即**整格批量弹出**。秒级精度单层轮（60 格）够用，要更细精度/更长跨度用**层级时间轮**（Kafka 的分层实现）；时间轮只放近期任务，远期任务留在 DB/ZSET，临近再装载。',
                '**分片并行**：按 shard_key 把任务分给 N 个调度节点，各管各的时间轮，吞吐线性扩容；同 key 任务仍在同一分片内保序。',
                '**投递侧削峰**：触发后不逐条调执行器，而是**批量写 MQ**，执行器按消费能力拉取，调度被削成入队吞吐问题；配合优先级与反压（MQ 堆积时降速装填）。收尾补边界：若业务只是“延时 10 分钟发券”，直接用 RocketMQ 延迟消息或 Redis ZSET 轮询，**不要为简单场景自建调度系统**——呼应主答案的边界感。',
              ],
            },
          ],
        },
        {
          id: 'sd-classic-notification',
          title: '设计一个通知系统：短信/邮件/推送多渠道，峰值百万级，不丢、不重、不扰民。',
          difficulty: 'advanced',
          tags: ['通知系统', '消息推送', '异步化'],
          points: [
            '**整体形态 = 异步解耦的消息管道**：业务方只往"通知中台"投递一条消息（模板 id、参数、目标用户、渠道策略），后续**渠道路由、限流、重试、记录**全部由中台承担——业务与渠道彻底解耦，接新渠道只是加一个 adapter。',
            '**可靠性设计**：投递走 MQ 削峰；每条通知**落库 + 状态机**（待发送/发送中/成功/失败/终态）；失败重试**指数退避 + 有界**；渠道回执（运营商/厂商回调）更新终态，**对账任务**兜底扫描超时未回执的记录——"不丢"靠落库 + 重试 + 对账三件套。',
            '**不重（幂等）**：业务方传**唯一业务键**（orderId + notifyType），中台按唯一键去重；渠道层手机号 + 模板 + 时间窗再挡一层——用户视角的重复才是重复。',
            '**不扰民（限流与合并）**：用户级频控（同一用户每小时最多 N 条，按优先级保留高优）；渠道级限流（短信供应商 QPS 合同额度，令牌桶）；**合并策略**——同一订单的多个事件合并成一条；夜间静默（非紧急通知延迟到早晨）。扰民引发的投诉与退订是通知系统最大的业务风险。',
            '容量与成本：模板渲染前置（不要在发送线程做）；**优先级分层**（验证码 > 交易通知 > 营销）保证高优在渠道拥堵时仍能发出；营销类走单独通道与时间窗。',
          ],
          followUps: [
            {
              question: 'App 推送（Push）的到达率为什么远低于短信？系统层面能做什么？',
              points: [
                'Push 依赖厂商通道（APNs/FCM/厂商推送）与 **App 进程存活**：用户杀进程、禁通知、通道 token 失效都会静默丢失；短信直达 SIM 卡所以高。',
                '系统侧优化：**token 生命周期管理**（多设备 token 集合、失效清理）、多通道降级（Push 失败升级短信/邮件，按通知价值决定是否降级）、回执驱动的补偿重发——**通知系统的高级感在"按通知价值匹配渠道成本"**：验证码用最贵的通道保时效，营销用最便宜的通道控成本。',
              ],
            },
          ],
        },
        {
          id: 'sd-classic-ride-dispatch',
          title: '设计一个网约车派单系统：百万司机实时位置，请求要秒级应答、就近匹配。',
          difficulty: 'advanced',
          tags: ['LBS', '派单', '地理空间索引'],
          points: [
            '两个核心数据面：**司机位置流**（百万级司机每 3~5 秒上报位置/朝向/载客状态）与**订单流**（发单 → 派单 → 行程 → 结算）。位置写多读多、可容忍毫秒级误差——典型的**内存 + 空间索引**场景。',
            '**地理空间索引是技术核心**：Redis GEO（zset + geohash 编码）、内存网格（城市切 500m 网格，司机按格注册）或 Google S2——"3 公里内空闲司机"变成查周边网格；geohash 的坑要会讲：**边界问题**（相邻点 hash 前缀可能不同），解法是查本格 + 周围 8 格。',
            '**派单不是"找最近的"**：匹配引擎做多目标权衡——距离与 ETA、司机服务分、顺路程度、公平性（防头部司机垄断）；**批量派单**（N 单 N 司机做二分图匹配，K-M 算法对比贪心）能显著降低整体绕路率。应答 SLA 秒级——先贪心快速应答再异步优化，或小窗口攒批匹配。',
            '**链路与一致性**：上报走长连接网关 → MQ 削峰 → 位置服务写内存网格（TTL 自动过期下线）；派单时用**原子操作（Lua/分布式锁）防一单多派**；订单与位置分库，行程链路用 Saga/消息保证最终一致。',
            '兜底设计：派单失败重试与扩大半径、高峰期调度策略（排队模式/动态定价）、位置服务分级降级——LBS 的可用性直接等于业务可用性。',
          ],
          followUps: [
            {
              question: '为什么用 geohash/网格而不是直接对经纬度建 B+ 树范围查询？',
              points: [
                '"2 公里内"是**二维空间范围查询**，B+ 树只能按单一维度有序——按经度排序后纬度完全无序，一个圆圈要拆成大量区间扫描，等于全表扫。',
                'geohash 把二维编码成一维字符串（前缀相近 ≈ 位置相近），让周边查询退化为**前缀匹配 + 跳表**；代价是极点与边界失真，靠查邻格修正——**空间索引的本质是把二维邻近性映射到一维有序性**。',
              ],
            },
          ],
        },
        {
          id: 'sd-classic-autocomplete',
          title: '设计一个搜索联想（Autocomplete）服务：输入前缀，毫秒级返回 Top 热门候选。',
          difficulty: 'intermediate',
          tags: ['搜索', 'Trie', 'TopK'],
          points: [
            '核心数据结构 **Trie（前缀树）**：查询沿前缀走 O(len)；每个节点挂**该前缀下的 TopK 热词**（离线/写入时预聚合），命中前缀后**直接读榜单**，无需遍历子树——"毫秒级 Top 候选"的关键：**把 TopK 的计算挪到查询之前**。',
            '工程形态三选：**内存 Trie 集群**（性能最好，更新与运维复杂）；**Redis zset + 前缀 key**（前缀为 key、词为 member、热度为 score，实现快但 key 数量爆炸）；**ES completion suggester**（底层 FST，开箱即用）——起步选 ES/Redis，亿级词与极致延迟才自建 Trie 服务。',
            '热度闭环：用户点击/搜索行为回流 → 实时热度管道（Flink 窗口聚合）→ **增量更新节点榜单**（整树重建太重，按前缀分片热更新）；**敏感与运营干预**：黑名单过滤、置顶/屏蔽做成规则层而不是写死数据。',
            '体验细节也是考点：**拼音/纠错**（"shouji" 命中"手机"——拼音归一化层；编辑距离 1~2 的纠错候选）、个性化（用户历史加权）、**防抖 + 缓存**（前端 100ms 防抖、前缀结果短 TTL 缓存）。',
          ],
          followUps: [
            {
              question: 'Trie 很吃内存，亿级词条怎么优化？',
              points: [
                '**压缩三件套**：只存**热搜词库**（长尾词不进 Trie，全量交给搜索引擎兜底）；**Double-Array Trie / FST**（共享前缀压缩，ES/Lucene 的 FST 内存占用比朴素 Trie 低一个数量级）；节点只存 TopK 榜单。',
                '分片降维：按首字母/首二字母分片多实例，单机词库可控；本地缓存挡热点前缀——**内存问题的解法永远是"进内存的少一点"**，而不是堆机器。',
              ],
            },
          ],
        },
        {
          id: 'sd-classic-coupon',
          title: '设计一个营销优惠券系统：亿级发放、防超发、防刷、可核销对账。',
          difficulty: 'advanced',
          tags: ['优惠券', '营销系统', '幂等'],
          points: [
            '领域模型先行：**券模板**（批次：面额、门槛、总量、有效期、适用范围）与**用户券实例**（一张券一条记录，状态机：未领取/可用/已使用/已过期/已退回）——"发放"是模板 → 实例的创建，"核销"是实例的状态迁移，模型清晰是防超发的前提。',
            '**发放防超发**：模板剩余量是热点计数——**Redis 预扣**（DECR 原子递减，负数即拒绝并回补）+ 异步落库创建实例（MQ 削峰）；**兜底约束落在数据库**：剩余数乐观锁 + 用户领取记录唯一索引（user_id + batchId）——缓存是加速，**DB 约束才是防超发的最后防线**。',
            '**防刷**是营销系统的特色考点：领取接口**限流 + 风控**（设备指纹、频次、黑产名单）、发放幂等（同用户同批次唯一）、**券码不可预测**（随机码防遍历薅羊毛）；高风险场景加验证码/延迟到账。',
            '**核销与对账**：核销在交易链路里必须**原子**（券状态 + 订单事务一致：先锁券再下单，或 TCC——冻结/确认/释放）；退单要**退券**（状态回滚）；**对账任务**日切核对"发放量 = 核销量 + 有效余量 + 退回量"——营销资损事故大多靠对账发现。',
            '答题升华：优惠券系统 = **计数系统 + 状态机 + 风控**的组合；把"防超发（库存思路）、防重复（幂等思路）、防刷（风控思路）、防错账（对账思路）"四条线各自点到，就是完整答案。',
          ],
          followUps: [
            {
              question: '爆款券（100 万张，瞬间 5000 万人抢）和普通满减券，设计上最大的区别是什么？',
              points: [
                '爆款券本质是**秒杀库存**：Redis 预扣 + **分桶**（1 个热点 key 拆 100 个子桶降低单 key 竞争）、请求端排队与随机丢弃、**先抢资格后发放**（抢到资格异步到账，成功率与体验都更优）。',
                '普通券量大但无瞬时尖峰：按批量任务/规则触发即可，重点在**批量写入**（攒批 insert）与成本——**"按流量形态选架构"**，同一系统里两种券走两条链路，是这道题的高级答案。',
              ],
            },
          ],
        },
        {
          id: 'sd-classic-distributed-kv',
          title: '设计一个分布式 KV 存储：PB 级数据、毫秒级点查、可水平扩展、容灾。',
          difficulty: 'advanced',
          tags: ['分布式存储', '一致性哈希', 'Raft'],
          points: [
            '先收窄需求（面试第一步）：点查为主还是范围扫描？强一致还是最终一致？多大写吞吐？——**"分布式 KV"是一个谱系**：Redis（内存、主从、AP 倾向）、DynamoDB/Cassandra（无主、AP、最终一致）、TiKV/etcd（Raft 强一致）——先定位再设计，避免上来就堆组件。',
            '**数据分片**：**一致性哈希 + 虚拟节点**（或 range 分片如 TiKV）——虚拟节点平衡负载与异构机器，扩缩容只迁移相邻弧段；分片元数据由**路由层/元数据中心**管理（proxy 或客户端路由表，etcd/ZK 存元数据）。',
            '**副本与一致性**：每分片 **3 副本**，**Raft** 复制（多数派确认、Leader 读写、故障自动选主）是 CP 路线；AP 路线（Dynamo 风格）用 Quorum N/W/R + 向量时钟 + **读修复/反熵**收敛冲突，牺牲强一致换高可用——两条路线的取舍必须能讲清（呼应 CAP）。',
            '**单分片引擎**决定读写形态：LSM-Tree（写多读少，RocksDB 是事实标准——WAL + memtable + SSTable + Compaction）或 B+ 树（读多）；布隆过滤器挡无效读；大 value 拆分或走块存储。',
            '**容灾与运维**：副本跨机架/跨 AZ 放置、**Snapshot + 增量日志**备份恢复、慢节点探测与副本迁移、热点 key 打散与本地缓存——存储系统答辩的重点往往在"出问题时怎么办"。',
          ],
          followUps: [
            {
              question: 'Dynamo 风格的"读修复"和"反熵"是怎么把副本收敛到一致的？',
              points: [
                '写入接受后异步扩散到全部副本（hinted handoff 兜底宕机节点）；**读修复**——读到不一致版本时把最新版本写回旧副本，顺手修复；**反熵**——后台周期性比对副本（**Merkle 树**逐层哈希比对定位差异区间，只同步差异部分）。',
                '冲突裁决：版本向量/时间戳（Last-Write-Wins）或多版本交应用裁决——**最终一致不是"不管了"，而是一套收敛协议**；讲得出 Merkle 树的哈希比对原理，这道题就是满分姿态。',
              ],
            },
          ],
        },
        {
          id: 'sd-classic-object-storage',
          title: '设计一个对象/文件存储系统：海量文件、按 key 读写、低成本、不丢数据。',
          difficulty: 'advanced',
          tags: ['对象存储', '纠删码', '元数据'],
          points: [
            '三层结构：**接入层**（RESTful：PUT/GET/DELETE 按 bucket + object key；鉴权与限流）→ **元数据层**（key → 数据位置映射、版本、ACL；海量元数据本身要分片或专用集群）→ **数据层**（海量数据节点）——**数据与元数据彻底分离**是对象存储与文件系统的关键分野，路径信息全部折叠进 key。',
            '**数据不丢靠冗余**：**多副本**（3 副本，简单、读性能好、成本 3x）或**纠删码 EC**——k 数据块 + m 校验块（如 8+4），丢任意 4 块可恢复，存储开销仅 1.5x，**冷数据与海量场景的事实标准**；代价是读写与修复的计算开销——**冷热分层**：热数据副本、冷数据 EC。',
            '**放置与修复**：块/条带**跨机架/跨 AZ 放置**（防整柜/整机房丢失）；后台 **scrubbing**（周期校验 checksum）发现坏块 → 自动重算重建到新节点——对象存储的"不丢"是**持续修复出来的状态**，不是一次性写入的保证。',
            '工程要点：大文件**分块上传 + 断点续传**（分块 checksum、并行上传）；小文件**合并存储**（log-structured 打包，防元数据爆炸）；生命周期管理（冷热分层、归档、TTL 清理）控制成本。',
            '成本结构是对象存储的灵魂：EC 压缩冗余、高密盘降单位成本、机柜级定制硬件——**答"为什么便宜"比答"怎么存"更体现理解**。',
          ],
          followUps: [
            {
              question: '3 副本和 8+4 纠删码，怎么选？EC 的读写代价具体是什么？',
              points: [
                '**读**：副本可任意副本读（延迟低、可就近）；EC 读一个对象要**读 k 个块重组**（网络放大），热点数据体验差。**写**：副本写 3 份即可；EC 要算校验并写 k+m 份条带。**修复**：副本直接拷一份；EC 读 k 块重算（可用局部校验码 LRC 优化修复流量）。',
                '选型即冷热：**热数据/小文件 → 副本**；**冷数据/大文件/归档 → EC**（成本敏感、读写不频繁）。说得出"EC 是用 CPU 与网络换存储成本"，就拿到了这道题的主线。',
              ],
            },
          ],
        },
        {
          id: 'sd-classic-qrcode-login',
          title: '设计「扫码登录」：二维码背后是什么状态机？手机确认后 PC 怎么知道？',
          difficulty: 'intermediate',
          tags: ['扫码登录', '状态机', '长轮询'],
          points: [
            '先把参与方和信任链说清：**PC（待登录端）、手机（已登录 + 身份凭证）、认证服务端**。核心安全前提：**PC 从头到尾不输入账号密码**——密码只在手机侧（已强认证），扫码本质是"**手机把一次授权转交给 PC 会话**"，这比在陌生电脑上输密码安全得多（规避键盘记录与钓鱼）。',
            '**二维码的本质是一个一次性的临时凭证（qrcodeId/token）**，不是用户信息。完整状态机（答题的主线）：① PC 请求生成二维码 → 服务端创建 **待扫描（created）** 记录（qrcodeId + 过期时间约 1-2 分钟，一次性）→ 二维码内容是带参 URL 或 token 本身；② 手机扫码 → 解析出 qrcodeId，App 里**展示确认页**（显示登录目标设备/地点，用户确认才继续——**防恶意二维码劫持**的关键一步）；③ 用户点确认 → 手机 App 凭**自己的登录态**调用「确认接口」，服务端把该 qrcodeId 置为 **已确认（confirmed）** 并**预绑定一次性的 login ticket**；④ PC 侧拿到 ticket → 换取正式会话（Set-Cookie），状态机走完 → **已登录**。还有 **过期（expired）与已用（consumed）**两个终态：过期要重新生成，ticket 换会话后作废防重放。',
            '**PC 怎么知道"手机已确认"——轮询 vs 推送（必考点）**：**短轮询**简单但 QPS 放大严重；**长轮询**（PC 挂起请求，服务端确认后立即返回，超时重新发起）是大多数实现的选择——请求挂起要设合理超时（如 25s，小于网关超时）；**WebSocket** 实时性最好但为登录场景维护长连接偏重。轮询的**安全细节**：PC 端轮询用的 qrcodeId 只能查状态不能换会话（权限分离），确认产生的 **login ticket 是一次性的**且与 qrcodeId 绑定——防止"知道二维码内容的人冒领会话"。',
            '边界与安全清单（主动说出这些是加分项）：**二维码过期刷新**（前端倒计时自动重新生成）；**防 CSRF/会话固定**（ticket 换会话后 rotate session id）；**手机确认页必须二次展示设备信息**并由用户主动点确认（防"扫码即登录"的盲扫风险）；**限流与防刷**（二维码生成接口按 IP/设备限流，防止批量生成探测）；登录成功后通知手机端（"你的账号在 xx 登录"）形成闭环可审计。',
          ],
          followUps: [
            {
              question: '如果用户扫了二维码，但确认前 PC 端断网/刷新了页面，怎么保证不出问题？',
              points: [
                '用状态机的**单向性与一次性**兜底：qrcodeId 生成后与"PC 会话"绑定（生成时服务端记录发起会话标识），确认后产生的 ticket **只能被原会话取走**——刷新页面后拿新 qrcodeId 重新走流程，旧的到过期自动作废；不存在"别人确认了我的二维码却登录到我这"的交叉。',
                '竞态处理：确认与过期可能同时发生（用户刚点确认、二维码恰好过期）——服务端对 qrcodeId 的状态流转用**原子 CAS**（只有 created 状态能流转到 confirmed），过期后确认接口直接失败并提示重新扫码。状态机 + 单向流转 + 原子变更，是这类"多端协作凭证"题的万能骨架（与扫码支付、授权确认同构）。',
              ],
            },
          ],
        },
      ],
    },
  ],
};
