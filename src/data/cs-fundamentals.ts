import type { Track } from '../types';

export const csTrack: Track = {
  id: 'cs-fundamentals',
  name: '计算机基础',
  icon: '🧱',
  tagline: '网络、操作系统、算法与数据库的地基',
  description: '所有技术岗通用的计算机基础：计算机网络、操作系统、数据结构与算法、数据库原理，是大厂面试的必考区域。',
  color: 'violet',
  topics: [
    {
      id: 'cs-network',
      name: '计算机网络',
      description: '考察 TCP/UDP、HTTP/HTTPS、DNS、CDN 等协议栈的理解深度，是后端与全栈岗位的必考区域。',
      references: [
        { label: 'MDN HTTP 文档', url: 'https://developer.mozilla.org/zh-CN/docs/Web/HTTP' },
        { label: '小林 coding · 图解网络', url: 'https://xiaolincoding.com/network/', note: '中文面试向的网络知识体系整理' },
        { label: 'RFC 9293（TCP 规范）', url: 'https://www.rfc-editor.org/rfc/rfc9293.html', note: 'TCP 协议权威规范' },
        { label: 'Cloudflare Learning Center', url: 'https://www.cloudflare.com/learning/' },
      ],
      questions: [
        {
          id: 'cs-net-osi-tcpip',
          title: 'OSI 七层模型和 TCP/IP 模型分别是什么？为什么要分层？',
          difficulty: 'basic',
          tags: ['网络模型', '分层'],
          points: [
            'OSI 从上到下：**应用、表示、会话、传输、网络、数据链路、物理**七层；工程实践普遍使用 TCP/IP 四层：应用层（HTTP/DNS）、传输层（TCP/UDP）、网络层（IP）、网络接口层，两者是“理论模型 vs 工程实现”的关系。',
            '分层的核心价值是**关注点分离**：每层只向上提供服务、只依赖下层接口，某一层的实现变化（如 IPv4 → IPv6）不影响其他层——这也是一切软件系统分层的思想来源。',
            '各层典型职责与设备：传输层用**端口号**区分主机上的进程；网络层负责**路由与跨网转发**（路由器）；数据链路层负责相邻节点间的帧传输与 MAC 寻址（交换机）；应用层面向业务语义。',
            '答题口诀“每层解决一个问题”：物理层管比特传输，链路层管相邻节点可靠成帧，网络层管端到端寻址路由，传输层管端到端进程间传输（可靠或高效），应用层管业务。',
          ],
          followUps: [
            {
              question: '一次 HTTP 请求的数据从浏览器发出，在协议栈里是怎么被逐层封装的？',
              points: [
                '封装是自上而下**逐层加头**：应用层产出 HTTP 报文 → 传输层加 TCP 头（源/目的**端口**，区分进程）成为段 → 网络层加 IP 头（源/目的 **IP 地址**，负责跨网路由）成为包 → 数据链路层加以太网**帧头帧尾**（源/目的 **MAC 地址**，负责相邻节点投递）成为帧 → 物理层变比特流。接收端逐层**解封装**，每层只拆自己的头。',
                '关键细节是 **MTU 与分片**：以太网 MTU 通常 1500 字节，IP 包超限会在网络层**分片**，任何一片丢失整包都要重传、代价巨大——所以 TCP 用 **MSS**（MTU 减去 IP/TCP 头，典型 1460）在传输层就切好报文，主动避开 IP 分片。',
                '经典陷阱题：“TCP 在哪一层？ARP 在哪一层？”——ARP 为 IP 提供 MAC 解析，通常归为网络层与链路层之间的辅助协议；能顺带说出端口在 TCP 头、IP 地址在网络层头，说明封装是真理解，不是背七层名字。',
              ],
            },
          ],
        },
        {
          id: 'cs-net-tcp-handshake',
          title: 'TCP 为什么是三次握手，而不是两次或四次？',
          difficulty: 'basic',
          tags: ['TCP', '三次握手'],
          points: [
            '三次握手的核心目的是**双方各自确认收发能力正常**，并同步初始序列号（ISN）。',
            '两次握手的问题：服务端无法确认客户端能收到自己的消息，且历史失效的连接请求（网络中滞留的 SYN）到达服务端会**建立无效连接**，浪费资源。',
            '四次握手没必要：SYN+ACK 可以合并，四次只是把可合并的步骤拆开，浪费一次往返。',
            '第三次握手若丢失，服务端会重传 SYN+ACK 并等待；大量半连接会占用 SYN 队列，这也是 **SYN Flood 攻击**的原理，可用 syncookie 缓解。',
            'ISN（初始序列号）要随机生成：一是避免上一次连接的旧报文被误认为新连接的数据，二是序列号可预测容易被伪造报文攻击。',
          ],
          followUps: [
            {
              question: '四次挥手为什么比三次握手多一次？TIME_WAIT 为什么要等 2MSL？',
              points: [
                '多一次的根源：**被动方的 ACK 与 FIN 不能合并**。握手时 SYN+ACK 都是收到 SYN 后立即产生的，可以合并；挥手时收到 FIN 只代表“对方不发数据了”，**自己可能还有数据没发完**，所以先 ACK，等数据发完再单独发 FIN——中间这段就是 CLOSE_WAIT 半关闭状态。',
                'TIME_WAIT 等 **2MSL** 的两个理由：① **保证最后一个 ACK 可重传**——ACK 丢了被动方会重传 FIN，主动方必须还在 TIME_WAIT 才能重新应答（一个 MSL 是报文最大存活时间，一来一回正好覆盖）；② 让本连接**旧报文自然消亡**，避免串进复用相同四元组的新连接。',
                '为什么握手不需要类似等待：握手阶段连接上还没有应用数据，旧 SYN 的危害由**随机 ISN** 抵御（旧序列号落不进新连接的窗口）；挥手前连接上可能还有数据在飞，只能靠时间隔离。',
                '边界延伸：TIME_WAIT 出现在**主动关闭方**，大量出现说明连接被频繁建立/关闭——客户端靠连接池复用或 `tcp_tw_reuse`（依赖时间戳）缓解；服务端应让对端先关。`tcp_tw_recycle` 在 NAT 下会丢包，已从内核移除，面试主动说出来是加分项。',
              ],
            },
            {
              question: 'TCP 如何保证可靠传输？',
              points: [
                '先给框架再展开：**可靠 = 不丢 + 不乱 + 不错 + 不压垮**，分别对应**确认重传**（ACK + 超时/快速重传 + SACK）、**序列号排序**、**校验和**、**流量控制与拥塞控制**。这是主问题的总纲，追问时先给框架避免罗列零散。',
                '机制怎么配合：发送方按窗口发出字节流 → 接收方校验和通过后**累积确认**（ACK 携带期望的下一字节号）并缓存重排乱序报文 → 发送方超时或 3 个重复 ACK 触发重传补缺口 → 窗口动态调节速率。交给应用的一定是按序字节流。',
                '深挖钩子：重传的三代演进（超时重传 → 快速重传 → SACK 选择性确认）、RTO 的自适应计算、流控 vs 拥塞控制的视角差异——每个都能再展开一层，主动留钩子体现体系化掌握。',
              ],
            },
          ],
        },
        {
          id: 'cs-net-tcp-teardown',
          title: '四次挥手的过程是怎样的？TIME_WAIT 为什么要等 2MSL？',
          difficulty: 'basic',
          tags: ['TCP', '四次挥手', 'TIME_WAIT'],
          points: [
            '流程：主动关闭方发 **FIN** → 被动方回 **ACK**（此时被动方可能还有数据没发完，所以 ACK 和 FIN 必须分开，这是比握手多一次的根本原因）→ 被动方数据发完后发 FIN → 主动方回 ACK 并进入 **TIME_WAIT**。',
            'TIME_WAIT 等待 **2MSL** 的原因：① 若最后一个 ACK 丢失，被动方会重传 FIN，主动方必须还活着去应答；② 让本连接的所有旧报文在网络中自然消亡，避免污染复用相同四元组的新连接。',
            'TIME_WAIT 过多的危害与缓解：它出现在**主动关闭方**，大量出现通常说明连接被频繁建立/关闭（比如没用连接池）。作为客户端可开启 `tcp_tw_reuse`（依赖时间戳）安全复用端口；服务端则尽量让客户端先关闭。',
            '排查手段：`netstat` / `ss` 统计各状态连接数；TIME_WAIT 不占文件句柄、本身开销很小，主要风险是占用四元组端口、影响新建连接（端口耗尽）。',
          ],
          followUps: [
            {
              question: 'CLOSE_WAIT 堆积说明什么问题？',
              points: [
                'CLOSE_WAIT 出现在**被动关闭方**：对端发 FIN、本端回 ACK 后，只要应用**一直不调用 close()**，连接就停在这里。所以线上大量 CLOSE_WAIT = **代码存在“收到关闭却不释放连接”的 bug**——它是应用层问题，与内核参数无关（TIME_WAIT 才是参数可调的）。',
                '典型成因：异常分支**漏掉 finally close**；HTTP 场景对端 keep-alive 超时主动断开、服务端读到 EOF（半关闭）却不走关闭流程；连接池把已被对端断开的死连接留在池里反复复用失败。',
                '危害链：CLOSE_WAIT 堆积 → fd 持续占用 → 撞上 `ulimit -n` / `fs.file-max` 上限 → **accept 新连接失败，整个进程不可用**。排查：`ss -antp state close-wait | wc -l` 确认数量并定位进程，再 review 其连接生命周期管理。',
                '修复方向：try-with-resources / defer 保证释放；连接池配**空闲检测与借出校验**剔除死连接；read 返回 EOF 必须关闭。收尾对比：TIME_WAIT 多说明“连接太频繁”，CLOSE_WAIT 多说明“代码有泄漏”——两种堆积、两种病因，一句话点破是高频加分点。',
              ],
            },
            {
              question: 'HTTP 的 keep-alive 和 TCP 的 keepalive 是一回事吗？',
              points: [
                '**不是一回事，是两个层次的机制**：HTTP keep-alive 是**应用层**的连接复用——一条 TCP 连接串行发多个 HTTP 请求，省去每次建连（`Connection: keep-alive`，HTTP/1.1 默认开启）；TCP keepalive 是**传输层**的存活探测——连接空闲超过 `tcp_keepalive_time`（默认 7200 秒）后内核自动发探测包确认对端存活。',
                '**目的不同**：前者省的是**建连开销**（三次握手 + TLS + 慢启动）；后者解决的是**死连接检测**——对端宕机/网络中断后，本端若无数据要发就永远不知道连接已死，探测包触发对端 RST 或本端超时报错。',
                '工程关联：网关/负载均衡的 idle timeout（如 Nginx `keepalive_timeout` 默认 75s（65s 是常见自定义值））通常**先于** TCP keepalive 掐断空闲连接，所以长连接应用要发**应用层心跳**或调大 keepalive 间隔——IM 长连接网关的心跳本质上就是应用层重造 keepalive，且间隔可控、还能顺便携带业务状态。',
                '一句话总结：**一个管复用，一个管探活**。能区分两者并说出“为什么应用层还要再造心跳”，说明理解的是连接生命周期管理，而不是背两个名词。',
              ],
            },
          ],
        },
        {
          id: 'cs-net-tcp-vs-udp',
          title: 'TCP 和 UDP 的区别是什么？分别适用什么场景？',
          difficulty: 'basic',
          tags: ['TCP', 'UDP'],
          points: [
            'TCP：**面向连接、可靠传输（确认重传）、有序（序列号排序）、流量与拥塞控制、字节流**；UDP：**无连接、尽最大努力交付、面向报文、没有拥塞控制**，头部固定 8 字节，开销更小。',
            '字节流 vs 报文常被追问：TCP 没有消息边界，会出现**粘包/拆包**，需要应用层解决（定长、分隔符、长度前缀）；UDP 保留报文边界，一次发送对应一次接收。',
            '场景划分按“正确性 vs 实时性”：TCP 用于 HTTP、文件传输、数据库连接等不容出错的场景；UDP 用于**音视频通话、直播、DNS、游戏同步**等偶尔丢包可容忍、重传反而有害的场景。',
            'UDP 并非低级协议：QUIC（HTTP/3）、KCP 等都在 UDP 之上用应用层实现可靠传输与拥塞控制，绕开内核 TCP 的僵化，兼顾可靠与低延迟。',
          ],
          followUps: [
            {
              question: '如果让你在 UDP 之上实现可靠传输，你会怎么做？',
              points: [
                '把 TCP 的可靠性机制在**应用层重造**：**序列号 + ACK** 确认重传、接收端**乱序重排**、**滑动窗口**做流量控制、**拥塞控制**限制发送速率——QUIC 是完整实现这套语义的工业级范例；KCP 则牺牲公平性换低延迟（更激进的重传与退避策略），说明这套机制可以按业务定制。',
                '为什么要绕开 TCP：**内核协议栈升级周期以年计**，新算法落地难；TCP 字节流要求全局有序，一条流丢包会阻塞整个连接（队头阻塞），QUIC 让 stream 之间独立确认；用户态实现随应用发版迭代，重传与拥塞策略可按场景定制。',
                '代价主动说：用户态收发包与加密的 **CPU 开销更高**；激进策略会破坏 **TCP 友好性**（公网上挤占他人带宽要谨慎）；部分网络对 UDP 有 QoS 限制，需要退化回 TCP 兜底。结论表达：UDP 之上的可靠传输是“用工程复杂度换可控性”，不是免费午餐。',
              ],
            },
          ],
        },
        {
          id: 'cs-net-dns',
          title: 'DNS 的解析过程是怎样的？有哪些常见的优化手段？',
          difficulty: 'basic',
          tags: ['DNS', '域名解析'],
          points: [
            '解析链路是**递归 + 迭代结合**：客户端 → 本地 DNS 服务器（递归查询，替你问到底）→ 本地 DNS 依次询问**根域名服务器 → 顶级域服务器（.com）→ 权威域名服务器**（迭代查询），最终把 IP 返回给客户端。',
            '多层缓存：**浏览器缓存 → 操作系统缓存（含 hosts 文件）→ 本地 DNS 服务器缓存**，由记录的 TTL 控制过期时间，绝大多数解析在缓存层就结束了。',
            '传输层协议：普通查询用 **UDP 53**（快、报文小）；响应超过 512 字节（早期限制）或 DNSSEC 场景、以及主从之间的**区域传送**用 TCP。',
            '工程优化：**HTTPDNS** 让客户端绕过运营商 LocalDNS，解决其**劫持与调度不准**的问题；CDN 的 CNAME 调度也发生在 DNS 解析阶段，按用户位置返回最近边缘节点。',
          ],
          followUps: [
            {
              question: 'DNS 的 TTL 过长或过短各有什么问题？发布切流时 DNS 有什么坑？',
              points: [
                '**TTL 是一致性与解析成本的旋钮**：过长则缓存命中率高、权威服务器压力小，但**故障切换与扩容不生效**——用户拿着旧 IP 继续请求已下线的节点；过短则调度灵活，但解析量暴涨、每次多一跳延迟。生产常见 60-600 秒，按变更频率定。',
                '切流的坑在**缓存不可控**：运营商 LocalDNS 可能**无视 TTL 私自延长缓存**，切流后长时间仍有用户打到旧机房；浏览器与操作系统还有各自的解析缓存。对策：提前数小时调低 TTL 再变更、HTTPDNS 绕开 LocalDNS、旧线路保留一段时间做兼容回源。',
                '进阶表达：把 DNS 当**流量调度的第一层**而不是纯解析——多线路智能解析做容灾分流、按运营商与地域调度；但它粒度粗、生效慢，精确实时的调度要靠网关层与服务发现补位——能说出 DNS 调度的边界，比背解析流程高一档。',
              ],
            },
          ],
        },
        {
          id: 'cs-net-http-caching',
          title: 'HTTP 的强缓存和协商缓存是怎么工作的？',
          difficulty: 'basic',
          tags: ['HTTP', '缓存'],
          points: [
            '**强缓存**：命中时浏览器直接使用本地副本，不发请求。HTTP/1.1 用 `Cache-Control: max-age=N`（优先级更高），旧协议用 `Expires`（绝对时间，受客户端时钟影响）。',
            '**协商缓存**：强缓存过期后带上标识问服务器，资源未变返回 **304**。两组标识：`Last-Modified / If-Modified-Since`（秒级精度）和 `ETag / If-None-Match`（内容指纹，优先级更高，能解决一秒内多次修改、mtime 不可靠的问题）。',
            '典型策略：HTML 入口用 `Cache-Control: no-cache`（每次协商保证新鲜），带内容哈希文件名的 JS/CSS/图片用 `max-age=31536000, immutable`（一年强缓存，改版靠换文件名）。',
            '常用指令辨析：`no-cache` 是“可以缓存但必须协商”，`no-store` 是“完全不缓存”，`s-maxage` 只对 CDN 等共享缓存生效——这三组区别是高频考点。',
          ],
          followUps: [
            {
              question: 'from memory cache 和 from disk cache 有什么区别？资源存在哪里？',
              points: [
                '**读取顺序**：Service Worker → **Memory Cache** → **Disk Cache** → 协商缓存/网络。控制台看到 `from memory cache` 说明直接读了内存副本，`from disk cache` 读了磁盘——两者都**没有发生网络请求**，也都没走协商。',
                '**两类缓存的取舍**：memory cache 读取快（微秒级）但**随渲染进程结束而释放**（刷新标签页还在，关掉就没），容量小；disk cache 持久化、重启浏览器仍在，但读取要一次磁盘 IO。哪些资源进哪类由浏览器启发式决定（小而高频的常进 memory，大文件进 disk），**不受开发者直接控制**。',
                '边界提醒：强缓存命中（memory/disk cache）时连协商都不会发生，ETag/Last-Modified 只有强缓存过期后才登场；再往上还有 HTTP/2 的 **push cache**（极短暂的会话级缓存），能按层次说全就是加分项。',
              ],
            },
          ],
        },
        {
          id: 'cs-net-tcp-reliable',
          title: 'TCP 是如何保证可靠传输的？',
          difficulty: 'intermediate',
          tags: ['TCP', '滑动窗口', '重传'],
          points: [
            '核心机制四件套：**序列号 + 确认应答（ACK）**保证有序不丢，**校验和**检错，**超时重传与快速重传**恢复丢包，**滑动窗口**做流量控制。',
            '**滑动窗口**：接收方在 TCP 头部通告自己的接收能力，发送方未确认的在途数据不能超过窗口大小，实现**流量控制**——防止发送过快压垮接收方。',
            '重传策略演进：**超时重传**的 RTO 随 RTT 自适应测量，但等超时代价大；**快速重传**在收到 3 个重复 ACK 时立即重传，不必等超时；**SACK** 选项让接收方告知已收到的不连续区间，只补缺口，减少不必要的重传。',
            '总结框架（加分表达）：可靠性 = 不丢（重传）+ 不乱（序列号排序）+ 不错（校验和）+ 不压垮接收方（流量控制）+ 不压垮网络（拥塞控制）。注意流控与拥塞控制的区别：前者是接收方视角，后者是网络视角。',
          ],
          followUps: [
            {
              question: '流量控制和拥塞控制的区别是什么？',
              points: [
                '**视角与对象不同**：流量控制是**端到端**问题——接收方处理能力有限，通过 TCP 头的**窗口字段（rwnd）**通告剩余缓冲；拥塞控制是**全局网络**问题——链路与路由器容量有限，发送方用**拥塞窗口 cwnd**（自己估计、对方不可见）探测网络。',
                '**信息来源不同**：rwnd 是接收方**显式告知**的精确值；cwnd 是发送方根据丢包/延迟信号**推测**的估计值。实际发送窗口 = **min(rwnd, cwnd)**，两套机制同时生效、独立演化——这一句是主答案末尾结论的展开。',
                '形象区分：流控是“别倒太快，我杯子快满了”（接收方视角）；拥塞控制是“水管快堵了，大家都慢点”（网络视角）。流控侧边界细节：rwnd 降为 0 时发送方停止发送并启动**持续计时器**定期发窗口探测，防零窗口死锁。',
                '延伸：Window Scale 选项解决 16 位窗口字段最大 64KB 的限制；延迟确认（Delayed ACK）也会影响速率感知；BBR 时代拥塞控制不再依赖丢包而改测瓶颈带宽与最小 RTT——两套机制在现代协议栈里都在演进。',
              ],
            },
            {
              question: 'RTO 是怎么计算的？为什么不能设得过长或过短？',
              points: [
                '**RTO 基于 RTT 的自适应估计**（RFC 6298）：对每次 RTT 采样更新平滑值 `SRTT = (1-α)·SRTT + α·RTT`（α = 1/8）与偏差 `RTTVAR = (1-β)·RTTVAR + β·|SRTT - RTT|`（β = 1/4），最终 **RTO = SRTT + 4·RTTVAR**——均值加 4 倍标准差，容忍网络抖动。',
                '**太短的代价**：RTO 小于实际 RTT，报文还在路上就被判丢失触发重传——冗余报文挤占带宽，ACK 混淆导致再重传（**重传风暴**），恰恰在网络拥塞时雪上加霜。',
                '**太长的代价**：真丢包要等一个 RTO 才恢复，吞吐骤降。配套规则：**Karn 算法**规定重传报文的 RTT 不计入采样（无法区分 ACK 属于哪次发送），重传后 RTO **指数退避**（每次翻倍）避免持续施压拥塞网络。',
                '演进收尾：既然等超时代价大，就有了快速重传（3 个重复 ACK 立即重传）与 SACK（只补缺口），以及针对尾包丢失的 TLP 探测——这条演进线正好回答“RTO 难调准，协议怎么绕开它”，可与拥塞控制专题互相串起来讲。',
              ],
            },
          ],
        },
        {
          id: 'cs-net-http-evolution',
          title: 'HTTP/1.1、HTTP/2、HTTP/3 分别解决了什么问题？',
          difficulty: 'intermediate',
          tags: ['HTTP', 'HTTP/2', 'QUIC'],
          points: [
            '**HTTP/1.1 的痛点**：文本协议解析低效；同一连接上响应必须按序返回，存在**队头阻塞**；keep-alive 复用了连接但同一时刻只能处理一个请求，浏览器只能靠多开连接（6-8 个）缓解。',
            '**HTTP/2**：二进制分帧、**多路复用**（一个 TCP 连接上并发多个 stream）、头部压缩 **HPACK**、服务器推送。但它只解决了 HTTP 层的队头阻塞——TCP 层丢一个包，所有 stream 都得等重传（TCP 字节流要求有序交付）。',
            '**HTTP/3**：传输层换成 **QUIC**（基于 UDP），stream 之间真正独立，单条 stream 丢包不再阻塞其他 stream；握手与 TLS 1.3 合并实现 **1-RTT/0-RTT** 建连；用 **Connection ID** 标识连接而非四元组，WiFi 切 4G 连接不中断（连接迁移）。',
            '代价与现状：QUIC 在**用户态**实现、强制 TLS 1.3，部署依赖服务器与中间设备支持；HTTP/2 的 HPACK 动态表要求头部按序解码，也残留一定队头约束——这正是它没有彻底解决问题的一部分。',
          ],
          followUps: [
            {
              question: 'QUIC 为什么基于 UDP 实现，而不是升级 TCP？',
              points: [
                '**TCP 的演进被锁死在内核**：协议栈在操作系统内核里，改一次要等系统升级，部署周期以年计；NAT、防火墙等**中间设备**按 TCP 语义深度解析，带新选项的报文可能被直接丢弃——在 TCP 上加特性随时被网络“截胡”。UDP 被当作尽力而为的搬运工透传，**QUIC 把可靠传输整体放到用户态**，随应用发版迭代。',
                '**UDP 的无连接语义还带来两个自由**：用 **Connection ID** 而非四元组标识连接，WiFi 切 4G 连接不中断（**连接迁移**，TCP 换 IP 就得重建）；stream 之间独立确认，单流丢包不阻塞其他流，彻底解决**传输层队头阻塞**。',
                '代价要主动说：用户态收发包与加解密的 **CPU 开销高于内核 TCP**；少数网络对 UDP 限速，需要退化回 HTTP/2 over TCP 兜底。能说出“内核僵化倒逼协议用户态化”这条主线，就抓住了这道题的本质。',
              ],
            },
            {
              question: 'HPACK 是怎么压缩头部的？为什么它要求头部按序解码？',
              points: [
                '三个手段：**静态表**（预置 61 个常见头字段如 `:method`、`content-type`，只发索引）、**动态表**（连接级缓存，把出现过的键值对存进去，后续只发索引）、**Huffman 编码**（压剩余字符串值）。重复头部的开销从几百字节压到几个字节。',
                '必须按序的原因：**动态表是连接级共享状态**，编码端按发送顺序更新，解码端必须按相同顺序同步，两边表状态才一致——一旦乱序，索引指向的键值对就错了。所以 HTTP/2 帧必须按序交付，**TCP 丢包会卡住头部解码**——队头阻塞以另一种形式残留，这正是主答案里“HTTP/2 没彻底解决问题”的机制细节。',
                '呼应 HTTP/3：**QPACK** 用**独立的单向流**传动态表更新，头部可落后于数据流，解码端按“数据引用的表版本”解码，允许乱序——代价是可能要等表状态或退回静态表。能讲出 HPACK → QPACK 的演进动机，说明理解的是“压缩与并发的矛盾”。',
              ],
            },
          ],
        },
        {
          id: 'cs-net-https-handshake',
          title: 'HTTPS 的握手过程是怎样的？证书是如何校验的？',
          difficulty: 'intermediate',
          tags: ['HTTPS', 'TLS', '证书'],
          points: [
            '以 TLS 1.2 的 RSA 握手为例：ClientHello（随机数 + 支持的加密套件）→ ServerHello + **证书** → 客户端验证证书后，用证书中的公钥加密**预主密钥**发给服务端 → 双方用「两个随机数 + 预主密钥」推导出**对称会话密钥**，之后的通信全部使用对称加密。',
            '为什么要混合加密：**非对称加密**（RSA/ECDHE）慢但适合做密钥交换与身份验证；**对称加密**（AES）快，适合加密大量业务数据——各取所长。',
            'TLS 1.3 的改进：握手从 2-RTT 降到 **1-RTT**，会话恢复可到 **0-RTT**；删除了 RSA 密钥交换（它不具备**前向安全性**），只保留（EC）DHE——即使服务器私钥日后泄露，历史抓包也无法解密。',
            '证书校验链：检查**有效期**、**域名匹配**（SAN）、是否被**吊销**（CRL/OCSP），再沿**证书链**逐级验签直到受信任的根 CA——根证书预置在操作系统和浏览器中，这是信任的锚点。',
            '安全本质：中间人伪造的证书无法通过 CA 私钥的链式验签；除非用户手动信任了恶意证书，HTTPS 才可能被中间人攻破。',
          ],
          followUps: [
            {
              question: '对称加密和非对称加密各自的优缺点是什么？',
              points: [
                '**对称加密（AES/ChaCha20）**：加解密同一密钥，速度快（AES-NI 硬件加速下 GB/s 级），适合大量数据；致命弱点是**密钥分发**——不安全信道上怎么把密钥交给对方？直接传等于裸奔；且 n 人两两通信需要 O(n²) 个密钥，管理爆炸。',
                '**非对称加密（RSA/ECC）**：公钥公开、私钥自持，天然解决分发与**身份认证**（私钥签名可验身份）；但计算慢（比对称慢 2-3 个数量级），且单方向加密只能保护“发给自己”的数据——用公钥加密的信息只有私钥能解，反向通道没有保护。',
                '**HTTPS 的组合拳正是取长补短**：握手阶段用非对称（ECDHE 交换密钥 + 证书验签）协商出**会话密钥**，之后全部业务数据用对称加密——非对称只用在商量密钥的一两次往返上，慢被摊薄到整个连接生命周期之外。',
                '边界延伸：RSA 只能做密钥交换/签名；ECDHE 支持**前向安全**（每次会话临时密钥，私钥日后泄露也解不开历史流量）；量子计算威胁下“先存后解”让前向安全从加分项变成必需品——这一句直接引出下一条追问。',
              ],
            },
            {
              question: 'TLS 1.3 为什么删掉了 RSA 密钥交换？',
              points: [
                '**RSA 密钥交换没有前向安全**：客户端用服务器证书公钥加密预主密钥，攻击者今天抓包存下全部流量，**未来任何一天**拿到服务器私钥（拖库、漏洞、司法调取），即可解出每个会话的预主密钥、推导全部历史会话密钥——历史流量全裸。',
                '**（EC）DHE 的修正**：双方每次握手交换**临时** DH 公钥，算出共享密钥后即丢弃；长期私钥只用于**签名认证**（证明临时公钥没被中间人替换），不参与密钥推导——私钥日后泄露，历史流量依然安全，因为临时密钥早已不存在。',
                '顺带的性能收益：TLS 1.3 把密钥材料（key_share）合并进 ClientHello，握手降到 **1-RTT**，会话恢复（PSK）可 **0-RTT**；但 0-RTT 数据有**重放风险**，只允许幂等请求（GET/幂等业务）——主动讲出这个边界是加分点。',
                '答题落点：删 RSA 不是因为 RSA 算法被破解，而是**“静态密钥交换”这种用法在威胁模型上不合格**——RSA 做证书签名完全没问题。能区分“算法安全性”与“协议用法安全性”，就是这道题的深度所在。',
              ],
            },
          ],
        },
        {
          id: 'cs-net-cdn',
          title: 'CDN 的原理是什么？如何让用户拿到最近的资源？',
          difficulty: 'intermediate',
          tags: ['CDN', '缓存'],
          points: [
            'CDN = 分布式**边缘节点**缓存集群 + 调度系统。两个核心环节：**调度**——域名 CNAME 到 CDN 的智能调度 DNS，按用户 IP/运营商/地理位置返回最近节点；**缓存与回源**——节点有缓存直接返回，未命中则回源站拉取并缓存。',
            '缓存语义复用 HTTP：源站通过 `Cache-Control` / `Expires` 控制边缘缓存时长，`s-maxage` 专门约束 CDN 这类共享缓存。',
            '更新一致性三板斧：**版本化 URL**（文件名带内容 hash，新旧共存，推荐）、调用 CDN 的**刷新/预热接口**主动清除、设置较短 TTL 兜底。',
            'CDN 不只做静态加速：还承担 **DDoS 防护、TLS 卸载、动态链路加速**（回源走 CDN 优化的专线路由）等职责，本质是“把计算和存储推向离用户更近的地方”。',
          ],
          followUps: [
            {
              question: '什么是回源风暴？怎么防止缓存失效瞬间把源站打垮？',
              points: [
                '**成因**：大量请求同时 miss——热门资源集中过期、CDN 侧批量刷新配置失误、新资源上线瞬间全网节点同时回源，回源流量被放大数倍到数十倍压向源站；源站平时被 CDN 挡着、容量按命中率规划，往往最先被打垮。',
                '**四层缓解**：**预热**——大促或发布前主动把热点拉到边缘，避开开抢瞬间集中 miss；**回源合并（single-flight）**——同一资源的并发 miss 只放一个回源，其余等结果共享；**分级缓存**——边缘 → 区域中心 → 源站逐层挡；**TTL 加随机抖动**，错开过期时刻。',
                '兜底意识：极端场景仍有漏网，源站自身要有限流与降级预案（静态兜底页、只读模式）；把**回源带宽与回源 QPS** 纳入监控告警——它是缓存配置错误的直接信号，说出监控点体现生产意识。',
              ],
            },
            {
              question: '动态内容没法缓存，CDN 怎么加速？',
              points: [
                '**加速的不是内容，是链路**：API、个性化页面这类动态请求，走 CDN 的**骨干网/专线回源**——用户到就近边缘走最短公网路径，边缘到源站走 CDN 内网优化路由与长连接，绕开公网拥塞和跨运营商绕行；这是动态加速（DCDN 类产品）的主流形态。',
                '**协议与连接优化**：边缘节点做 **TLS 卸载与会话复用**（用户侧 1-RTT，边缘到源站长连接复用）、QUIC 抗弱网抖动，缩短的是每一跳的建立与传输成本。',
                '**更进一步是边缘计算**：把可就近执行的逻辑（鉴权、AB 实验、HTML 拼装、缓存规则）下沉到边缘运行（Cloudflare Workers 等），动态响应就近生成——呼应主答案“把计算和存储推向离用户更近的地方”。必须回源的数据库查询只能优化传输路径，这一点要诚实说清，避免把 CDN 说成万能。',
              ],
            },
          ],
        },
        {
          id: 'cs-net-url-to-page',
          title: '从输入 URL 到页面展示，中间发生了什么？',
          difficulty: 'intermediate',
          tags: ['HTTP', '浏览器渲染', '综合题'],
          points: [
            '网络阶段按序作答：**DNS 解析**（多层缓存 → 递归/迭代）→ **TCP 三次握手** → 若是 HTTPS 再走 **TLS 握手** → 发送 HTTP 请求。强缓存命中的页面，DNS 和 TCP 都不会发生。',
            '服务端链路：请求经**负载均衡**（LVS/Nginx）→ 网关/反向代理 → 应用服务（查缓存、查数据库）→ 返回响应（状态码、响应头、body）；301/302 会从头重走整个流程。',
            '浏览器渲染：解析 HTML 构建 **DOM 树**，解析 CSS 构建 **CSSOM**，合成**渲染树** → 布局（Layout）→ 绘制（Paint）→ 合成（Composite）；同步 `<script>` 会阻塞 DOM 解析，所以有 defer/async。',
            '答题技巧：这条链路是“总纲题”，答案要为追问留钩子——每个环节（握手、缓存、负载均衡、渲染）都能展开成一题，先给骨架再等面试官选分支深挖。',
          ],
          followUps: [
            {
              question: '这条链路中哪些环节最容易出现线上问题？',
              points: [
                '**DNS 层**：LocalDNS 劫持与调度不准（用户被解析到错误或远端节点）、TTL 过长导致切流不生效、解析超时。对策：HTTPDNS、域名预解析、合理 TTL 与多供应商容灾。',
                '**连接与 TLS 层**：握手 RTT 高导致首字节慢（海外/弱网）、**证书过期**引发全站报错（经典事故）、TLS 版本兼容问题。对策：会话复用、证书监控与自动续期、协议灰度升级。',
                '**服务端链路**：负载均衡健康检查误摘节点、网关连接队列打满（对应 TCP 队列专题）、应用依赖的缓存/DB 慢查询拖垮响应、失败重试风暴放大故障——这是后端排查的主战场，可与连接池、慢 SQL 专题串联。',
                '**渲染层与答题策略**：同步 script 阻塞 DOM、大图未懒加载、CDN 缓存配置错误导致回源风暴。高分表达是按层给出“典型故障 + 一句对策 + 监控指标”（DNS 成功率、握手耗时、TTFB、FCP）——展示的不是链路背诵，而是**可观测性与故障定位意识**。',
              ],
            },
            {
              question: '重排（reflow）和重绘（repaint）有什么区别？',
              points: [
                '**区别在影响范围**：重排 = **几何属性**（位置、尺寸、布局）变化，浏览器要重新计算 Layout，**必然连带重绘**；重绘 = 仅**外观属性**（color、background、visibility）变化，跳过布局直接重画，代价小一档。改 `width/top` 触发重排，改 `color` 只触发重绘。',
                '**还有更便宜的路径要主动提**：`transform`、`opacity` 只走**合成（composite）**，不触发重排重绘（提升为合成层后由 GPU 处理），动画首选；反之读取 `offsetTop` / `getComputedStyle` 会触发**强制同步布局**——改样式后立刻读几何值，浏览器被迫马上重排，循环读写交错是性能杀手。',
                '优化手段：批量 DOM 操作（DocumentFragment、display:none 中转）、用 class 统一改样式而非逐条内联、动画用 transform/opacity + `will-change`、读写分离（FastDOM 思路）。',
                '收尾呼应：这块是“URL 到页面”渲染阶段（渲染树 → Layout → Paint → Composite 流水线）的深挖——流水线上任何一步失效，后续步骤全部重来，这就是代价差异的根源。',
              ],
            },
          ],
        },
        {
          id: 'cs-net-tcp-congestion',
          title: 'TCP 拥塞控制是怎么工作的？讲讲慢启动、拥塞避免、快重传、快恢复。',
          difficulty: 'advanced',
          tags: ['TCP', '拥塞控制'],
          points: [
            '发送方维护**拥塞窗口 cwnd**（对网络拥塞程度的估计），实际发送窗口 = min(接收窗口 rwnd, cwnd)——流控看接收方，拥塞控制看网络，两个窗口取小。',
            '**慢启动**：cwnd 从小值起步，每收到一个 ACK 就 +1（每经过一个 RTT 翻倍，指数增长），到达阈值 **ssthresh** 后切换到拥塞避免。',
            '**拥塞避免**：cwnd 每个 RTT 只 +1（线性增长，加法增大），缓慢探测网络容量上限，直到出现丢包信号。',
            '丢包的两种处理（Reno）：**超时**说明严重拥塞——ssthresh = cwnd/2，cwnd 重置为 1 重新慢启动；**3 个重复 ACK 触发快速重传**说明轻度丢包——ssthresh = cwnd/2 后进入**快恢复**，cwnd 减半继续拥塞避免（乘法减小），不必从零开始。',
            '延伸加分：传统 Reno/Cubic（Linux 默认）把丢包当作拥塞信号，在无线网络和高丢包链路上误判严重；**BBR** 改为主动测量瓶颈带宽与最小 RTT 来建模，不再依赖丢包，长肥管道上吞吐显著更好。',
          ],
          followUps: [
            {
              question: '为什么超时重传和快重传之后，cwnd 的处理策略完全不同？',
              points: [
                '**两种丢包信号代表不同的拥塞程度**：**超时**意味着丢失报文之后的所有报文都没有 ACK——网络可能已经严重拥塞甚至瘫痪，于是 ssthresh = cwnd/2、cwnd 重置为 1，从慢启动重新探测；**3 个重复 ACK** 说明丢失报文之后的报文仍在到达——网络还在通，只是轻度丢包，cwnd 减半进入快恢复即可，保留大部分发送能力。',
                '这套分级体现的设计思想：**按信号严重程度调节退避力度**，而不是一刀切——信号越强（全网静默）说明拥塞越重、退得越狠；信号弱（局部缺口）说明只是个别包丢了。',
                '边界延伸：3 个重复 ACK 也可能由**乱序**而非丢包产生——SACK/D-SACK 让接收方报告已收到的不连续区间，发送方能区分“真丢包”与“乱序”，避免误降 cwnd；这也是 SACK 成为现代标配的原因。',
              ],
            },
            {
              question: 'BBR 的核心思想是什么？它有什么局限？',
              points: [
                '**从“响应丢包”改为“主动建模”**：Reno/Cubic 把缓冲区填满、出现丢包才降速——高带宽链路上先撑爆 Buffer（**bufferbloat**，排队延迟暴涨），无线网络把误丢包当拥塞误判；BBR 测量**瓶颈带宽 BtlBw** 与**最小 RTT RTprop**，把在途数据维持在 **BDP = 带宽 × 最小 RTT** 附近——不靠排队也不靠丢包就能跑满带宽。',
                '**局限一：公平性**——不看丢包，与 Cubic 共存时会持续多占带宽（v1 尤其明显，v2/v3 加入压制机制）；**局限二：估计漂移**——最小 RTT 用长期窗口估计，路由切换、ACK 聚合都会让模型失真，对 ACK 丢失也更敏感。',
                '工程落点：BBR 依赖 **pacing**（按计算出的速率匀速发包）而非突发窗口，需要内核与 `fq` 队列支持；长肥管道、跨地域、高丢包链路收益大，浅缓冲低延迟内网收益有限——能按场景说收益，比背参数高一档。',
              ],
            },
          ],
        },
        {
          id: 'cs-net-tcp-queue',
          title: 'TCP 的半连接队列和全连接队列是怎么回事？SYN Flood 如何应对？',
          difficulty: 'advanced',
          tags: ['TCP', 'SYN Flood', '生产排查'],
          points: [
            '服务端收到 SYN 回复 SYN+ACK 后，连接处于 **SYN_RCVD**，进入**半连接队列**（SYN 队列）；收到第三次握手的 ACK 后变为 ESTABLISHED，等待应用调用 accept，进入**全连接队列**（accept 队列，长度由 `listen(backlog)` 与内核 `somaxconn` 取小决定）。',
            '全连接队列溢出的现象：默认配置下服务端**静默丢弃**第三次握手的 ACK（`tcp_abort_on_overflow=0`），客户端以为自己连上了，但请求无响应超时；关键排查命令是 `netstat -s | grep -i overflow`。',
            '**SYN Flood**：攻击者伪造大量 SYN（往往是假源 IP）灌满半连接队列，正常用户无法建连。核心缓解是 **syncookies**：不再占用队列，把连接信息编码进 SYN+ACK 的序列号，收到合法 ACK 后再重建连接；调大队列、减少 SYN+ACK 重传次数只是辅助手段。',
            '应用层视角：accept 速度跟不上（业务线程处理慢）时，调大 backlog 只是缓冲，最终上限由消费能力决定；正确方向是优化处理速度或做接入层拆分。',
          ],
          followUps: [
            {
              question: 'accept 队列满了之后，正在完成三次握手的新连接会发生什么？',
              points: [
                'Linux 默认行为（`tcp_abort_on_overflow=0`）：收到第三次握手的 ACK 时若全连接队列已满，内核**静默丢弃这个 ACK**，连接停在 SYN_RCVD；客户端却已进入 ESTABLISHED，发数据收不到 ACK 只能**超时重传**——用户侧现象是“连上了但请求超时”，极具迷惑性。',
                '后续演化：服务端会重传 SYN+ACK（次数由 `tcp_synack_retries` 控制），期间应用 accept 腾出位置、客户端重传的 ACK 被接收，连接才真正建立；重传耗尽则连接丢弃，客户端报 timeout/reset。',
                '打开 `tcp_abort_on_overflow=1` 改为直接回 **RST**——客户端立刻拿到“connection refused”，快速失败、失败可见，代价是放弃内核的重试缓冲。**要“慢”还是要“无声失败”，这个权衡表达本身就是得分点**。',
                '治理顺序：`netstat -s | grep -i overflow` 确认溢出 → 查应用 accept 消费速度（线程阻塞、慢请求、事件循环卡顿）→ 调大 `backlog` 与 `somaxconn` 只是缓冲，**根治靠提升处理速度或扩接入层**——与“消息队列堆积先看消费能力”完全同构。',
              ],
            },
            {
              question: '如何观测半连接队列溢出？',
              points: [
                '**第一手证据是内核计数器**：`netstat -s | grep -i listen`（或 `nstat -az TcpExtListenOverflows TcpExtListenDrops TcpExtSyncookiesSent`）——`ListenOverflows` 是全连接队列满丢弃的次数；`SyncookiesSent` 持续增长说明半连接队列被打满、已触发 syncookie 防御。',
                '**连接状态分布**：`ss -ant | awk \'{print $1}\' | sort | uniq -c` 里大量 **SYN_RCVD** 就是半连接堆积的直接表现；再看 `ss -lnt` 的 Recv-Q/Send-Q——对 LISTEN 状态的 socket，**Recv-Q 是当前全连接队列长度、Send-Q 是队列上限**，逼近即溢出前兆。',
                '**压力归因**：SYN_RCVD 巨量且源 IP 分散/伪造、`SyncookiesRecv` 有回包 → 疑似 SYN Flood；SYN_RCVD 适中但 overflow 持续增长 → 更可能是应用 accept 慢。可用 `tcpdump` 抓 SYN/SYN+ACK 的重传模式佐证。',
                '收口：overflow 增速、syncookie 触发次数要进**监控告警**——半连接问题在用户侧只是“偶尔连不上”，没有主动观测几乎无法定位，这句话体现生产意识。',
              ],
            },
          ],
        },
      ],
    },
    {
      id: 'cs-algo',
      name: '数据结构与算法',
      description: '考察基础数据结构、复杂度分析与经典解题套路，配合手撕代码检验编码功底，是所有岗位的通用门槛。',
      references: [
        { label: 'OI Wiki', url: 'https://oi-wiki.org/' },
        { label: 'LeetCode 中国站', url: 'https://leetcode.cn/' },
        { label: 'VisuAlgo 数据结构可视化', url: 'https://visualgo.net/zh' },
        { label: 'Big-O Cheat Sheet', url: 'https://www.bigocheatsheet.com/' },
      ],
      questions: [
        {
          id: 'cs-algo-array-vs-linkedlist',
          title: '数组和链表的区别是什么？各自适合什么场景？',
          difficulty: 'basic',
          tags: ['数组', '链表'],
          points: [
            '**数组**：内存连续，支持下标 **O(1) 随机访问**，尾部插入均摊 O(1)；但中间插入/删除要整体搬移 O(n)，扩容需要整块迁移。**链表**：节点 + 指针，在已知前驱的位置**O(1) 插入/删除**；但随机访问必须 O(n) 遍历，且每个节点有指针额外开销。',
            '隐藏考点——**缓存局部性**：数组连续存储能利用 CPU 缓存行预读，实际遍历速度远快于复杂度相同的链表；这就是“链表理论删除 O(1)，实践却常常不如数组”的原因。',
            '选型经验：读多写少、按下标访问 → 数组/动态数组；频繁头尾增删 → 双端队列；需要持有节点位置反复中间增删 → 链表（如 LRU 的双向链表 + 哈希表组合）。',
            '延伸加分：**跳表**在有序链表上加多级索引，把查找降到 O(log n)，实现比平衡树简单且范围查询天然有序——Redis 的 zset 选它不选红黑树，就是实现成本与范围查询的权衡。',
          ],
          followUps: [
            {
              question: '动态数组的扩容因子为什么常取 1.5 倍或 2 倍？放大缩小各有什么后果？',
              points: [
                '**均摊 O(1) 的前提是几何增长**：每次扩到 k 倍，n 次 push 的总搬运量是 n + n/k + n/k² + … ≤ n·k/(k-1)，总量 O(n)、均摊每次 O(1)；若每次只固定加常数（如 +10），总代价 O(n²)，push 退化。',
                '**k 的权衡**：k 越大搬运越少，但**内存浪费越多**（2 倍最多浪费一半、4 倍浪费 75%），且单次扩容的**停顿毛刺**更明显。工程折中：Java `ArrayList` 用 1.5 倍（`oldCapacity + (oldCapacity >> 1)`，释放的旧块尺寸更可能被后续请求复用）；Go slice 容量小于 256 时翻倍，之后约 1.25 倍渐进（Go 1.18+ 的口径，旧版阈值是 1024）——大数组放缓增速控制内存。',
                '边界延伸：很多实现**不主动缩容**，避免反复增删导致的扩缩抖动；能预估规模时**构造时指定初始容量**（`new ArrayList<>(n)`）直接消灭扩容，是标准优化——扩容毛刺与 HashMap 题里“rehash 不能写入”是同源问题，能串起来讲更显体系。',
              ],
            },
          ],
        },
        {
          id: 'cs-algo-hashmap',
          title: 'HashMap 的底层原理是什么？如何处理哈希冲突和扩容？',
          difficulty: 'basic',
          tags: ['HashMap', '哈希'],
          points: [
            '整体结构是**数组 + 链表/红黑树**（Java 8+）：写入时先对 key 的 hashCode 做扰动（高低位异或，让高位参与定位），再用 `(n - 1) & hash` 定位桶下标——容量取 2 的幂，位运算等价于取模。',
            '**冲突处理（拉链法）**：同一桶内用链表串起来；链表长度 ≥ 8 且数组容量 ≥ 64 时转**红黑树**，查询从 O(n) 降到 O(log n)；节点数回降到 6 时还原为链表，避免频繁结构切换。',
            '**扩容**：负载因子（元素数 / 容量）超过阈值（Java 默认 0.75）时容量翻倍 rehash；由于容量是 2 的幂，元素要么留在原下标、要么落到“原下标 + 旧容量”处。rehash 是一次性 O(n)，**期间不能写入**——这是 HashMap 性能毛刺和线程不安全问题的根源。',
            '线程安全替代：用 **ConcurrentHashMap**（CAS + synchronized 锁单个桶，锁粒度细）；反面教材是 Java 7 HashMap 多线程 resize 的**头插法死循环**。Go 的 map 不做并发保护，并发读写直接触发运行时 fatal error（不可 recover），`-race` 可检出。',
          ],
          followUps: [
            {
              question: '为什么负载因子默认是 0.75？',
              points: [
                '**空间与时间的折中点**：负载因子越大 → 冲突越多 → 链表越长查询退化；越小 → 数组越空 → rehash 越频繁、内存浪费。0.75 是经验值：泊松分布下默认参数时桶内节点数达到 8 的概率已低于千万分之一（Java 源码注释的推导），查询基本保持 O(1)，这也呼应了“链表长度 8 才树化”的阈值选择。',
                '**工程实感**：默认容量 16 × 0.75 = 12，放第 13 个元素就触发扩容；而扩容是一次性 O(n) 且**期间不能写**（HashMap 性能毛刺的根源）——所以能预估大小时**初始化指定容量**（如 Guava `Maps.newHashMapWithExpectedSize` 按 0.75 折算）是标准优化。',
                '横向对比体现“可调”：Go map 的扩容阈值是 6.5（8 槽 bmap 桶结构，口径不同）；Redis dict 正常扩容阈值 1.0；执行 BGSAVE/BGREWRITEAOF 期间提高到 5（dict_force_resize_ratio），避免 rehash 触发 COW 内存放大；负载因子低于 0.1 触发缩容。**各实现按自己的冲突代价选阈值**，0.75 不是数学常数而是工程权衡——这层对比是加分点。',
              ],
            },
            {
              question: 'HashMap 在多线程环境下会有什么问题？',
              points: [
                '**Java 7 经典事故：头插法成环**——两个线程同时 resize，头插迁移导致链表反向指针成环，后续 get 落入该桶 **CPU 100% 死循环**。Java 8 改尾插法成环消失，但并发 put 依然会**覆盖丢失**（两个线程同时写没有原子性保护）。',
                '**读写并发的问题面**：一个线程 put 触发扩容，另一个线程 get/遍历可能读到迁移到一半的结构（null、丢元素）甚至 ConcurrentModificationException。结论：**HashMap 的线程不安全不是某一种 bug，而是“没有任何同步设计”的必然结果**。',
                '**正确替代按场景选**：高并发读写 → **ConcurrentHashMap**（CAS + synchronized 锁单桶，读几乎无锁，size 是弱一致值）；复合操作要原子（不存在才放入）→ `putIfAbsent` / `computeIfAbsent`；低并发图省事 → `Collections.synchronizedMap`（全表一把锁，吞吐差）。',
                'Go 生态对照：Go map 并发读写直接 **fatal error（不可 recover，进程退出）**，官方故意让问题立刻暴露；正确做法是 `sync.RWMutex` 包一层或读多写少用 `sync.Map`。两个生态说的是同一件事：**并发容器要用专门的，而不是裸结构加运气**。',
              ],
            },
          ],
        },
        {
          id: 'cs-algo-stack-queue',
          title: '栈和队列的特性是什么？说说它们各自的典型应用。',
          difficulty: 'basic',
          tags: ['栈', '队列'],
          points: [
            '**栈（LIFO）**：push/pop 均 O(1)。典型应用：**函数调用栈**（递归的本质）、括号匹配、表达式求值与逆波兰式、浏览器前进后退、DFS 的显式栈实现、编辑器撤销。',
            '**队列（FIFO）**：入队出队均 O(1)。典型应用：BFS 的层序遍历、线程池与任务队列、**生产者-消费者**的缓冲区，也是消息中间件的思想原型。',
            '**单调栈**：栈内元素保持单调，一次遍历解决“下一个更大元素”“每日温度”“柱状图中最大矩形”类问题——本质是每个元素至多进栈出栈一次，把 O(n²) 的两两比较降为 O(n)。',
            '变体辨析：**双端队列**支持两端 O(1) 增删（滑动窗口最大值配单调队列）；**循环队列**用取模复用数组空间，判空判满要么留一个空位、要么额外记 size。',
          ],
          followUps: [
            {
              question: '用两个栈实现一个队列，怎么做到均摊 O(1)？',
              points: [
                '结构：**in 栈**只管入队（直接压栈）；出队时若 **out 栈**为空，把 in 栈**整体一次性倒入** out 栈再弹顶；out 栈非空则直接弹——元素在 out 栈中已是正序，保证出队顺序正确。',
                '**均摊 O(1) 的论证**：每个元素一生最多经历“压 in、倒入 out、弹出”三次 O(1) 操作，n 个元素总代价 O(n)；单次倒栈最坏 O(n)，但两次倒栈之间必然隔着至少 |out| 次廉价出队，代价被摊平——单次最坏与均摊的差别在这里体现得最清楚。',
                '边界与变体：倒的时机必须是“**out 空才倒**”，倒一半会破坏顺序；两个队列实现栈则不对称——每次 pop 要把前 n-1 个元素出队再重新入队，单次 O(n)，说明栈与队列互相模拟的成本并不对等，能点出这种不对称是加分项。',
              ],
            },
          ],
        },
        {
          id: 'cs-algo-complexity',
          title: '如何分析一段代码的时间复杂度和空间复杂度？',
          difficulty: 'basic',
          tags: ['复杂度', '分析'],
          points: [
            '复杂度描述的是**增长趋势**而非精确耗时：只保留最高阶项、忽略系数。分析时找**执行次数最多的核心语句**，看它随输入规模 n 如何增长。',
            '常见量级与来源对应：O(1) 哈希定位；**O(log n) 来自“每步把问题规模减半”**（二分、平衡树、跳表）；O(n) 单层遍历；O(n log n) 分治排序；O(n²) 双层嵌套；O(2ⁿ) 无记忆化的指数递归。',
            '递归用**递归式**分析：画递归树或套主定理，如 `T(n) = 2T(n/2) + O(n)` 展开后是 O(n log n)（归并排序）；递归的**调用栈深度也算空间复杂度**。',
            '三个易被追问的细节：区分**最好/最坏/均摊**（动态数组扩容是均摊 O(1)）；空间复杂度只算**额外辅助空间**；复杂度相同还要比常数与缓存友好度（数组遍历实测远快于链表）。',
          ],
          followUps: [
            {
              question: '主定理（Master Theorem）怎么用？什么情况下它不适用？',
              points: [
                '适用形如 **T(n) = a·T(n/b) + f(n)** 的分治递归式（a 个子问题、规模各 n/b、合并代价 f(n)），核心是比较**叶子总代价 n^(log_b a)** 与 **f(n)**：f(n) 多项式地更小 → T = Θ(n^(log_b a))；两者同阶 → 加一个 **log 因子**；f(n) 多项式地更大且满足正则条件 → T = Θ(f(n))。',
                '三个标准例子：二分查找 `T(n) = T(n/2) + O(1)`，a = 1、b = 2，n^(log₂1) = 1 与 f(n) = 1 同阶 → **O(log n)**；归并排序 `T(n) = 2T(n/2) + O(n)`，n^(log₂2) = n 与 f(n) = n 同阶 → **O(n log n)**；`T(n) = 2T(n/2) + O(n log n)` 属于“同阶但差一个 log”→ **O(n log² n)**。',
                '**不适用场景要能说出**：子问题规模不均匀（`T(n) = T(n-1) + O(n)`，快排最坏情形，只能画递归树得 O(n²)）、子问题重叠（DP 场景，不是分治）、f(n) 不满足正则条件——万能兜底是**画递归树逐层求和**，这比硬套公式更能体现理解。',
              ],
            },
          ],
        },
        {
          id: 'cs-algo-binary-search',
          title: '二分查找的模板和易错点是什么？有哪些常见变体？',
          difficulty: 'basic',
          tags: ['二分查找'],
          points: [
            '模板三要素：**先定义清楚搜索区间的循环不变量**（闭区间还是左闭右开）、边界移动条件、终止条件；选定一种写法就要一以贯之。',
            '```ts\n// 左闭右闭：返回第一个 >= target 的下标\nlet left = 0, right = nums.length - 1;\nwhile (left <= right) {\n  const mid = left + ((right - left) >> 1); // 防止 (left + right) 相加溢出\n  if (nums[mid] < target) left = mid + 1;\n  else right = mid - 1;\n}\nreturn left; // 越界说明不存在\n```',
            '易错点：`mid` 写成 `(left + right) / 2` 会**整型溢出**；死循环来自区间没有收缩（如 `left = mid` 却不 +1）；数组无序或含重复元素时直接套模板会出错。',
            '高频变体：**找第一个/最后一个等于 target**（左右边界）、**旋转有序数组**找最小值或目标（关键是判断哪半边有序）、**二分答案**（“最小的最大值 / 最大的最小值”类题，对答案区间二分 + 校验函数）。',
            '适用前提不是“有序”而是**二段性**：只要能找到一个性质，把区间确定性地分成“肯定不满足 / 可能满足”两半，就可以二分。',
          ],
          followUps: [
            {
              question: '旋转有序数组里怎么找目标值？核心判断是什么？',
              points: [
                '核心是**每一步先判断哪半边是有序的**：`nums[left] <= nums[mid]` 说明左半边有序（注意等号，剩一两个元素时也成立），否则右半边有序。旋转后的数组**至少有一半是有序的**，这就是二段性来源；有序的那半边可以精确判断 target 是否落在其区间内——在就进这半边，不在就去另一半。',
                '```ts\nfunction search(nums: number[], target: number): number {\n  let l = 0, r = nums.length - 1;\n  while (l <= r) {\n    const mid = l + ((r - l) >> 1);\n    if (nums[mid] === target) return mid;\n    if (nums[l] <= nums[mid]) {                    // 左半有序\n      if (nums[l] <= target && target < nums[mid]) r = mid - 1;\n      else l = mid + 1;\n    } else {                                       // 右半有序\n      if (nums[mid] < target && target <= nums[r]) l = mid + 1;\n      else r = mid - 1;\n    }\n  }\n  return -1;\n}\n```',
                '边界与变体：**找最小值**与 `nums[right]` 比较更稳（`nums[mid] > nums[right]` 说明最小值在右半）；若允许**重复元素**，`nums[mid] === nums[left]` 时无法判断哪半有序，只能 `left++` 收缩，最坏退化为 O(n)——能主动说出这个退化条件（LeetCode 81 的考点）是加分项。',
              ],
            },
          ],
        },
        {
          id: 'cs-algo-sort-compare',
          title: '常见排序算法的复杂度与稳定性如何对比？为什么工程上偏爱快排？',
          difficulty: 'basic',
          tags: ['排序', '稳定性'],
          points: [
            '一张对比表：**快排**平均 O(n log n)、最坏 O(n²)，不稳定、原地；**归并**稳定 O(n log n)，但要 O(n) 辅助空间；**堆排** O(n log n) 不稳定且缓存不友好；**插入排序** O(n²) 但**小规模或近有序数据上近乎线性**；**计数/基数/桶排序**可达 O(n)，但依赖数据范围，属于非比较排序（突破了比较排序 O(n log n) 的决策树下界）。',
            '**稳定性**指相等元素排序后相对顺序不变，意义在于**多关键字排序**（先按名字排，再按部门稳定排，部门内名字顺序保留）。快排、堆排不稳定的根源都是**远距离交换**。',
            '快排为什么快：原地、顺序访问**缓存友好**、常数小；最坏 O(n²) 用**随机选 pivot / 三数取中**规避，递归过深时切换堆排兜底（Introsort，即 C++ std::sort 的做法）；大量重复元素用**三路划分**。',
            '工程对应：Java 对基本类型用双轴快排（稳定无意义），对对象用 **TimSort**（归并 + 插入，稳定，还利用了真实数据常见的局部有序）——标准库的选择本身就是这道题的标准答案。',
          ],
          followUps: [
            {
              question: '快排怎么处理大量重复元素？（三路划分）',
              points: [
                '**二路快排在全等数组上退化**：每次 partition 只把一个 pivot 归位，“等于 pivot”的元素全部挤在同一侧，递归深度退到 O(n)、整体 O(n²)。根因：二路划分只区分“< pivot”与“≥ pivot”，没有给“等于”单独的空间。',
                '**三路划分**（荷兰国旗问题）：一次 partition 切成**小于 / 等于 / 大于 pivot** 三段，等于段直接就位不再递归，只递归两侧——全等数组一次 partition 结束，O(n)；大量重复时复杂度接近 O(n·k)（k 为不同元素个数）。',
                '代码直觉：\n```ts\n// lt: 小于段右界, gt: 大于段左界, i: 扫描指针\nwhile (i < gt) {\n  if (nums[i] < pivot) swap(nums, i++, lt++);\n  else if (nums[i] > pivot) swap(nums, i, --gt); // i 不动，换来的数再判\n  else i++;                                      // 等于段直接跳过\n}\n```',
                '工程呼应：Java 双轴快排、C++ Introsort 都融合了等值优化；这道题的更一般结论是“**让算法利用数据分布特征**”——重复多用三路、近有序用插入、大规模换堆排兜底，与主答案的标准库策略互相印证。',
              ],
            },
            {
              question: '为什么基于比较的排序下界是 O(n log n)？',
              points: [
                '**决策树论证**：任何比较排序的过程都能画成一棵二叉决策树——内部节点是一次比较，叶子是一种排列结果。n 个元素共 **n! 种排列**，正确排序必须能区分所有排列，所以决策树**至少 n! 个叶子**。',
                '**树高 = 最坏比较次数**：高度 h 的二叉树至多 2^h 个叶子，故 2^h ≥ n!，h ≥ log₂(n!)；由 Stirling 近似 log(n!) ≈ n·log n − 1.44n，得**最坏情况 Ω(n log n)**。这不是某个算法的缺陷，而是“只靠比较获取信息”的信息论上限——注意它约束的是最坏情况，快排平均 O(n log n) 与之相容。',
                '**突破下界的唯一出路是不比较**：计数排序（值为下标）、基数排序（按位分桶）、桶排序（值域分桶）达到 O(n) 量级，代价是依赖数据范围/分布且需要额外空间——这正是对比表里“非比较排序 O(n)”的来源，也与主答案的适用条件呼应。',
                '答题升华：比较排序已到 O(n log n)，优化空间只剩**常数与缓存**——这解释了快排实践更快（原地、顺序访问、缓存友好）和 TimSort 利用局部有序（galloping 模式）的原因。能讲出下界，说明知道“优化的尽头在哪”。',
              ],
            },
          ],
        },
        {
          id: 'cs-algo-binary-tree-traversal',
          title: '二叉树的遍历方式有哪些？迭代（非递归）实现的核心思路是什么？',
          difficulty: 'intermediate',
          tags: ['二叉树', '遍历'],
          points: [
            'DFS 三序（前/中/后序，区别只在根的访问时机）+ BFS 层序。递归实现平凡，面试常要求**迭代版**：前序用栈（根入栈，弹出访问后先压右再压左）；中序“左链一路入栈，弹出访问，转向右子树”；后序可用“前序变形（根右左）再整体反转”或记录上一次访问节点。',
            '```ts\n// 迭代中序遍历\nconst stack: TreeNode[] = [];\nconst res: number[] = [];\nlet cur = root;\nwhile (cur || stack.length) {\n  while (cur) { stack.push(cur); cur = cur.left; } // 左链入栈\n  cur = stack.pop()!;   // 弹出即访问\n  res.push(cur.val);\n  cur = cur.right;      // 转向右子树\n}\n```',
            '层序遍历用**队列**，按层记录 size 处理“每层一组”的变体；zigzag 层序对偶数层反转结果或用双端队列。',
            '**Morris 遍历**：利用前驱节点空闲的右指针临时指回自己，O(1) 空间完成中序/前序遍历，代价是临时改树结构（要恢复）——考察对树指针的深刻理解，属于高级加分项。',
            '遍历与应用的映射：中序遍历 **BST 得到升序序列**（验证 BST、第 k 小）；前序适合**序列化/复制树**；后序是**自底向上**（深度、直径、平衡性判断）。',
          ],
          followUps: [
            {
              question: '给前序和中序遍历结果，怎么重建二叉树？为什么前序加后序不行？',
              points: [
                '**分治三步**：前序第一个元素是**根** → 在中序里定位根，左边是**左子树的中序**、右边是**右子树的中序** → 按左子树长度切出两棵子树的前序，递归构建。用哈希表存“中序值 → 下标”避免每次线性查找，整体 O(n)。',
                '```ts\nfunction build(pre: number[], inorder: number[]): TreeNode | null {\n  const idx = new Map<number, number>();        // 中序值 -> 下标\n  inorder.forEach((v, i) => idx.set(v, i));\n  let preIdx = 0;\n  const dfs = (l: number, r: number): TreeNode | null => {\n    if (l > r) return null;\n    const root = new TreeNode(pre[preIdx++]);   // 前序当前位置即根\n    const m = idx.get(root.val)!;\n    root.left = dfs(l, m - 1);                  // 中序左段建左子树\n    root.right = dfs(m + 1, r);                 // 中序右段建右子树\n    return root;\n  };\n  return dfs(0, inorder.length - 1);\n}\n```',
                '为什么**前序 + 后序不能唯一重建**：只有一个孩子的节点无法判断它是左孩子还是右孩子——前序 [1,2]、后序 [2,1] 既可以是“1 只有左孩子 2”，也可以是“1 只有右孩子 2”。中序的不可替代性在于它把左右子树**分隔开**；特殊情形：BST 只用前序即可重建（用值域上下界代替中序分隔）。',
              ],
            },
            {
              question: '验证 BST 为什么不能只比较父子节点？正确做法有哪几种？',
              points: [
                '**反例**：`[10, 5, 15, 3, 12]`——12 是 15 的左孩子（12 < 15 通过父子比较），但 12 **大于根 10**，违反了“左子树整体小于根”的约束；BST 的量词是“整棵子树”，约束是向下传递的，父子局部检查覆盖不了。',
                '两种正确做法：① **上下界递归**——每个节点携带 (min, max) 区间，走左收窄上界、走右收窄下界，出界即假；② **中序遍历严格递增**——维护前驱值，出现 `cur <= prev` 立即返回假。两者都是 O(n)；边界：节点值可能取到整型极值，上下界要用可空哨兵或更大类型（Java 用 `Long`）。',
                '延伸：中序法顺带解决 **BST 第 k 小**（中序计数）与 **BST 转有序双向链表**；能从“定义里的量词（整棵子树）”推出为什么局部检查是错的，体现把数学定义翻译成算法约束的能力——这正是这道题想考的。',
              ],
            },
          ],
        },
        {
          id: 'cs-algo-heap-topk',
          title: '堆的结构和性质是什么？为什么求 TopK 要用小顶堆？',
          difficulty: 'intermediate',
          tags: ['堆', 'TopK', '优先队列'],
          points: [
            '堆 = **完全二叉树 + 数组存储**：下标 i 的左右孩子是 `2i+1`、`2i+2`，父节点是 `(i-1)/2`；大顶堆保证父 ≥ 子。插入/删除通过**上浮/下沉**维护堆序，O(log n)；**自底向上建堆是 O(n)**，不是 n log n。',
            '**求最大的 K 个数用大小为 K 的小顶堆**：堆中始终维护“当前最大的 K 个”，新元素比堆顶大才替换。内存只有 **O(K)**，能**流式处理**——数据量大到装不下内存、或数据流式到来时，这是唯一可行方案；求最小的 K 个对称地用大顶堆。',
            '方案对比要主动给出：全量排序 O(n log n) 且要求数据全部在内存；**快速选择（quickselect）**平均 O(n)、原地，适合一次性静态数据，但最坏 O(n²)（随机 pivot 缓解）且不稳定；堆方案 O(n log K)、内存可控，是工程默认解。',
            '堆在系统里的化身：**优先队列**（任务调度）、定时器（每次取最小到期时间）、合并 K 个有序链表（K 路归并 + 小顶堆，O(N log K)）——面试官爱追问“除了算法题，堆用在哪”，这些是标准弹药。',
          ],
          followUps: [
            {
              question: '为什么自底向上建堆是 O(n) 而不是 O(n log n)？',
              points: [
                '**关键在多数节点下沉距离很短**：从最后一个非叶子节点（下标 n/2 - 1）起逐个下沉。完全二叉树中**约一半是叶子**（不用动）、四分之一在倒数第二层（至多下沉 1 层）……自底向上第 h 层节点数约 n/2^(h+1)，每个至多下沉 h 层。',
                '**按层求和**：总代价 ≈ Σ (n/2^(h+1)) · O(h) = O(n) · Σ h/2^(h+1)。级数 Σ h/2^h 收敛于 2，总量是 **O(2n) = O(n)**。对比逐个插入建堆（n 次 × 上浮 O(log n) = O(n log n)）：插入是“少数元素走长路”，建堆下沉是“多数元素走短路”。',
                '加分延伸：这个“节点数 × 高度”的求和技巧在树算法分析里通用；它还解释了工程事实——**建堆便宜、维护堆贵**，所以堆适合“先建好、反复取最值”（TopK、优先队列、定时器），不适合频繁随机插入的场景。',
              ],
            },
            {
              question: '数据流式到来且内存有限时，TopK 具体怎么做？',
              points: [
                '**单机流程**：维护大小为 K 的**小顶堆**（只存 K 个元素与键）——堆未满直接入堆；堆满且新元素 > 堆顶则**弹堆顶、压新元素**（O(log K)），否则直接丢弃。任一时刻 TopK 就是堆内元素，内存 O(K)、单遍扫描。',
                '**为什么必须小顶堆**：堆顶是堆内最小值，即新元素的**准入门槛**——比门槛小的不可能进 TopK，O(1) 淘汰；门槛随数据流单调升高，被淘汰者不会后悔（这正是“流”的含义）。求最小的 K 个对称换大顶堆。',
                '**分布式扩展**：各分片节点维护本地 K 小顶堆（或哈希计数 + 局部 TopK），汇总节点合并 M 个分片的 M·K 个候选再取全局 TopK；分片按 key 哈希保证同一元素只在一个分片计数。',
                '**工程变体**：热词/URL 等字符串用“哈希表计数 + 堆”组合；超大数据用 Count-Min Sketch 近似计数（误差换内存）；Flink 里对应 window + TopN 算子。收尾对比：quickselect 要静态全量数据，堆方案 O(n log K) 才是流式唯一可行解——正好回应主答案的方案对比。',
              ],
            },
          ],
        },
        {
          id: 'cs-algo-two-pointer-window',
          title: '双指针和滑动窗口的适用条件是什么？给出通用模板。',
          difficulty: 'intermediate',
          tags: ['双指针', '滑动窗口'],
          points: [
            '双指针三种形态：**对撞指针**（有序数组两数之和、回文判断、接雨水）、**快慢指针**（链表判环——Floyd 判圈、找中点）、**同向指针**（原地删除/去重数组元素）。',
            '**滑动窗口 = 同向双指针 + 窗口内状态维护**：右指针扩张探索，约束被破坏时左指针收缩，核心不变量是**两个指针都只向前走**，因此整体 O(n)。',
            '```ts\n// 无重复字符的最长子串（滑动窗口模板）\nlet left = 0, ans = 0;\nconst count = new Map<string, number>();\nfor (let right = 0; right < s.length; right++) {\n  count.set(s[right], (count.get(s[right]) ?? 0) + 1);\n  while (count.get(s[right])! > 1) {        // 约束被破坏：收缩左边界\n    count.set(s[left], count.get(s[left])! - 1);\n    left++;\n  }\n  ans = Math.max(ans, right - left + 1);    // 窗口合法，更新答案\n}\n```',
            '适用条件（判断能不能用）：问题关于**连续子数组/子串**，且窗口扩大或缩小时目标函数**单调变化**（如窗口和随窗口单调）。若窗口内还需要最大值/最小值，哈希表维护不了，要叠加**单调队列**。',
            '高频题型对照：最小覆盖子串（计数表 + valid 计数器）、长度最小的子数组（和的单调性）、滑动窗口最大值（单调队列存下标）。',
          ],
          followUps: [
            {
              question: '滑动窗口内的最大值为什么必须用单调队列？它怎么做到整体 O(n)？',
              points: [
                '**普通窗口状态维护不了最值**：计数表能 O(1) 增减但只适合“存在性/数量”约束；每步重算最大值是 O(k)；用堆又删不掉过期元素（延迟删除会让堆膨胀）。**单调队列（存下标、值单调递减）**两全：队头永远是当前窗口最大值，过期元素也能 O(1) 清理。',
                '维护规则：右指针入队时**从队尾弹出所有 ≤ 新元素的下标**——这些元素比新元素早出现又更小，窗口右移后永远不可能再成为最大值，直接淘汰；左指针推进时只在**队头下标滑出窗口**时弹队头。“又大又新者淘汰又小又旧者”就是单调性的来源。',
                '复杂度论证：每个下标**至多入队一次、出队一次**，均摊 O(1)、整体 O(n)——与单调栈“把两两比较降到 O(n)”是同一个均摊原理。凡窗口内要最值的题（最大值、最小值、最值差 ≤ limit）都套这个结构，说出这个模型迁移是加分项。',
              ],
            },
            {
              question: '快慢指针为什么在带环链表里一定相遇？怎么找到环的入口？',
              points: [
                '**必相遇的论证**：两针进入环后，快针每轮比慢针多走 1 步，**相对距离每轮缩小 1**——距离是有限非负整数，必然先减到 0（相遇）而不是跳过；“跳过”意味着一轮缩小 ≥ 2，与相对速度 1 矛盾。这是 Floyd 判圈正确性的根基。',
                '**找环入口的推导**：设头到入口距离 a，入口到相遇点距离 b，环长 c。相遇时慢针走了 a + b，快针走了 a + b + n·c，且路程是慢针两倍：`a + b + n·c = 2(a + b)`，得 **a = n·c - b**——“从头部走到入口”与“从相遇点继续走到入口”步数相同。实现：相遇后把一针放回头部，两针**同速**前进，再次相遇处即入口。',
                '边界与延伸：判空前依次检查 `head`、`head.next`；快针走两步所以 `fast.next` 也要判空。同一套“隐式链表 + 相对速度”模型还能解决快乐数、找重复数——能迁移说明理解的是模型而不是套路。',
              ],
            },
          ],
        },
        {
          id: 'cs-algo-dp',
          title: '动态规划的核心思想是什么？说说你的解题步骤。',
          difficulty: 'intermediate',
          tags: ['动态规划', 'DP'],
          points: [
            'DP 的本质：问题具有**最优子结构**（最优解由子问题最优解构成）与**重叠子问题**（递归会重复求解），于是**把每个子问题只算一次并记录结果**。与贪心的区别：贪心每步做出局部选择不回溯，且需要证明贪心选择性成立，DP 则枚举所有子问题保证全局最优。',
            '解题四步：**定义状态**（dp[i] 的准确含义，最关键的一步）、**推导转移方程**（dp[i] 如何由更小状态得到）、**初始化与边界**、**确定遍历顺序**（保证计算 dp[i] 时依赖项已就绪）。',
            '```text\n// 爬楼梯：dp[i] = 到达第 i 阶的方法数\n// 转移：最后一步跨 1 阶或 2 阶 → dp[i] = dp[i-1] + dp[i-2]\n// 边界：dp[0] = 1, dp[1] = 1\n// 优化：只依赖前两项，滚动变量把空间从 O(n) 压到 O(1)\n```',
            '套路分类对号入座：**线性 DP**（打家劫舍、最长递增子序列）、**双序列**（编辑距离、最长公共子序列，二维 dp 表）、**背包**（0-1 背包容量倒序遍历、完全背包正序）、**区间 DP**、**状态机 DP**（股票买卖系列的“持有/不持有”两状态）。',
            '优化方向：**滚动数组**把二维压一维；**记忆化搜索**（自顶向下）与递推（自底向上）等价，面试先把状态定义讲清楚，再谈优化。',
          ],
          followUps: [
            {
              question: '贪心和 DP 的区别是什么？什么情况下贪心会失效？',
              points: [
                '**本质区别是“选择范围”**：DP 枚举所有子问题的解并取最优，靠最优子结构合并；贪心每步只做**局部最优选择且永不回溯**——把搜索空间砍成一条路径，更快（通常 O(n log n) 内），但要求问题满足**贪心选择性质**（局部最优能拼出全局最优）。',
                '**贪心失效的经典反例**：面额 [1, 3, 4] 凑 6 元——贪心取 4+1+1（3 枚），最优是 3+3（2 枚）。共同特征是**面额不成倍数**，局部选择破坏了后续组合；而标准币制（1/5/10/50）贪心恰好成立——反例要能随手写出，这是本题的必背弹药。',
                '**判断标准**：能给出**可证明的选择准则**（区间调度按右端点排序、Huffman 每次合并最小两堆）才用贪心；证不出来就 DP。面试中主动说“我先试贪心，但准则无法证明，改 DP 保正确性”比闷头写 DP 更体现判断力。',
                '统一视角收尾：记忆化搜索 = 暴力递归 + 缓存，贪心 = 递归树里只走“看起来最好”的一条分支；两者的性能差距来自**被放弃的分支是否真的不可能最优**。',
              ],
            },
            {
              question: '0-1 背包为什么容量要倒序遍历？',
              points: [
                '**正序会让同一物品被重复选取**：一维转移 `dp[j] = max(dp[j], dp[j-w] + v)` 中，j 从小到大时 dp[j-w] 是**本轮刚更新过**的值——等于装了本物品又再装一次，那是**完全背包**的语义。倒序保证 dp[j-w] 还是**上一轮（未考虑本物品）**的值，每件物品至多选一次。',
                '对照二维写法理解：`dp[i][j] = max(dp[i-1][j], dp[i-1][j-w] + v)` 显式依赖上一行；压维后“倒序”就是在模拟“读上一行、写本行”。理解了这一点，**滚动数组压维的合法性**是推导出来的，不用背口诀。',
                '对称记忆与边界：**完全背包正序**（恰好要利用本轮更新过的值实现重复选取）；多重背包二进制拆分或单调队列优化；“恰好装满”初始化 dp[0]=0 其余 -∞（非法态），“不超过容量”全初始化 0——初始化也是高频追问点。',
              ],
            },
          ],
        },
        {
          id: 'cs-algo-graph-bfs-dfs',
          title: '图的存储方式有哪些？BFS 和 DFS 怎么实现、各适合什么问题？',
          difficulty: 'intermediate',
          tags: ['图', 'BFS', 'DFS'],
          points: [
            '两种存储：**邻接矩阵** O(V²) 空间、查边 O(1)，适合稠密图；**邻接表** O(V+E)，适合稀疏图（工程默认），无向图要存两条有向边。',
            '**BFS**：队列 + visited，按层扩散，天然求出**无权图最短路径**（单词接龙、打开转盘锁）；**多源 BFS** 把所有起点同时入队，直接得到“每个点到最近源的距离”（01 矩阵、腐烂橘子）。',
            '**DFS**：递归或显式栈，深入到底；适合**连通性、路径搜索、拓扑排序**（Kahn 算法基于入度的 BFS，或 DFS 后序反转）与**环检测**（三色标记：白未访问、灰在栈中、黑已完成，访问到灰色节点即有环，课程表问题）。',
            '复杂度都是 O(V+E)；工程注意两点：大图递归会**爆栈**（改显式栈）；**visited 要在入队/入栈时标记**而不是出队时，否则同一节点重复入队导致超时。',
          ],
          followUps: [
            {
              question: 'Dijkstra 为什么不能处理负权边？Bellman-Ford 为什么可以？',
              points: [
                '**Dijkstra 的贪心假设被负权边打破**：它认定节点出队（取最小 dist）时最短距离已确定、不再更新；但负权边可能让“晚确定的节点”绕出更短路径反馈回来——此时该节点已被标记完成，无法修正。最小反例：`A→B` 权 1、`A→C` 权 2、`C→B` 权 -2——Dijkstra 先确定 B = 1，实际应为 0。',
                '**Bellman-Ford 的做法是全局松弛**：对所有边松弛 n-1 轮（最短路径至多 n-1 条边，n-1 轮必收敛），不做任何“已确定”假设，所以负权边不影响正确性；第 n 轮仍有边可松弛则说明存在**负环**——顺带完成负环检测。代价是 O(V·E)，比 Dijkstra + 堆的 O((V+E)·log V) 慢得多；SPFA 是它的队列优化，平均快、最坏仍退化到 O(V·E)。',
                '落点表达：多数工程场景（时间、距离、成本）权重非负，Dijkstra/A* 是默认；出现“补贴、返现”类负权语义才需要 Bellman-Ford——**先问数据特征再选算法**，与选数据结构是同一种思维。',
              ],
            },
            {
              question: '什么是双向 BFS？什么条件下值得用？',
              points: [
                '**从起点和终点同时扩散**：两侧各维护一层队列交替扩展，某侧要扩展的节点已出现在另一侧的访问集合中即相遇，拼接两侧路径就是最短路径。搜索空间从 **b^d**（b 为分支因子、d 为深度）降到约 **2·b^(d/2)**——d = 8、b = 10 时从 1 亿量级降到 2 万量级，这是单词接龙、打开转盘锁的标准优化。',
                '**适用条件要能说清**：① 终点**已知且有限**（有向图要能沿反边扩展——建反图或遍历时取反向邻接）；② 只求最短距离/路径，不要求遍历全集；③ 分支因子大、深度深时收益最大。终点未知或动态变化的场景不适用。',
                '实现细节：**交替扩展节点数较少的一侧**（两侧深度均衡才吃得到 b^(d/2) 的收益）；相遇判定要在扩展时检查对面 visited，而不是等入队后，否则多扩一层；两侧各自维护 visited 与父指针用于回溯拼接。',
              ],
            },
          ],
        },
        {
          id: 'cs-algo-lru',
          title: '手写一个 LRU 缓存，你会用什么数据结构？时间复杂度是多少？',
          difficulty: 'advanced',
          tags: ['LRU', '设计题', '缓存'],
          points: [
            '要求 get/put 均 **O(1)** → **哈希表 + 双向链表**：哈希表 O(1) 定位节点，双向链表 O(1) 完成摘除与头插——必须是**双向**的，单链表删除节点要先找前驱，就退化了。',
            '```ts\n// Node 含 key/value/prev/next；map: Map<key, Node>\nconst node = map.get(key)!;\nremove(node);          // 从链表摘除\npushFront(node);       // 头插 = 标记为最近使用\nif (map.size > capacity) {\n  const tail = dummy.prev!;\n  remove(tail);\n  map.delete(tail.key); // 哈希表与链表必须同步删除\n}\n```',
            '实现细节决定对错：用**虚拟头尾节点（哨兵）**消除边界判断；get 命中也要把节点移到头部（被访问即最近使用）；节点的 key 要存进节点里，淘汰尾部时才能反查哈希表删除。',
            '工程对应：Java 的 `LinkedHashMap(accessOrder=true)` 重写 `removeEldestEntry` 三行实现；Redis 用的是**近似 LRU**（随机采样淘汰，省去全局链表的内存），并在 4.0 后提供 **LFU** 解决“偶发的批量扫描把真热点挤出去”的缓存污染问题。',
            '追问预演：并发下怎么做（分桶加锁，或 Caffeine 的异步 buffer 思路）；LRU 的缺陷与替代（LFU、W-TinyLFU）。',
          ],
          followUps: [
            {
              question: '为什么必须用双向链表，单向链表不行吗？',
              points: [
                '**决定性操作是 O(1) 删除任意节点**：命中与淘汰都要摘除链中节点，双向链表节点自带 prev，配合哈希表直达节点后 O(1) 摘除；单链表摘除需要**前驱**，要么 O(n) 回头找，要么额外维护“key → 前驱”的第二张表（删除后还要回改前驱指针，复杂且易错）。',
                '逐操作检视：**get 命中**要把节点移到头部 = 摘除（需要 prev）+ 头插，两步都 O(1)；**淘汰尾部**要拿到尾节点的前驱做新的尾，双向链表 `tail.prev` 直接可得，单链表取倒数第二个节点同样 O(n)。两条高频路径都被 prev 卡死——这就是“必须双向”的完整论证。',
                '对照真实实现：Java `LinkedHashMap(accessOrder=true)` 内部就是哈希表 + **双向**链表；面试官若追问“能否省内存”，可答存在单链表 + 数组模拟等简化实现，但都牺牲 O(1)——**这是由操作需求倒逼数据结构设计的题**，结论不是背出来的。',
              ],
            },
            {
              question: 'Redis 为什么用近似 LRU？LFU 解决了 LRU 的什么问题？',
              points: [
                '**精确 LRU 的代价是全局双向链表**：每次访问都要移动节点（改 4 个指针），高并发下链表成为**锁竞争热点**，每节点还多 2 个指针的内存。Redis 的近似 LRU：每 key 只存 **24 bit 空闲时间戳**，淘汰时**随机采样 N 个（默认 5）**、淘汰其中最久未访问的——访问路径 O(1)、无全局结构，采样数越大越接近真 LRU。',
                '**LRU 的结构性缺陷：缓存污染**——偶发批量扫描（全表遍历、爬虫、离线任务）把真热点瞬间挤出，扫描结束后缓存里全是不会再访问的一次性 key，命中率断崖下跌。根因：LRU 只看“最近”不看“频率”，把“刚出现”误当“重要”。',
                '**LFU 的修正**：Redis 4.0 引入，按访问**频率**淘汰——每 key 存对数近似计数器（8 bit 表示大访问次数，Morris 计数思想）+ 周期衰减（`lfu-decay-time`），批量扫描的 key 频率只有 1，挤不掉长期热点；对数计数避免溢出与内存膨胀。',
                '对比升华：近似 LRU 是**用统计近似换实现复杂度**，LFU 是**引入新维度换命中率**；Caffeine 采用的 W-TinyLFU 再加“准入窗口 + Count-Min Sketch 频率估计”，同时抗扫描污染与时间局部性漂移。能讲出这条演进线，说明理解的是淘汰策略的设计权衡，而不只是背 Redis 配置。',
              ],
            },
          ],
        },
        {
          id: 'cs-algo-backtracking',
          title: '回溯算法的解题框架是什么？怎么做剪枝和去重？',
          difficulty: 'intermediate',
          tags: ['回溯', '递归', '剪枝'],
          points: [
            '回溯 = **决策树上的系统性枚举 + 状态恢复**。模板三段：做选择 → 递归下一层 → **撤销选择**；路径（已做选择）+ 选择列表 + 终止条件是三要素，撤销保证回到分叉点时状态干净。',
            '三类高频模板：**排列**（每层用 `used[]` 排除已选元素）、**组合/子集**（`start` 下标只向后选，天然防 [1,2]/[2,1] 重复；子集在进入每个节点时收集，组合在叶子收集）、**棋盘/切割**（N 皇后、单词搜索，按位置逐层决策）。',
            '复杂度按**决策树节点数**估：子集 2^n、排列 n!、组合 C(n,k)——回溯没有统一闭式，说清解空间大小比背符号重要。',
            '剪枝四板斧：**可行性剪枝**（部分解已非法立即返回）、**最优性剪枝**（已劣于当前最优则放弃）、**排序预处理**（让提前终止与同层去重成为可能）、**剩余不足直接返回**（还差 k 个但剩余元素不够）。',
            '与 DP 的分界：要“所有方案”→ 回溯；只问最优值/方案数且子问题重叠 → DP。两者都是决策树，区别只在子问题是否重复出现。',
          ],
          followUps: [
            {
              question: '输入含重复元素时怎么去重？树层去重和树枝去重是什么？',
              points: [
                '同层去重：排序后 `i > start && nums[i] === nums[i-1]` 则 continue，同层同值只探索一次。',
                '排列没有 start，用 `nums[i] === nums[i-1] && !used[i-1]`（used[i-1] 为 false 说明前一同值元素已回溯，是同层兄弟）。',
                '统一视角：去重剪的是**决策树同层兄弟节点**，不是路径上的元素（否则 [1,1,2] 这类合法解也被剪掉）。',
              ],
            },
            {
              question: 'N 皇后怎么做 O(1) 冲突检查？',
              points: [
                '逐行放置消掉行冲突后剩三类：列、主对角线（row−col 为定值）、副对角线（row+col 为定值），各用一个布尔数组标记，检查与回退 O(1)；对角线共 2n−1 条，row−col 加 n−1 做偏移。',
                '同款手法适用于数独、单词搜索。',
              ],
            },
          ],
        },
      ],
    },
    {
      id: 'cs-db',
      name: '数据库基础',
      description: '通识概念层：事务与 ACID、连接池、NoSQL 引擎特性与 CAP 选型——MySQL 索引/事务/MVCC 的实战细节见后端方向。',
      references: [
        { label: 'MySQL 8.0 Reference Manual', url: 'https://dev.mysql.com/doc/refman/8.0/en/' },
        { label: 'MySQL · InnoDB 概述', url: 'https://dev.mysql.com/doc/refman/8.0/en/innodb-introduction.html' },
        { label: 'Redis 官方文档', url: 'https://redis.io/docs/latest/' },
        { label: 'PostgreSQL 文档', url: 'https://www.postgresql.org/docs/' },
      ],
      questions: [
        {
          id: 'cs-db-relational-vs-nosql',
          title: '关系型数据库和非关系型数据库的区别是什么？如何选型？',
          difficulty: 'basic',
          tags: ['数据库选型', 'NoSQL'],
          points: [
            '关系型（MySQL/PostgreSQL）：**Schema 明确、SQL 与 JOIN 表达力强、事务（ACID）完备**，数据关联紧密、一致性要求高的场景首选；代价是**水平扩展复杂**（分库分表牵动业务）、海量写入性能受限。',
            '非关系型按模型分四类：**KV（Redis）**内存级毫秒以下延迟；**文档（MongoDB）**JSON 模型、Schema 灵活、嵌套免 JOIN；**列族（HBase/Cassandra）**海量写入、宽表扫描；**图（Neo4j）**多跳关系遍历。共同取舍是：**弱化跨行跨库事务与复杂查询，换扩展性与可用性**。',
            '选型口诀：交易与复杂查询 → 关系型；缓存/计数/会话 → Redis；字段多变的半结构化数据 → MongoDB；亿级写入、按 rowkey 访问 → HBase/Cassandra。主流架构通常是**组合拳**：MySQL 保核心交易 + Redis 挡读流量 + ES 承担搜索。',
            '面试表达：不要说“谁取代谁”，要说**数据特征决定存储模型**——一致性要求、读写模式（点查/范围/聚合）、数据量与增速、Schema 演变频率，四个维度做决策。',
          ],
          followUps: [
            {
              question: 'MySQL 单表数据量太大时有哪些扩展路径？各自代价是什么？',
              points: [
                '**演进顺序**：先索引优化与**冷热分离/归档**（多数“撑不住”是没用好索引）→ **读写分离**（主从复制，读多写少收益最大，代价是**复制延迟**——写后立即读可能读不到，要按业务读主或会话粘住主库）→ **分库分表** → 换 **NewSQL**（TiDB/OceanBase：兼容 MySQL 协议、自动分片 + 分布式事务）。',
                '**分库分表的代价清单**：跨库 JOIN 消失（冗余字段、异构索引或应用层组装）；跨库事务变分布式事务（能避免就避免，改最终一致）；**全局唯一 ID** 要换雪花/号段发号；分片键之外的条件查询要广播（配 ES 或异构一份）；扩容要做数据迁移（**翻倍扩容**或一致性哈希降低迁移量）。',
                '选分片键的原则：让**最高频的查询路径**带上分片键（订单表按 user_id 拆，而不是按订单号），宁可冗余数据也别广播查询；表达时主动说“能不分就不分”——分表是把数据库的复杂度转移到应用层，是最后手段。',
              ],
            },
          ],
        },
        {
          id: 'cs-db-acid',
          title: '事务是什么？ACID 四个特性分别约束什么？',
          difficulty: 'basic',
          tags: ['事务', 'ACID'],
          points: [
            '**事务**：把一组数据库操作打包成一个**不可分割的执行单元**——要么全部生效，要么全部不生效。它解决的问题是：多条语句执行中途失败（进程崩溃、并发穿插）时，数据不会停在"半完成"的中间态。',
            '**原子性（Atomicity）**：全部成功或全部不做，不存在中间态；**一致性（Consistency）**：事务前后数据满足所有业务约束（转账双方总额不变、库存不为负）。',
            '**隔离性（Isolation）**：并发事务互不干扰——完全隔离的代价是串行化，所以数据库用隔离级别在"干扰程度"与"并发度"之间分级；**持久性（Durability）**：提交即落盘生效，宕机不丢。',
            '理解层次：**A、I、D 是机制，C 是目的**——数据库提供前三者的机制，一致性还要靠应用层约束（唯一索引、校验、对账）共同达成；具体到 InnoDB 的实现（undo/redo log、MVCC、隔离级别的细节）属于实战考点，见"后端开发 → MySQL"领域。',
          ],
          followUps: [
            {
              question: '原子性和一致性有什么区别？A、I、D 都做到了就一定有 C 吗？',
              points: [
                '**原子性是执行语义，一致性是数据状态**：A 只保证"这组操作全做或全不做"，不关心做的是什么；C 要求事务前后数据满足**所有业务约束**（总额守恒、唯一性、外键）。A 是手段之一，C 是目的——这个概念关系是本题的题眼。',
                '反例最有说服力：转账事务完整提交了（A、I、D 全达标），但业务代码把收款人写错了、或应用层漏了"余额非负"约束——数据**一致地错着**。所以 **C = AID（数据库机制）+ 应用层约束**（唯一索引、校验逻辑、对账），把 C 全归功于数据库是最常见的理解偏差。',
                '落点延伸：隔离级别的选择也影响 C 的业务感受——RR 下同一事务内两次读一致，RC 下可能读到变化值，依赖可重复读的业务逻辑在 RC 下会算错账。答这道题的本质是区分**数据库提供机制**与**业务定义正确**。',
              ],
            },
            {
              question: '转账操作为什么要放在一个事务里？不这么做会有什么具体后果？',
              points: [
                '拆开后中间态会暴露：扣款成功、入账失败（或进程恰好在两条语句之间崩溃），钱凭空消失——数据库对**单条语句**天然原子，跨多条语句的原子性必须靠事务边界显式声明。',
                '进阶认知：事务边界不是越大越好——事务里混入 RPC、文件 IO 等慢操作会长时间持锁、放大故障面（"大事务"是线上性能问题与事故的惯犯）；正确做法是事务只包住数据库操作，外部副作用用补偿、重试与对账机制兜底。',
              ],
            },
          ],
        },
        {
          id: 'cs-db-connection-pool',
          title: '数据库连接池的原理是什么？为什么需要连接池？',
          difficulty: 'basic',
          tags: ['连接池', '数据库'],
          points: [
            '建立一条 MySQL 连接要经历 **TCP 三次握手、（可选）TLS、认证授权、分配会话内存**，数十毫秒级；连接池通过**复用连接**把成本摊薄到近零，同时充当**并发闸门**——连接数就是数据库的并发上限，防止应用把 DB 打垮。',
            '核心参数：**最大/最小连接数**——不是越大越好，经验公式 `cores * 2 + 有效磁盘数`（HikariCP 建议），连接过多只会加剧上下文切换与锁竞争；**空闲超时**要小于 MySQL 的 `wait_timeout`，避免拿到已被服务端断开的失效连接；**获取超时**要 fail-fast，而不是无限等待拖垮上游。',
            '典型故障模式：**连接泄漏**（借出不还导致池耗尽，必须 try-with-resources/finally 归还）、慢 SQL 长期占用连接、发布重启时连接未优雅关闭、DB 网络抖动后的死连接。',
            '排查与监控：盯**活跃连接数、等待线程数、获取连接耗时**（HikariCP 等 pool 都有 metrics）；大量 “connection is not available” 报错时，先查慢 SQL 和泄漏，再考虑扩池子。',
          ],
          followUps: [
            {
              question: '连接池被打满时系统会表现成什么样？怎么定位？',
              points: [
                '**现象链**：应用报获取连接超时（HikariCP 的 `Connection is not available, request timed out`）→ 上游线程阻塞在等连接，Web 容器线程池被拖满 → 整个服务**假死**（连不碰数据库的接口也因无线程可用而超时）——连接池问题常被误判为“整个服务挂了”。',
                '**定位路径**：先看池 metrics（**active / idle / waiting** 三个数：active 打满且 waiting 持续增长就是真打满）→ 到数据库侧 `SHOW PROCESSLIST` 看这些连接在干什么——典型元凶是**慢 SQL、长事务、连接泄漏**（Sleep 状态大量堆积）；再核对 `maxLifetime` 是否小于 MySQL `wait_timeout`，避免拿到被服务端断开的**死连接**。',
                '**治理顺序**：先消灭慢 SQL 和泄漏（治本）→ 获取超时保持 fail-fast 并给上游降级（快速失败好过集体超时）→ 最后才是调池子；同时做**容量规划**：所有实例的 maxPoolSize 之和必须小于 DB 的 `max_connections`，否则发布扩容时会撞数据库侧上限。',
              ],
            },
          ],
        },
        {
          id: 'cs-db-nosql-categories',
          title: 'NoSQL 有哪些类型？Redis、MongoDB、HBase 分别适合什么场景？',
          difficulty: 'intermediate',
          tags: ['NoSQL', 'Redis', 'MongoDB', 'HBase'],
          points: [
            '**KV 型（Redis/Memcached）**：内存 KV，微秒/毫秒级延迟；适合缓存、计数器、排行榜（zset）、分布式锁、会话；限制是内存成本与单值大小。',
            '**文档型（MongoDB）**：BSON 文档模型，Schema 灵活、字段可随时增减，支持二级索引和相当完善的查询语言；适合内容管理、用户画像、商品详情等**字段多变、以聚合文档为单位读写**的数据。',
            '**列族型（HBase/Cassandra）**：基于 **LSM-Tree**（先写内存表 + 顺序日志，后台合并成 SSTable），把随机写变顺序写，**写入吞吐极高**；适合日志、消息存档、监控时序这类海量写、按 rowkey/主键范围读的宽表；弱点是随机更新与复杂查询。**图数据库（Neo4j）**：原生存边，社交、风控、知识图谱的多跳查询远快于关系型 JOIN。',
            '底层差异决定性能形态：Redis 是**内存 + 事件循环**换极致延迟；HBase/Cassandra 是 **LSM-Tree** 换写入吞吐（读放大是代价）；MongoDB 是文档模型 + B 树引擎换灵活性。**理解存储引擎，才能预判它擅长和不擅长什么**——这是 NoSQL 选型题的高分逻辑。',
          ],
          followUps: [
            {
              question: 'LSM-Tree 为什么写入快、读却慢？读放大和写放大是怎么来的？',
              points: [
                '**写路径全顺序化**：写入先追加 **WAL**、进内存表（memtable），写满后冻结刷成**不可变的 SSTable**，后台再分层合并（Compaction）——全程顺序 IO，没有随机写与页分裂，这是写入吞吐远超 B+ 树的根本原因。',
                '**读路径要翻多层**：一个 key 可能存在于 memtable 和任意一层 SSTable，必须**从新到旧逐层查找**，靠每层 **Bloom Filter** 与稀疏索引快速排除，最坏多层探测——这就是**读放大**；Compaction 反复重写同一 key 的多个版本，是**写放大**的来源。读、写、空间三者的放大互相牵制，Compaction 策略（leveled / size-tiered）就是在调这个三角。',
                '结论表达：**写多读少、按 key 批量扫**的场景 LSM 完胜（日志、时序、消息存档）；**点查与更新频繁、读延迟敏感**的场景 B+ 树更稳（交易）。Bloom Filter 参数、分区数、Compaction 限速都是调优旋钮——能把引擎特性与业务读写形态对上，就是选型题的高分答案。',
              ],
            },
            {
              question: 'Redis 单线程为什么还能这么快？6.0 引入多线程改了什么？',
              points: [
                '**快在四件事**：纯**内存操作**（微秒级，常比网络 RTT 还小）；**IO 多路复用**（epoll 让单线程管理数万连接）；**单线程无锁**——没有上下文切换与竞态，`INCR`、`SETNX` 天然原子；高效数据结构（SDS、跳表、紧凑编码）。瓶颈从来不是 CPU，是**内存容量与网络带宽**。',
                '**6.0 的多线程只并行化网络 IO**：读写 socket、解析协议由多线程完成，**命令执行仍是单线程**——保住“单命令原子”的语义不被破坏；解决的是高并发下 syscall 占大头的网络 IO 瓶颈，不是计算瓶颈。',
                '边界红线要主动说：单线程意味着**任何慢操作都阻塞所有请求**——`KEYS *`、百万级大 key 删除、超大 zset 全量操作都是事故来源；大 key 删除要用 `UNLINK`（异步释放）或渐进式清理。能把“单线程”讲成性能优势与故障红线的一体两面，才算理解到位。',
              ],
            },
          ],
        },
        {
          id: 'cs-db-cap-selection',
          title: 'CAP 定理是什么？它在数据库选型中如何体现？',
          difficulty: 'intermediate',
          tags: ['CAP', '一致性', '分布式'],
          points: [
            '**CAP**：当网络**分区（P）**发生时，**一致性（C）与可用性（A）只能二选一**。关键理解：P 不是可选项而是常态（网络必然可能故障），所以真正的决策是“**分区发生时保 C 还是保 A**”，网络正常时 C 和 A 可以兼得。',
            '落地对应：**CP 系统**——etcd/ZooKeeper/HBase：分区时拒绝写入保数据一致（选主期间不可用），适合分布式锁、配置、元数据；**AP 系统**——Cassandra/DynamoDB/Eureka：分区时继续服务，返回可能过期的数据，靠**最终一致性**收敛，适合社交、Feed 类高可用场景。',
            '两个常见误用要主动纠正：① 单机数据库谈不上 CAP 权衡（没有分区问题）；② CAP 只约束**分布式复制**场景，不等于“分布式数据库永远三选二”。**BASE**（基本可用、软状态、最终一致）是 AP 路线在工程上的展开。',
            '选型实践：**交易、库存、账户**宁可短暂不可用也不能错账 → CP 倾向；**点赞数、浏览量、Feed**容忍秒级不一致换可用 → AP 倾向；跨系统的一致性多数用**最终一致方案**（消息队列 + 重试 + 对账）兜底，而不是动辄上分布式事务。',
          ],
          followUps: [
            {
              question: '为什么共识协议都要求多数派（quorum）？Raft 在分区下具体怎么表现？',
              points: [
                '**quorum 的本质是集合交集**：写入必须获得 **W > N/2** 个节点确认（N = 5 时写 3 个）——任意两个多数派集合必然相交，系统在不知道是否分区的情况下也能保证**全局最多只有一个有效版本**；没有多数派约束，两个分区的 Leader 各写各的，数据就分叉了。',
                '**Raft 分区下的行为**：少数派分区的候选者拿不到**多数派投票**，永远选不出 Leader；多数派一侧正常选主、正常提交——结果就是**少数派侧写入不可用、但数据不错乱**，这正是 CAP 里 CP 的具体机制。旧 Leader 靠**任期的单调性**压制：多数派侧选出更高 term 后，旧 Leader 收到更高 term 的消息自动退位，不会脑裂出两个有效 Leader。',
                '代价与恢复：N = 5 挂 2 就失去写入能力——**可用性上限被多数派锁死**，这是 CP 用可用性换一致性的明码标价；分区恢复后少数派回滚未提交日志、从新 Leader 追平。能讲完这条恢复路径，说明共识协议不是背名词。',
              ],
            },
            {
              question: '读己之写和单调读一致性怎么实现？',
              points: [
                '**读己之写（Read-your-writes）**：用户改完要立刻看到自己的修改。实现：① **会话粘主**——写后的一段时间把该用户的读路由到主库；② **版本号校验**——前端记录最后写入的时间戳/版本，读请求带上，副本数据落后就换主库或短暂等待；③ 关键写入直接读写主。',
                '**单调读（Monotonic Reads）**：保证不会先读到新值、再读到更旧的值（异步复制下两次刷新命中不同从库就会发生）。实现：同一用户**固定路由到同一副本**（按 user_id 哈希选从库、会话粘性），或副本校验版本不满足就等待追平。',
                '工程落点：这两者属于**会话保证**——只在单个用户视角内成立，不需要全局共识，代价远小于强一致；配合**主从延迟监控**（replication lag 指标告警）决定哪些读可以下放从库。这正是主答案“不同数据配不同一致性级别”的细粒度落地：全局最终一致 + 会话内读己之写，是互联网业务的标准配方。',
              ],
            },
          ],
        },
      ],
    },
  ],
};
